[
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 54,
    "EndLine": 54,
    "StartColumn": 23,
    "EndColumn": 152,
    "Match": "apInServerXpub = \"tpubDCvYeHUZisCMVTSfWDa1yevTf89NeF6TWxXUQwqkcmFrNvNdNvZQh1j4m4uTA4QcmPEwcrKVF8bJih1v16zDZacRr4j9MCAFQoSydKKy66q\"",
    "Secret": "tpubDCvYeHUZisCMVTSfWDa1yevTf89NeF6TWxXUQwqkcmFrNvNdNvZQh1j4m4uTA4QcmPEwcrKVF8bJih1v16zDZacRr4j9MCAFQoSydKKy66q",
    "File": "modules/core/src/commonTest/kotlin/fr/acinq/lightning/tests/TestConstants.kt",
    "SymlinkFile": "",
    "Commit": "41f78b2981009d51a2718ec25ca28cf1ee600b02",
    "Entropy": 5.446229,
    "Author": "bitever-ops",
    "Email": "bitever@example.com",
    "Date": "2026-05-07T00:26:29Z",
    "Message": "260507: BEC-fork eclair-kmp + close handler diagnostic logger\n\nInitial push of the BitEver fork of ACINQ lightning-kmp 1.11.5 with:\n- Header pass-through patch in ElectrumClient.kt for BEC chain SPV sync\n- Normal.kt close handler diagnostic logger (8 sites) under [260507-DBG]\n- Version bumped 1.11.5 -> 1.11.5-DEBUG to bypass gradle cache\n\nPhoenix app side (LightningEver-bitever-phoenix:260507) consumes this\nvia mavenLocal. See README for the full build & deploy flow.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "41f78b2981009d51a2718ec25ca28cf1ee600b02:modules/core/src/commonTest/kotlin/fr/acinq/lightning/tests/TestConstants.kt:generic-api-key:54",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 55,
    "EndLine": 55,
    "StartColumn": 21,
    "EndColumn": 150,
    "Match": "apInServerXpub = \"tpubDDt5vQap1awkyDXx1z1cP7QFKSZHDCCpbU8nSq9jy7X2grTjUVZDePexf6gc6AHtRRzkgfPW87K6EKUVV6t3Hu2hg7YkHkmMeLSfrP85x41\"",
    "Secret": "tpubDDt5vQap1awkyDXx1z1cP7QFKSZHDCCpbU8nSq9jy7X2grTjUVZDePexf6gc6AHtRRzkgfPW87K6EKUVV6t3Hu2hg7YkHkmMeLSfrP85x41",
    "File": "modules/core/src/commonTest/kotlin/fr/acinq/lightning/tests/TestConstants.kt",
    "SymlinkFile": "",
    "Commit": "41f78b2981009d51a2718ec25ca28cf1ee600b02",
    "Entropy": 5.457446,
    "Author": "bitever-ops",
    "Email": "bitever@example.com",
    "Date": "2026-05-07T00:26:29Z",
    "Message": "260507: BEC-fork eclair-kmp + close handler diagnostic logger\n\nInitial push of the BitEver fork of ACINQ lightning-kmp 1.11.5 with:\n- Header pass-through patch in ElectrumClient.kt for BEC chain SPV sync\n- Normal.kt close handler diagnostic logger (8 sites) under [260507-DBG]\n- Version bumped 1.11.5 -> 1.11.5-DEBUG to bypass gradle cache\n\nPhoenix app side (LightningEver-bitever-phoenix:260507) consumes this\nvia mavenLocal. See README for the full build & deploy flow.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "41f78b2981009d51a2718ec25ca28cf1ee600b02:modules/core/src/commonTest/kotlin/fr/acinq/lightning/tests/TestConstants.kt:generic-api-key:55",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 109,
    "EndLine": 110,
    "StartColumn": 39,
    "EndColumn": 1,
    "Match": "key: 1552dfba4f6cf29a62a0af13c8d6981d36d0ef8d61ba10fb0fe90da7634d7e1301",
    "Secret": "1552dfba4f6cf29a62a0af13c8d6981d36d0ef8d61ba10fb0fe90da7634d7e1301",
    "File": "modules/core/src/commonTest/kotlin/fr/acinq/lightning/transactions/AnchorOutputsTestsCommon.kt",
    "SymlinkFile": "",
    "Commit": "41f78b2981009d51a2718ec25ca28cf1ee600b02",
    "Entropy": 3.843512,
    "Author": "bitever-ops",
    "Email": "bitever@example.com",
    "Date": "2026-05-07T00:26:29Z",
    "Message": "260507: BEC-fork eclair-kmp + close handler diagnostic logger\n\nInitial push of the BitEver fork of ACINQ lightning-kmp 1.11.5 with:\n- Header pass-through patch in ElectrumClient.kt for BEC chain SPV sync\n- Normal.kt close handler diagnostic logger (8 sites) under [260507-DBG]\n- Version bumped 1.11.5 -> 1.11.5-DEBUG to bypass gradle cache\n\nPhoenix app side (LightningEver-bitever-phoenix:260507) consumes this\nvia mavenLocal. See README for the full build & deploy flow.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "41f78b2981009d51a2718ec25ca28cf1ee600b02:modules/core/src/commonTest/kotlin/fr/acinq/lightning/transactions/AnchorOutputsTestsCommon.kt:generic-api-key:109",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 120,
    "EndLine": 121,
    "StartColumn": 31,
    "EndColumn": 1,
    "Match": "key: 8deba327a7cc6d638ab0eb025770400a6184afcba6713c210d8d10e199ff2fda01",
    "Secret": "8deba327a7cc6d638ab0eb025770400a6184afcba6713c210d8d10e199ff2fda01",
    "File": "modules/core/src/commonTest/kotlin/fr/acinq/lightning/transactions/AnchorOutputsTestsCommon.kt",
    "SymlinkFile": "",
    "Commit": "41f78b2981009d51a2718ec25ca28cf1ee600b02",
    "Entropy": 3.8642378,
    "Author": "bitever-ops",
    "Email": "bitever@example.com",
    "Date": "2026-05-07T00:26:29Z",
    "Message": "260507: BEC-fork eclair-kmp + close handler diagnostic logger\n\nInitial push of the BitEver fork of ACINQ lightning-kmp 1.11.5 with:\n- Header pass-through patch in ElectrumClient.kt for BEC chain SPV sync\n- Normal.kt close handler diagnostic logger (8 sites) under [260507-DBG]\n- Version bumped 1.11.5 -> 1.11.5-DEBUG to bypass gradle cache\n\nPhoenix app side (LightningEver-bitever-phoenix:260507) consumes this\nvia mavenLocal. See README for the full build & deploy flow.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "41f78b2981009d51a2718ec25ca28cf1ee600b02:modules/core/src/commonTest/kotlin/fr/acinq/lightning/transactions/AnchorOutputsTestsCommon.kt:generic-api-key:120",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 122,
    "EndLine": 123,
    "StartColumn": 38,
    "EndColumn": 1,
    "Match": "key: adf3464ce9c2f230fd2582fda4c6965e4993ca5524e8c9580e3df0cf226981ad01",
    "Secret": "adf3464ce9c2f230fd2582fda4c6965e4993ca5524e8c9580e3df0cf226981ad01",
    "File": "modules/core/src/commonTest/kotlin/fr/acinq/lightning/transactions/AnchorOutputsTestsCommon.kt",
    "SymlinkFile": "",
    "Commit": "41f78b2981009d51a2718ec25ca28cf1ee600b02",
    "Entropy": 3.756368,
    "Author": "bitever-ops",
    "Email": "bitever@example.com",
    "Date": "2026-05-07T00:26:29Z",
    "Message": "260507: BEC-fork eclair-kmp + close handler diagnostic logger\n\nInitial push of the BitEver fork of ACINQ lightning-kmp 1.11.5 with:\n- Header pass-through patch in ElectrumClient.kt for BEC chain SPV sync\n- Normal.kt close handler diagnostic logger (8 sites) under [260507-DBG]\n- Version bumped 1.11.5 -> 1.11.5-DEBUG to bypass gradle cache\n\nPhoenix app side (LightningEver-bitever-phoenix:260507) consumes this\nvia mavenLocal. See README for the full build & deploy flow.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "41f78b2981009d51a2718ec25ca28cf1ee600b02:modules/core/src/commonTest/kotlin/fr/acinq/lightning/transactions/AnchorOutputsTestsCommon.kt:generic-api-key:122",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 19,
    "EndLine": 19,
    "StartColumn": 40,
    "EndColumn": 91,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "modules/core/src/commonTest/resources/nonreg/v4/Closing_Mutual_ebb9087c/data.json",
    "SymlinkFile": "",
    "Commit": "41f78b2981009d51a2718ec25ca28cf1ee600b02",
    "Entropy": 3.690942,
    "Author": "bitever-ops",
    "Email": "bitever@example.com",
    "Date": "2026-05-07T00:26:29Z",
    "Message": "260507: BEC-fork eclair-kmp + close handler diagnostic logger\n\nInitial push of the BitEver fork of ACINQ lightning-kmp 1.11.5 with:\n- Header pass-through patch in ElectrumClient.kt for BEC chain SPV sync\n- Normal.kt close handler diagnostic logger (8 sites) under [260507-DBG]\n- Version bumped 1.11.5 -> 1.11.5-DEBUG to bypass gradle cache\n\nPhoenix app side (LightningEver-bitever-phoenix:260507) consumes this\nvia mavenLocal. See README for the full build & deploy flow.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "41f78b2981009d51a2718ec25ca28cf1ee600b02:modules/core/src/commonTest/resources/nonreg/v4/Closing_Mutual_ebb9087c/data.json:generic-api-key:19",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 96,
    "EndLine": 96,
    "StartColumn": 35,
    "EndColumn": 108,
    "Match": "key\": \"02a73d5523148f33b473419816e41c58bfec5cb332ec86b9a80ce6f1825bca170c\"",
    "Secret": "02a73d5523148f33b473419816e41c58bfec5cb332ec86b9a80ce6f1825bca170c",
    "File": "modules/core/src/commonTest/resources/nonreg/v4/Closing_Mutual_ebb9087c/data.json",
    "SymlinkFile": "",
    "Commit": "41f78b2981009d51a2718ec25ca28cf1ee600b02",
    "Entropy": 3.872936,
    "Author": "bitever-ops",
    "Email": "bitever@example.com",
    "Date": "2026-05-07T00:26:29Z",
    "Message": "260507: BEC-fork eclair-kmp + close handler diagnostic logger\n\nInitial push of the BitEver fork of ACINQ lightning-kmp 1.11.5 with:\n- Header pass-through patch in ElectrumClient.kt for BEC chain SPV sync\n- Normal.kt close handler diagnostic logger (8 sites) under [260507-DBG]\n- Version bumped 1.11.5 -> 1.11.5-DEBUG to bypass gradle cache\n\nPhoenix app side (LightningEver-bitever-phoenix:260507) consumes this\nvia mavenLocal. See README for the full build & deploy flow.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "41f78b2981009d51a2718ec25ca28cf1ee600b02:modules/core/src/commonTest/resources/nonreg/v4/Closing_Mutual_ebb9087c/data.json:generic-api-key:96",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 167,
    "EndLine": 167,
    "StartColumn": 29,
    "EndColumn": 110,
    "Match": "KeyScript\": \"002057ca65f637bc83aaa046335b1aa7ef3b5347c2b40aef1c83c7831202a32cf73e\"",
    "Secret": "002057ca65f637bc83aaa046335b1aa7ef3b5347c2b40aef1c83c7831202a32cf73e",
    "File": "modules/core/src/commonTest/resources/nonreg/v4/Closing_Mutual_ebb9087c/data.json",
    "SymlinkFile": "",
    "Commit": "41f78b2981009d51a2718ec25ca28cf1ee600b02",
    "Entropy": 3.6884527,
    "Author": "bitever-ops",
    "Email": "bitever@example.com",
    "Date": "2026-05-07T00:26:29Z",
    "Message": "260507: BEC-fork eclair-kmp + close handler diagnostic logger\n\nInitial push of the BitEver fork of ACINQ lightning-kmp 1.11.5 with:\n- Header pass-through patch in ElectrumClient.kt for BEC chain SPV sync\n- Normal.kt close handler diagnostic logger (8 sites) under [260507-DBG]\n- Version bumped 1.11.5 -> 1.11.5-DEBUG to bypass gradle cache\n\nPhoenix app side (LightningEver-bitever-phoenix:260507) consumes this\nvia mavenLocal. See README for the full build & deploy flow.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "41f78b2981009d51a2718ec25ca28cf1ee600b02:modules/core/src/commonTest/resources/nonreg/v4/Closing_Mutual_ebb9087c/data.json:generic-api-key:167",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 180,
    "EndLine": 180,
    "StartColumn": 29,
    "EndColumn": 110,
    "Match": "KeyScript\": \"002057ca65f637bc83aaa046335b1aa7ef3b5347c2b40aef1c83c7831202a32cf73e\"",
    "Secret": "002057ca65f637bc83aaa046335b1aa7ef3b5347c2b40aef1c83c7831202a32cf73e",
    "File": "modules/core/src/commonTest/resources/nonreg/v4/Closing_Mutual_ebb9087c/data.json",
    "SymlinkFile": "",
    "Commit": "41f78b2981009d51a2718ec25ca28cf1ee600b02",
    "Entropy": 3

    "StartColumn": 35,
    "EndColumn": 108,
    "Match": "key\": \"027d2e4ca089015b111c4f6a2370d179f04ae26455a21d394a41374d6d79189758\"",
    "Secret": "027d2e4ca089015b111c4f6a2370d179f04ae26455a21d394a41374d6d79189758",
    "File": "modules/core/src/commonTest/resources/nonreg/v4/WaitForRemotePublishFutureCommitment_ebb9087c/data.json",
    "SymlinkFile": "",
    "Commit": "41f78b2981009d51a2718ec25ca28cf1ee600b02",
    "Entropy": 3.8402407,
    "Author": "bitever-ops",
    "Email": "bitever@example.com",
    "Date": "2026-05-07T00:26:29Z",
    "Message": "260507: BEC-fork eclair-kmp + close handler diagnostic logger\n\nInitial push of the BitEver fork of ACINQ lightning-kmp 1.11.5 with:\n- Header pass-through patch in ElectrumClient.kt for BEC chain SPV sync\n- Normal.kt close handler diagnostic logger (8 sites) under [260507-DBG]\n- Version bumped 1.11.5 -> 1.11.5-DEBUG to bypass gradle cache\n\nPhoenix app side (LightningEver-bitever-phoenix:260507) consumes this\nvia mavenLocal. See README for the full build & deploy flow.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "41f78b2981009d51a2718ec25ca28cf1ee600b02:modules/core/src/commonTest/resources/nonreg/v4/WaitForRemotePublishFutureCommitment_ebb9087c/data.json:generic-api-key:95",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 19,
    "EndLine": 19,
    "StartColumn": 40,
    "EndColumn": 91,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "modules/core/src/commonTest/resources/nonreg/v4/Normal_ff34df87/data.json",
    "SymlinkFile": "",
    "Commit": "41f78b2981009d51a2718ec25ca28cf1ee600b02",
    "Entropy": 3.690942,
    "Author": "bitever-ops",
    "Email": "bitever@example.com",
    "Date": "2026-05-07T00:26:29Z",
    "Message": "260507: BEC-fork eclair-kmp + close handler diagnostic logger\n\nInitial push of the BitEver fork of ACINQ lightning-kmp 1.11.5 with:\n- Header pass-through patch in ElectrumClient.kt for BEC chain SPV sync\n- Normal.kt close handler diagnostic logger (8 sites) under [260507-DBG]\n- Version bumped 1.11.5 -> 1.11.5-DEBUG to bypass gradle cache\n\nPhoenix app side (LightningEver-bitever-phoenix:260507) consumes this\nvia mavenLocal. See README for the full build & deploy flow.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "41f78b2981009d51a2718ec25ca28cf1ee600b02:modules/core/src/commonTest/resources/nonreg/v4/Normal_ff34df87/data.json:generic-api-key:19",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 99,
    "EndLine": 99,
    "StartColumn": 35,
    "EndColumn": 108,
    "Match": "key\": \"03ced98078c4a4cb054c071e90ad74dcfa4ec4235f7c43c5e3b50b84d75f58855e\"",
    "Secret": "03ced98078c4a4cb054c071e90ad74dcfa4ec4235f7c43c5e3b50b84d75f58855e",
    "File": "modules/core/src/commonTest/resources/nonreg/v4/Normal_ff34df87/data.json",
    "SymlinkFile": "",
    "Commit": "41f78b2981009d51a2718ec25ca28cf1ee600b02",
    "Entropy": 3.701919,
    "Author": "bitever-ops",
    "Email": "bitever@example.com",
    "Date": "2026-05-07T00:26:29Z",
    "Message": "260507: BEC-fork eclair-kmp + close handler diagnostic logger\n\nInitial push of the BitEver fork of ACINQ lightning-kmp 1.11.5 with:\n- Header pass-through patch in ElectrumClient.kt for BEC chain SPV sync\n- Normal.kt close handler diagnostic logger (8 sites) under [260507-DBG]\n- Version bumped 1.11.5 -> 1.11.5-DEBUG to bypass gradle cache\n\nPhoenix app side (LightningEver-bitever-phoenix:260507) consumes this\nvia mavenLocal. See README for the full build & deploy flow.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "41f78b2981009d51a2718ec25ca28cf1ee600b02:modules/core/src/commonTest/resources/nonreg/v4/Normal_ff34df87/data.json:generic-api-key:99",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 408,
    "EndLine": 408,
    "StartColumn": 39,
    "EndColumn": 112,
    "Match": "key\": \"03adb09c35563b342c924c2eab5a86789f9321c8dbbd351982cab0ec4c724de5d9\"",
    "Secret": "03adb09c35563b342c924c2eab5a86789f9321c8dbbd351982cab0ec4c724de5d9",
    "File": "modules/core/src/commonTest/resources/nonreg/v4/Normal_ff34df87/data.json",
    "SymlinkFile": "",
    "Commit": "41f78b2981009d51a2718ec25ca28cf1ee600b02",
    "Entropy": 3.8562179,
    "Author": "bitever-ops",
    "Email": "bitever@example.com",
    "Date": "2026-05-07T00:26:29Z",
    "Message": "260507: BEC-fork eclair-kmp + close handler diagnostic logger\n\nInitial push of the BitEver fork of ACINQ lightning-kmp 1.11.5 with:\n- Header pass-through patch in ElectrumClient.kt for BEC chain SPV sync\n- Normal.kt close handler diagnostic logger (8 sites) under [260507-DBG]\n- Version bumped 1.11.5 -> 1.11.5-DEBUG to bypass gradle cache\n\nPhoenix app side (LightningEver-bitever-phoenix:260507) consumes this\nvia mavenLocal. See README for the full build & deploy flow.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "41f78b2981009d51a2718ec25ca28cf1ee600b02:modules/core/src/commonTest/resources/nonreg/v4/Normal_ff34df87/data.json:generic-api-key:408",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 412,
    "EndLine": 412,
    "StartColumn": 37,
    "EndColumn": 94,
    "Match": "KeyScript\": \"00148948b4526e08b8166d8f2a14e0ceab30d5a70160\"",
    "Secret": "00148948b4526e08b8166d8f2a14e0ceab30d5a70160",
    "File": "modules/core/src/commonTest/resources/nonreg/v4/Normal_ff34df87/data.json",
    "SymlinkFile": "",
    "Commit": "41f78b2981009d51a2718ec25ca28cf1ee600b02",
    "Entropy": 3.7429364,
    "Author": "bitever-ops",
    "Email": "bitever@example.com",
    "Date": "2026-05-07T00:26:29Z",
    "Message": "260507: BEC-fork eclair-kmp + close handler diagnostic logger\n\nInitial push of the BitEver fork of ACINQ lightning-kmp 1.11.5 with:\n- Header pass-through patch in ElectrumClient.kt for BEC chain SPV sync\n- Normal.kt close handler diagnostic logger (8 sites) under [260507-DBG]\n- Version bumped 1.11.5 -> 1.11.5-DEBUG to bypass gradle cache\n\nPhoenix app side (LightningEver-bitever-phoenix:260507) consumes this\nvia mavenLocal. See README for the full build & deploy flow.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "41f78b2981009d51a2718ec25ca28cf1ee600b02:modules/core/src/commonTest/resources/nonreg/v4/Normal_ff34df87/data.json:generic-api-key:412",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 24,
    "EndLine": 24,
    "StartColumn": 40,
    "EndColumn": 91,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "modules/core/src/commonTest/resources/nonreg/v4/Closing_Mutual_ebb9087c/data.json",
    "SymlinkFile": "",
    "Commit": "9c32cc2850f8924c08c2602b5faa92a980e5684f",
    "Entropy": 3.690942,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2025-10-01T15:33:25Z",
    "Message": "Remove deprecated serialization format and fields (#685)\n\nThe v4 serialization format was released in July 2023 with better\nsupport for backwards-compatibility: the v2 and v3 formats used\nkotlin's serialization, which prevents us from removing legacy\nclasses and requires additional code whenever we change something\nin the channel state.\n\nUsers who haven't migrated their data to the v4 format will not be\nable to deserialize their channel data: they will need to use a\nprevious version of the app to read their old channel data, write\nit to the v4 format and then upgrade to the latest version.\n\nWe also remove a bunch of fields/options that were deprecated a\nwhile ago:\n\n- legacy channel funding states\n- unused legacy features\n- legacy node events\n- legacy encrypted channel backup",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "9c32cc2850f8924c08c2602b5faa92a980e5684f:modules/core/src/commonTest/resources/nonreg/v4/Closing_Mutual_ebb9087c/data.json:generic-api-key:24",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 104,
    "EndLine": 104,
    "StartColumn": 35,
    "EndColumn": 108,
    "Match": "key\": \"02a73d5523148f33b473419816e41c58bfec5cb332ec86b9a80ce6f1825bca170c\"",
    "Secret": "02a73d5523148f33b473419816e41c58bfec5cb332ec86b9a80ce6f1825bca170c",
    "File": "modules/core/src/commonTest/resources/nonreg/v4/Closing_Mutual_ebb9087c/data.json",
    "SymlinkFile": "",
    "Commit": "9c32cc2850f8924c08c2602b5faa92a980e5684f",
    "Entropy": 3.872936,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2025-10-01T15:33:25Z",
    "Message": "Remove deprecated serialization format and fields (#685)\n\nThe v4 serialization format was released in July 2023 with better\nsupport for backwards-compatibility: the v2 and v3 formats used\nkotlin's serialization, which prevents us from removing legacy\nclasses and requires additional code whenever we change something\nin the channel state.\n\nUsers who haven't migrated their data to the v4 format will not be\nable to deserialize their channel data: they will need to use a\nprevious version of the app to read their old channel data, write\nit to the v4 format and then upgrade to the latest version.\n\nWe also remove a bunch of fields/options that were deprecated a\nwhile ago:\n\n- legacy channel funding states\n- unused legacy features\n- legacy node events\n- legacy encrypted channel backup",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "9c32cc2850f8924c08c2602b5faa92a980e5684f:modules/core/src/commonTest/resources/nonreg/v4/Closing_Mutual_ebb9087c/data.json:generic-api-key:104",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 127,
    "EndLine": 127,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"002057ca65f637bc83aaa046335b1aa7ef3b5347c2b40aef1c83c7831202a32cf73e\"",
    "Secret": "002057ca65f637bc83aaa046335b1aa7ef3b5347c2b40aef1c83c7831202a32cf73e",
    "File": "modules/core/src/commonTest/resources/nonreg/v4/Closing_Mutual_ebb9087c/data.json",
    "SymlinkFile": "",
    "Commit": "9c32cc2850f8924c08c2602b5faa92a980e5684f",
    "Entropy": 3.6884527,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2025-10-01T15:33:25Z",
    "Message": "Remove deprecated serialization format and fields (#685)\n\nThe v4 serialization format was released in July 2023 with better\nsupport for backwards-compatibility: the v2 and v3 formats used\nkotlin's serialization, which prevents us from removing legacy\nclasses and requires additional code whenever we change something\nin the channel state.\n\nUsers who haven't migrated their data to the v4 format will not be\nable to deserialize their channel data: they will need to use a\nprevious version of the app to read their old channel data, write\nit to the v4 format and then upgrade to the latest version.\n\nWe also remove a bunch of fields/options that were deprecated a\nwhile ago:\n\n- legacy channel funding states\n- unused legacy features\n- legacy node events\n- legacy encrypted channel backup",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "9c32cc2850f8924c08c2602b5faa92a980e5684f:modules/core/src/commonTest/resources/nonreg/v4/Closing_Mutual_ebb9087c/data.json:generic-api-key:127",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 166,
    "EndLine": 166,
    "StartColumn": 29,
    "EndColumn": 110,
    "Match": "KeyScript\": \"002057ca65f637bc83aaa046335b1aa7ef3b5347c2b40aef1c83c7831202a32cf73e\"",
    "Secret": "002057ca65f637bc83aaa046335b1aa7ef3b5347c2b40aef1c83c7831202a32cf73e",
    "File": "modules/core/src/commonTest/resources/nonreg/v4/Closing_Mutual_ebb9087c/data.json",
    "SymlinkFile": "",
    "Commit": "9c32cc2850f8924c08c2602b5faa92a980e5684f",
    "Entropy": 3.6884527,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2025-10-01T15:33:25Z",
    "Message": "Remove deprecated serialization format and fields (#685)\n\nThe v4 serialization format was released in July 2023 with better\nsupport for backwards-compatibility: the v2 and v3 formats used\nkotlin's serialization, which prevents us from removing legacy\nclasses and requires additional code whenever we change something\nin the channel state.\n\nUsers who haven't migrated their data to the v4 format will not be\nable to deserialize their channel data: they will need to use a\nprevious version of the app to read their old channel data, write\nit to the v4 format and then upgrade to the latest version.\n\nWe also remove a bunch of fields/options that were deprecated a\nwhile ago:\n\n- legacy channel funding states\n- unused legacy features\n- legacy node events\n- legacy encrypted channel backup",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "9c32cc2850f8924c08c2602b5faa92a980e5684f:modules/core/src/commonTest/resources/nonreg/v4/Closing_Mutual_ebb9087c/data.json:generic-api-key:166",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 180,
    "EndLine": 180,
    "StartColumn": 29,
    "EndColumn": 110,
    "Match": "KeyScript\": \"002057ca65f637bc83aaa046335b1aa7ef3b5347c2b40aef1c83c7831202a32cf73e\"",
    "Secret": "002057ca65f637bc83aaa046335b1aa7ef3b5347c2b40aef1c83c7831202a32cf73e",
    "File": "modules/core/src/commonTest/resources/nonreg/v4/Closing_Mutual_ebb9087c/data.json",
    "SymlinkFile": "",
    "Commit": "9c32cc2850f8924c08c2602b5faa92a980e5684f",
    "Entropy": 3.6884527,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2025-10-01T15:33:25Z",
    "Message": "Remove deprecated serialization format and fields (#685)\n\nThe v4 serialization format was released in July 2023 with better\nsupport for backwards-compatibility: the v2 and v3 formats used\nkotlin's serialization, which prevents us from removing legacy\nclasses and requires additional code whenever we change something\nin the channel state.\n\nUsers who haven't migrated their data to the v4 format will not be\nable to deserialize their channel data: they will need to use a\nprevious version of the app to read their old channel data, write\nit to the v4 format and then upgrade to the latest version.\n\nWe also remove a bunch of fields/options that were deprecated a\nwhile ago:\n\n- legacy channel funding states\n- unused legacy features\n- legacy node events\n- legacy encrypted channel backup",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "9c32cc2850f8924c08c2602b5faa92a980e5684f:modules/core/src/commonTest/resources/nonreg/v4/Closing_Mutual_ebb9087c/data.json:generic-api-key:180",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 24,
    "EndLine": 24,
    "StartColumn": 40,
    "EndColumn": 91,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "modules/core/src/commonTest/resources/nonreg/v4/Closing_Local_ebb9087c/data.json",
    "SymlinkFile": "",
    "Commit": "9c32cc2850f8924c08c2602b5faa92a980e5684f",
    "Entropy": 3.690942,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2025-10-01T15:33:25Z",
    "Message": "Remove deprecated serialization format and fields (#685)\n\nThe v4 serialization format was released in July 2023 with better\nsupport for backwards-compatibility: the v2 and v3 formats used\nkotlin's serialization, which prevents us from removing legacy\nclasses and requires additional code whenever we change something\nin the channel state.\n\nUsers who haven't migrated their data to the v4 format will not be\nable to deserialize their channel data: they will need to use a\nprevious version of the app to read their old channel data, write\nit to the v4 format and then upgrade to the latest version.\n\nWe also remove a bunch of fields/options that were deprecated a\nwhile ago:\n\n- legacy channel funding states\n- unused legacy features\n- legacy node events\n- legacy encrypted channel backup",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "9c32cc2850f8924c08c2602b5faa92a980e5684f:modules/core/src/commonTest/resources/nonreg/v4/Closing_Local_ebb9087c/data.json:generic-api-key:24",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 104,
    "EndLine": 104,
    "StartColumn": 35,
    "EndColumn": 108,
    "Match": "key\": \"0262927c7f186107ea2b04522abda2e12dad48ff5d58646dda882b61f65cf6678f\"",
    "Secret": "0262927c7f186107ea2b04522abda2e12dad48ff5d58646dda882b61f65cf6678f",
    "File": "modules/core/src/commonTest/resources/nonreg/v4/Closing_Local_ebb9087c/data.json",
    "SymlinkFile": "",
    "Commit": "9c32cc2850f8924c08c2602b5faa92a980e5684f",
    "Entropy": 3.7272282,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2025-10-01T15:33:25Z",
    "Message": "Remove deprecated serialization format and fields (#685)\n\nThe v4 serialization format was released in July 2023 with better\nsupport for backwards-compatibility: the v2 and v3 formats used\nkotlin's serialization, which prevents us from removing legacy\nclasses and requires additional code whenever we change something\nin the channel state.\n\nUsers who haven't migrated their data to the v4 format will not be\nable to deserialize their channel data: they will need to use a\nprevious version of the app to read their old channel data, write\nit to the v4 format and then upgrade to the latest version.\n\nWe also remove a bunch of fields/options that were deprecated a\nwhile ago:\n\n- legacy channel funding states\n- unused legacy features\n- legacy node events\n- legacy encrypted channel backup",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "9c32cc2850f8924c08c2602b5faa92a980e5684f:modules/core/src/commonTest/resources/nonreg/v4/Closing_Local_ebb9087c/data.json:generic-api-key:104",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 144,
    "EndLine": 144,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"0020479c37a0e82b2fc0e0429b354c6a7a404ec4421c24ec04697691701ce841eff9\"",
    "Secret": "0020479c37a0e82b2fc0e0429b354c6a7a404ec4421c24ec04697691701ce841eff9",
    "File": "modules/core/src/commonTest/resources/nonreg/v4/Closing_Local_ebb9087c/data.json",
    "SymlinkFile": "",
    "Commit": "9c32cc2850f8924c08c2602b5faa92a980e5684f",
    "Entropy": 3.677116,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2025-10-01T15:33:25Z",
    "Message": "Remove deprecated serialization format and fields (#685)\n\nThe v4 serialization format was released in July 2023 with better\nsupport for backwards-compatibility: the v2 and v3 formats used\nkotlin's serialization, which prevents us from removing legacy\nclasses and requires additional code whenever we change something\nin the channel state.\n\nUsers who haven't migrated their data to the v4 format will not be\nable to deserialize their channel data: they will need to use a\nprevious version of the app to read their old channel data, write\nit to the v4 format and then upgrade to the latest version.\n\nWe also remove a bunch of fields/options that were deprecated a\nwhile ago:\n\n- legacy channel funding states\n- unused legacy features\n- legacy node events\n- legacy encrypted channel backup",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "9c32cc2850f8924c08c2602b5faa92a980e5684f:modules/core/src/commonTest/resources/nonreg/v4/Closing_Local_ebb9087c/data.json:generic-api-key:144",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 158,
    "EndLine": 158,
    "StartColumn": 53,
    "EndColumn": 134,
    "Match": "KeyScript\": \"00203b089cd6e67ed390b14fc4f427e31096e57d0255970f2a320711324cb978b86d\"",
    "Secret": "00203b089cd6e67ed390b14fc4f427e31096e57d0255970f2a320711324cb978b86d",
    "File": "modules/core/src/commonTest/resources/nonreg/v4/Closing_Local_ebb9087c/data.json",
    "SymlinkFile": "",
    "Commit": "9c32cc2850f8924c08c2602b5faa92a980e5684f",
    "Entropy": 3.

    "StartColumn": 34,
    "EndColumn": 107,
    "Match": "key\": \"02a23c28358ab531b3cc8782b7c43b38fe611e2b9b33d7cfa4d603e84ab2e470f2\"",
    "Secret": "02a23c28358ab531b3cc8782b7c43b38fe611e2b9b33d7cfa4d603e84ab2e470f2",
    "File": "modules/core/src/commonTest/resources/nonreg/v4/WaitForFundingConfirmed_fac54067/data.json",
    "SymlinkFile": "",
    "Commit": "9c32cc2850f8924c08c2602b5faa92a980e5684f",
    "Entropy": 3.813843,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2025-10-01T15:33:25Z",
    "Message": "Remove deprecated serialization format and fields (#685)\n\nThe v4 serialization format was released in July 2023 with better\nsupport for backwards-compatibility: the v2 and v3 formats used\nkotlin's serialization, which prevents us from removing legacy\nclasses and requires additional code whenever we change something\nin the channel state.\n\nUsers who haven't migrated their data to the v4 format will not be\nable to deserialize their channel data: they will need to use a\nprevious version of the app to read their old channel data, write\nit to the v4 format and then upgrade to the latest version.\n\nWe also remove a bunch of fields/options that were deprecated a\nwhile ago:\n\n- legacy channel funding states\n- unused legacy features\n- legacy node events\n- legacy encrypted channel backup",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "9c32cc2850f8924c08c2602b5faa92a980e5684f:modules/core/src/commonTest/resources/nonreg/v4/WaitForFundingConfirmed_fac54067/data.json:generic-api-key:107",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 126,
    "EndLine": 126,
    "StartColumn": 44,
    "EndColumn": 125,
    "Match": "KeyScript\": \"0020afa0e32aadb38f5e0bc47da245a8f3f7244c1969a771b4850d3b7a83d5b5e052\"",
    "Secret": "0020afa0e32aadb38f5e0bc47da245a8f3f7244c1969a771b4850d3b7a83d5b5e052",
    "File": "modules/core/src/commonTest/resources/nonreg/v4/WaitForRemotePublishFutureCommitment_ebb9087c/data.json",
    "SymlinkFile": "",
    "Commit": "9c32cc2850f8924c08c2602b5faa92a980e5684f",
    "Entropy": 3.852693,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2025-10-01T15:33:25Z",
    "Message": "Remove deprecated serialization format and fields (#685)\n\nThe v4 serialization format was released in July 2023 with better\nsupport for backwards-compatibility: the v2 and v3 formats used\nkotlin's serialization, which prevents us from removing legacy\nclasses and requires additional code whenever we change something\nin the channel state.\n\nUsers who haven't migrated their data to the v4 format will not be\nable to deserialize their channel data: they will need to use a\nprevious version of the app to read their old channel data, write\nit to the v4 format and then upgrade to the latest version.\n\nWe also remove a bunch of fields/options that were deprecated a\nwhile ago:\n\n- legacy channel funding states\n- unused legacy features\n- legacy node events\n- legacy encrypted channel backup",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "9c32cc2850f8924c08c2602b5faa92a980e5684f:modules/core/src/commonTest/resources/nonreg/v4/WaitForRemotePublishFutureCommitment_ebb9087c/data.json:generic-api-key:126",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 130,
    "EndLine": 130,
    "StartColumn": 44,
    "EndColumn": 125,
    "Match": "KeyScript\": \"00204ef1ad1e81b50fe0246e72589e627b4d97cea9600c0101726004f78f03fc3d2a\"",
    "Secret": "00204ef1ad1e81b50fe0246e72589e627b4d97cea9600c0101726004f78f03fc3d2a",
    "File": "modules/core/src/commonTest/resources/nonreg/v4/WaitForFundingConfirmed_fac54067/data.json",
    "SymlinkFile": "",
    "Commit": "9c32cc2850f8924c08c2602b5faa92a980e5684f",
    "Entropy": 3.813309,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2025-10-01T15:33:25Z",
    "Message": "Remove deprecated serialization format and fields (#685)\n\nThe v4 serialization format was released in July 2023 with better\nsupport for backwards-compatibility: the v2 and v3 formats used\nkotlin's serialization, which prevents us from removing legacy\nclasses and requires additional code whenever we change something\nin the channel state.\n\nUsers who haven't migrated their data to the v4 format will not be\nable to deserialize their channel data: they will need to use a\nprevious version of the app to read their old channel data, write\nit to the v4 format and then upgrade to the latest version.\n\nWe also remove a bunch of fields/options that were deprecated a\nwhile ago:\n\n- legacy channel funding states\n- unused legacy features\n- legacy node events\n- legacy encrypted channel backup",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "9c32cc2850f8924c08c2602b5faa92a980e5684f:modules/core/src/commonTest/resources/nonreg/v4/WaitForFundingConfirmed_fac54067/data.json:generic-api-key:130",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 24,
    "EndLine": 24,
    "StartColumn": 40,
    "EndColumn": 91,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "modules/core/src/commonTest/resources/nonreg/v4/Normal_ff34df87/data.json",
    "SymlinkFile": "",
    "Commit": "7d6d52c6cdf0862499137511621d586febad9712",
    "Entropy": 3.690942,
    "Author": "pm47",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2025-07-09T08:55:24Z",
    "Message": "Remove duplication of htlcs storage for `SpliceStatus` (#796)\n\nWhen creating a splice, we were storing two full copies of htlcs (for local and remote commits), that were already present in the main commitments.\n\nRemoving this duplication makes it possible to increase the default `maxAcceptedHtlcs` to 12.\n\nBest reviewed commit by commit, there are some preparatory commits, but the actual change is just one small commit.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "7d6d52c6cdf0862499137511621d586febad9712:modules/core/src/commonTest/resources/nonreg/v4/Normal_ff34df87/data.json:generic-api-key:24",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 107,
    "EndLine": 107,
    "StartColumn": 35,
    "EndColumn": 108,
    "Match": "key\": \"03ced98078c4a4cb054c071e90ad74dcfa4ec4235f7c43c5e3b50b84d75f58855e\"",
    "Secret": "03ced98078c4a4cb054c071e90ad74dcfa4ec4235f7c43c5e3b50b84d75f58855e",
    "File": "modules/core/src/commonTest/resources/nonreg/v4/Normal_ff34df87/data.json",
    "SymlinkFile": "",
    "Commit": "7d6d52c6cdf0862499137511621d586febad9712",
    "Entropy": 3.701919,
    "Author": "pm47",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2025-07-09T08:55:24Z",
    "Message": "Remove duplication of htlcs storage for `SpliceStatus` (#796)\n\nWhen creating a splice, we were storing two full copies of htlcs (for local and remote commits), that were already present in the main commitments.\n\nRemoving this duplication makes it possible to increase the default `maxAcceptedHtlcs` to 12.\n\nBest reviewed commit by commit, there are some preparatory commits, but the actual change is just one small commit.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "7d6d52c6cdf0862499137511621d586febad9712:modules/core/src/commonTest/resources/nonreg/v4/Normal_ff34df87/data.json:generic-api-key:107",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 228,
    "EndLine": 228,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"0020a72586c1e575ba88adf6196a86e6f30f79a115421be2baab5d81dbefd3f55004\"",
    "Secret": "0020a72586c1e575ba88adf6196a86e6f30f79a115421be2baab5d81dbefd3f55004",
    "File": "modules/core/src/commonTest/resources/nonreg/v4/Normal_ff34df87/data.json",
    "SymlinkFile": "",
    "Commit": "7d6d52c6cdf0862499137511621d586febad9712",
    "Entropy": 3.8592868,
    "Author": "pm47",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2025-07-09T08:55:24Z",
    "Message": "Remove duplication of htlcs storage for `SpliceStatus` (#796)\n\nWhen creating a splice, we were storing two full copies of htlcs (for local and remote commits), that were already present in the main commitments.\n\nRemoving this duplication makes it possible to increase the default `maxAcceptedHtlcs` to 12.\n\nBest reviewed commit by commit, there are some preparatory commits, but the actual change is just one small commit.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "7d6d52c6cdf0862499137511621d586febad9712:modules/core/src/commonTest/resources/nonreg/v4/Normal_ff34df87/data.json:generic-api-key:228",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 242,
    "EndLine": 242,
    "StartColumn": 53,
    "EndColumn": 134,
    "Match": "KeyScript\": \"00200c5f841665d884f5f2f1d3c2ba3ceb5a732792189108eed8527bcf102138b65e\"",
    "Secret": "00200c5f841665d884f5f2f1d3c2ba3ceb5a732792189108eed8527bcf102138b65e",
    "File": "modules/core/src/commonTest/resources/nonreg/v4/Normal_ff34df87/data.json",
    "SymlinkFile": "",
    "Commit": "7d6d52c6cdf0862499137511621d586febad9712",
    "Entropy": 3.8858953,
    "Author": "pm47",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2025-07-09T08:55:24Z",
    "Message": "Remove duplication of htlcs storage for `SpliceStatus` (#796)\n\nWhen creating a splice, we were storing two full copies of htlcs (for local and remote commits), that were already present in the main commitments.\n\nRemoving this duplication makes it possible to increase the default `maxAcceptedHtlcs` to 12.\n\nBest reviewed commit by commit, there are some preparatory commits, but the actual change is just one small commit.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "7d6d52c6cdf0862499137511621d586febad9712:modules/core/src/commonTest/resources/nonreg/v4/Normal_ff34df87/data.json:generic-api-key:242",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 260,
    "EndLine": 260,
    "StartColumn": 53,
    "EndColumn": 134,
    "Match": "KeyScript\": \"00202f500548eb86486434f03e36aadc6a41d711fae6ae0cf2f4a773abb49f301164\"",
    "Secret": "00202f500548eb86486434f03e36aadc6a41d711fae6ae0cf2f4a773abb49f301164",
    "File": "modules/core/src/commonTest/resources/nonreg/v4/Normal_ff34df87/data.json",
    "SymlinkFile": "",
    "Commit": "7d6d52c6cdf0862499137511621d586febad9712",
    "Entropy": 3.8093765,
    "Author": "pm47",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2025-07-09T08:55:24Z",
    "Message": "Remove duplication of htlcs storage for `SpliceStatus` (#796)\n\nWhen creating a splice, we were storing two full copies of htlcs (for local and remote commits), that were already present in the main commitments.\n\nRemoving this duplication makes it possible to increase the default `maxAcceptedHtlcs` to 12.\n\nBest reviewed commit by commit, there are some preparatory commits, but the actual change is just one small commit.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "7d6d52c6cdf0862499137511621d586febad9712:modules/core/src/commonTest/resources/nonreg/v4/Normal_ff34df87/data.json:generic-api-key:260",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 277,
    "EndLine": 277,
    "StartColumn": 53,
    "EndColumn": 134,
    "Match": "KeyScript\": \"002034ef6a0dd495579cc24720aea885bbecffbbd21a39cc57022e6829f1de718651\"",
    "Secret": "002034ef6a0dd495579cc24720aea885bbecffbbd21a39cc57022e6829f1de718651",
    "File": "modules/core/src/commonTest/resources/nonreg/v4/Normal_ff34df87/data.json",
    "SymlinkFile": "",
    "Commit": "7d6d52c6cdf0862499137511621d586febad9712",
    "Entropy": 3

    "File": "modules/core/src/commonTest/resources/nonreg/v4/Normal_77f198a3/data.json",
    "SymlinkFile": "",
    "Commit": "7d6d52c6cdf0862499137511621d586febad9712",
    "Entropy": 3.882958,
    "Author": "pm47",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2025-07-09T08:55:24Z",
    "Message": "Remove duplication of htlcs storage for `SpliceStatus` (#796)\n\nWhen creating a splice, we were storing two full copies of htlcs (for local and remote commits), that were already present in the main commitments.\n\nRemoving this duplication makes it possible to increase the default `maxAcceptedHtlcs` to 12.\n\nBest reviewed commit by commit, there are some preparatory commits, but the actual change is just one small commit.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "7d6d52c6cdf0862499137511621d586febad9712:modules/core/src/commonTest/resources/nonreg/v4/Normal_77f198a3/data.json:generic-api-key:819",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 837,
    "EndLine": 837,
    "StartColumn": 53,
    "EndColumn": 134,
    "Match": "KeyScript\": \"0020a6cd1e408296f9717d423c942a72bb96edc570a6f795c5c3a004905b6b81c454\"",
    "Secret": "0020a6cd1e408296f9717d423c942a72bb96edc570a6f795c5c3a004905b6b81c454",
    "File": "modules/core/src/commonTest/resources/nonreg/v4/Normal_77f198a3/data.json",
    "SymlinkFile": "",
    "Commit": "7d6d52c6cdf0862499137511621d586febad9712",
    "Entropy": 3.874556,
    "Author": "pm47",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2025-07-09T08:55:24Z",
    "Message": "Remove duplication of htlcs storage for `SpliceStatus` (#796)\n\nWhen creating a splice, we were storing two full copies of htlcs (for local and remote commits), that were already present in the main commitments.\n\nRemoving this duplication makes it possible to increase the default `maxAcceptedHtlcs` to 12.\n\nBest reviewed commit by commit, there are some preparatory commits, but the actual change is just one small commit.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "7d6d52c6cdf0862499137511621d586febad9712:modules/core/src/commonTest/resources/nonreg/v4/Normal_77f198a3/data.json:generic-api-key:837",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 854,
    "EndLine": 854,
    "StartColumn": 53,
    "EndColumn": 134,
    "Match": "KeyScript\": \"0020c04e8ba0a096777552a15d0bffd3e8b3e5ca25e8c205f693c6c8e082d82147f9\"",
    "Secret": "0020c04e8ba0a096777552a15d0bffd3e8b3e5ca25e8c205f693c6c8e082d82147f9",
    "File": "modules/core/src/commonTest/resources/nonreg/v4/Normal_77f198a3/data.json",
    "SymlinkFile": "",
    "Commit": "7d6d52c6cdf0862499137511621d586febad9712",
    "Entropy": 3.8808103,
    "Author": "pm47",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2025-07-09T08:55:24Z",
    "Message": "Remove duplication of htlcs storage for `SpliceStatus` (#796)\n\nWhen creating a splice, we were storing two full copies of htlcs (for local and remote commits), that were already present in the main commitments.\n\nRemoving this duplication makes it possible to increase the default `maxAcceptedHtlcs` to 12.\n\nBest reviewed commit by commit, there are some preparatory commits, but the actual change is just one small commit.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "7d6d52c6cdf0862499137511621d586febad9712:modules/core/src/commonTest/resources/nonreg/v4/Normal_77f198a3/data.json:generic-api-key:854",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 871,
    "EndLine": 871,
    "StartColumn": 53,
    "EndColumn": 134,
    "Match": "KeyScript\": \"0020c72184dac180911bebc44365a7c2d496fa3bd61788fe29b8c810646ee5b9e4ee\"",
    "Secret": "0020c72184dac180911bebc44365a7c2d496fa3bd61788fe29b8c810646ee5b9e4ee",
    "File": "modules/core/src/commonTest/resources/nonreg/v4/Normal_77f198a3/data.json",
    "SymlinkFile": "",
    "Commit": "7d6d52c6cdf0862499137511621d586febad9712",
    "Entropy": 3.8979917,
    "Author": "pm47",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2025-07-09T08:55:24Z",
    "Message": "Remove duplication of htlcs storage for `SpliceStatus` (#796)\n\nWhen creating a splice, we were storing two full copies of htlcs (for local and remote commits), that were already present in the main commitments.\n\nRemoving this duplication makes it possible to increase the default `maxAcceptedHtlcs` to 12.\n\nBest reviewed commit by commit, there are some preparatory commits, but the actual change is just one small commit.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "7d6d52c6cdf0862499137511621d586febad9712:modules/core/src/commonTest/resources/nonreg/v4/Normal_77f198a3/data.json:generic-api-key:871",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 889,
    "EndLine": 889,
    "StartColumn": 53,
    "EndColumn": 134,
    "Match": "KeyScript\": \"0020e65a8e5c40a36330ef15774d15d2093d30b73e7f3c7e7560a2fe49abaeb82ee8\"",
    "Secret": "0020e65a8e5c40a36330ef15774d15d2093d30b73e7f3c7e7560a2fe49abaeb82ee8",
    "File": "modules/core/src/commonTest/resources/nonreg/v4/Normal_77f198a3/data.json",
    "SymlinkFile": "",
    "Commit": "7d6d52c6cdf0862499137511621d586febad9712",
    "Entropy": 3.8310041,
    "Author": "pm47",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2025-07-09T08:55:24Z",
    "Message": "Remove duplication of htlcs storage for `SpliceStatus` (#796)\n\nWhen creating a splice, we were storing two full copies of htlcs (for local and remote commits), that were already present in the main commitments.\n\nRemoving this duplication makes it possible to increase the default `maxAcceptedHtlcs` to 12.\n\nBest reviewed commit by commit, there are some preparatory commits, but the actual change is just one small commit.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "7d6d52c6cdf0862499137511621d586febad9712:modules/core/src/commonTest/resources/nonreg/v4/Normal_77f198a3/data.json:generic-api-key:889",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 38,
    "EndLine": 38,
    "StartColumn": 22,
    "EndColumn": 151,
    "Match": "apInServerXpub = \"tpubDCvYeHUZisCMVTSfWDa1yevTf89NeF6TWxXUQwqkcmFrNvNdNvZQh1j4m4uTA4QcmPEwcrKVF8bJih1v16zDZacRr4j9MCAFQoSydKKy66q\"",
    "Secret": "tpubDCvYeHUZisCMVTSfWDa1yevTf89NeF6TWxXUQwqkcmFrNvNdNvZQh1j4m4uTA4QcmPEwcrKVF8bJih1v16zDZacRr4j9MCAFQoSydKKy66q",
    "File": "src/commonTest/kotlin/fr/acinq/lightning/tests/TestConstants.kt",
    "SymlinkFile": "",
    "Commit": "6618b516714ec40fc7f1c5e3899248e531cd531b",
    "Entropy": 5.446229,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-06-15T09:11:46Z",
    "Message": "Derive per-user swap-in server keys (#482)\n\nSwap-in servers provide an xpub, that can then be used to derive the\npublic key they'll use for each peer deterministically (based on every\npeer's `node_id`).",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "6618b516714ec40fc7f1c5e3899248e531cd531b:src/commonTest/kotlin/fr/acinq/lightning/tests/TestConstants.kt:generic-api-key:38",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 39,
    "EndLine": 39,
    "StartColumn": 21,
    "EndColumn": 150,
    "Match": "apInServerXpub = \"tpubDDt5vQap1awkyDXx1z1cP7QFKSZHDCCpbU8nSq9jy7X2grTjUVZDePexf6gc6AHtRRzkgfPW87K6EKUVV6t3Hu2hg7YkHkmMeLSfrP85x41\"",
    "Secret": "tpubDDt5vQap1awkyDXx1z1cP7QFKSZHDCCpbU8nSq9jy7X2grTjUVZDePexf6gc6AHtRRzkgfPW87K6EKUVV6t3Hu2hg7YkHkmMeLSfrP85x41",
    "File": "src/commonTest/kotlin/fr/acinq/lightning/tests/TestConstants.kt",
    "SymlinkFile": "",
    "Commit": "6618b516714ec40fc7f1c5e3899248e531cd531b",
    "Entropy": 5.457446,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-06-15T09:11:46Z",
    "Message": "Derive per-user swap-in server keys (#482)\n\nSwap-in servers provide an xpub, that can then be used to derive the\npublic key they'll use for each peer deterministically (based on every\npeer's `node_id`).",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "6618b516714ec40fc7f1c5e3899248e531cd531b:src/commonTest/kotlin/fr/acinq/lightning/tests/TestConstants.kt:generic-api-key:39",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 116,
    "EndLine": 116,
    "StartColumn": 34,
    "EndColumn": 107,
    "Match": "key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v2/Closing_0ba41d17/data.json",
    "SymlinkFile": "",
    "Commit": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2023-05-04T16:14:21Z",
    "Message": "Dynamic funding pubkey (#447)\n\nA different funding pubkey is used for each splice.\n\n---------\n\nCo-authored-by: t-bast <bastien@acinq.fr>",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b:src/commonTest/resources/nonreg/v2/Closing_0ba41d17/data.json:generic-api-key:116",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 110,
    "EndLine": 110,
    "StartColumn": 34,
    "EndColumn": 107,
    "Match": "key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v2/Closing_0efffae3/data.json",
    "SymlinkFile": "",
    "Commit": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2023-05-04T16:14:21Z",
    "Message": "Dynamic funding pubkey (#447)\n\nA different funding pubkey is used for each splice.\n\n---------\n\nCo-authored-by: t-bast <bastien@acinq.fr>",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b:src/commonTest/resources/nonreg/v2/Closing_0efffae3/data.json:generic-api-key:110",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 110,
    "EndLine": 110,
    "StartColumn": 34,
    "EndColumn": 107,
    "Match": "key\": \"02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c\"",
    "Secret": "02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c",
    "File": "src/commonTest/resources/nonreg/v2/Closing_0ed6ff68/data.json",
    "SymlinkFile": "",
    "Commit": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b",
    "Entropy": 3.815461,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2023-05-04T16:14:21Z",
    "Message": "Dynamic funding pubkey (#447)\n\nA different funding pubkey is used for each splice.\n\n---------\n\nCo-authored-by: t-bast <bastien@acinq.fr>",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b:src/commonTest/resources/nonreg/v2/Closing_0ed6ff68/data.json:generic-api-key:110",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 116,
    "EndLine": 116,
    "StartColumn": 34,
    "EndColumn": 107,
    "Match": "key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v2/Closing_3bb07fb6/data.json",
    "SymlinkFile": "",
    "Commit": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2023-05-04T16:14:21Z",
    "Message": "Dynamic funding pubkey (#447)\n\nA different funding pubkey is used for each splice.\n\n---------\n\nCo-authored-by: t-bast <bastien@acinq.fr>",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b:src/commonTest/resources/nonreg/v2/Closing_3bb07fb6/data.json:generic-api-key:116",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 131,
    "EndLine": 131,
    "StartColumn": 34,
    "EndColumn": 107,
    "Match": "key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v2/Closing_2fd2a3fa/data.json",
    "SymlinkFile": "",
    "Commit": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2023-05-04T16:14:21Z",
    "Message": "Dynamic funding pubkey (#447)\n\nA different funding pubkey is used for each splice.\n\n---------\n\nCo-authored-by: t-bast <bastien@acinq.fr>",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b:src/commonTest/resources/nonreg/v2/Closing_2fd2a3fa/data.json:generic-api-key:131",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 109,
    "EndLine": 109,
    "StartColumn": 34,
    "EndColumn": 107,
    "Match": "key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v2/Closing_ef682e2e/data.json",
    "SymlinkFile": "",
    "Commit": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2023-05-04T16:14:21Z",
    "Message": "Dynamic funding pubkey (#447)\n\nA different funding pubkey is used for each splice.\n\n---------\n\nCo-authored-by: t-bast <bastien@acinq.fr>",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b:src/commonTest/resources/nonreg/v2/Closing_ef682e2e/data.json:generic-api-key:109",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 110,
    "EndLine": 110,
    "StartColumn": 34,
    "EndColumn": 107,
    "Match": "key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v2/Closing_8f1a524e/data.json",
    "SymlinkFile": "",
    "Commit": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2023-05-04T16:14:21Z",
    "Message": "Dynamic funding pubkey (#447)\n\nA different funding pubkey is used for each splice.\n\n---------\n\nCo-authored-by: t-bast <bastien@acinq.fr>",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b:src/commonTest/resources/nonreg/v2/Closing_8f1a524e/data.json:generic-api-key:110",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 110,
    "EndLine": 110,
    "StartColumn": 34,
    "EndColumn": 107,
    "Match": "key\": \"02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c\"",
    "Secret": "02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c",
    "File": "src/commonTest/resources/nonreg/v2/Negotiating_d9b4cd96/data.json",
    "SymlinkFile": "",
    "Commit": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b",
    "Entropy": 3.815461,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2023-05-04T16:14:21Z",
    "Message": "Dynamic funding pubkey (#447)\n\nA different funding pubkey is used for each splice.\n\n---------\n\nCo-authored-by: t-bast <bastien@acinq.fr>",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b:src/commonTest/resources/nonreg/v2/Negotiating_d9b4cd96/data.json:generic-api-key:110",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 110,
    "EndLine": 110,
    "StartColumn": 34,
    "EndColumn": 107,
    "Match": "key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v2/Negotiating_c8d15808/data.json",
    "SymlinkFile": "",
    "Commit": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2023-05-04T16:14:21Z",
    "Message": "Dynamic funding pubkey (#447)\n\nA different funding pubkey is used for each splice.\n\n---------\n\nCo-authored-by: t-bast <bastien@acinq.fr>",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b:src/commonTest/resources/nonreg/v2/Negotiating_c8d15808/data.json:generic-api-key:110",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 110,
    "EndLine": 110,
    "StartColumn": 34,
    "EndColumn": 107,
    "Match": "key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v2/Negotiating_f52b19b8/data.json",
    "SymlinkFile": "",
    "Commit": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2023-05-04T16:14:21Z",
    "Message": "Dynamic funding pubkey (#447)\n\nA different funding pubkey is used for each splice.\n\n---------\n\nCo-authored-by: t-bast <bastien@acinq.fr>",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b:src/commonTest/resources/nonreg/v2/Negotiating_f52b19b8/data.json:generic-api-key:110",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 110,
    "EndLine": 110,
    "StartColumn": 34,
    "EndColumn": 107,
    "Match": "key\": \"02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c\"",
    "Secret": "02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c",
    "File": "src/commonTest/resources/nonreg/v2/Negotiating_ee10091c/data.json",
    "SymlinkFile": "",
    "Commit": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b",
    "Entropy": 3.815461,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2023-05-04T16:14:21Z",
    "Message": "Dynamic funding pubkey (#447)\n\nA different funding pubkey is used for each splice.\n\n---------\n\nCo-authored-by: t-bast <bastien@acinq.fr>",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b:src/commonTest/resources/nonreg/v2/Negotiating_ee10091c/data.json:generic-api-key:110",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 137,
    "EndLine": 137,
    "StartColumn": 34,
    "EndColumn": 107,
    "Match": "key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v2/Normal_e2253ddd/data.json",
    "SymlinkFile": "",
    "Commit": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2023-05-04T16:14:21Z",
    "Message": "Dynamic funding pubkey (#447)\n\nA different funding pubkey is used for each splice.\n\n---------\n\nCo-authored-by: t-bast <bastien@acinq.fr>",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b:src/commonTest/resources/nonreg/v2/Normal_e2253ddd/data.json:generic-api-key:137",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 109,
    "EndLine": 109,
    "StartColumn": 34,
    "EndColumn": 107,
    "Match": "key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v2/Normal_748a735b/data.json",
    "SymlinkFile": "",
    "Commit": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2023-05-04T16:14:21Z",
    "Message": "Dynamic funding pubkey (#447)\n\nA different funding pubkey is used for each splice.\n\n---------\n\nCo-authored-by: t-bast <bastien@acinq.fr>",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b:src/commonTest/resources/nonreg/v2/Normal_748a735b/data.json:generic-api-key:109",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 128,
    "EndLine": 128,
    "StartColumn": 34,
    "EndColumn": 107,
    "Match": "key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v2/Normal_ff4a71b6/data.json",
    "SymlinkFile": "",
    "Commit": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2023-05-04T16:14:21Z",
    "Message": "Dynamic funding pubkey (#447)\n\nA different funding pubkey is used for each splice.\n\n---------\n\nCo-authored-by: t-bast <bastien@acinq.fr>",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b:src/commonTest/resources/nonreg/v2/Normal_ff4a71b6/data.json:generic-api-key:128",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 110,
    "EndLine": 110,
    "StartColumn": 34,
    "EndColumn": 107,
    "Match": "key\": \"02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c\"",
    "Secret": "02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c",
    "File": "src/commonTest/resources/nonreg/v2/Normal_ff248f8d/data.json",
    "SymlinkFile": "",
    "Commit": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b",
    "Entropy": 3.815461,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2023-05-04T16:14:21Z",
    "Message": "Dynamic funding pubkey (#447)\n\nA different funding pubkey is used for each splice.\n\n---------\n\nCo-authored-by: t-bast <bastien@acinq.fr>",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b:src/commonTest/resources/nonreg/v2/Normal_ff248f8d/data.json:generic-api-key:110",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 110,
    "EndLine": 110,
    "StartColumn": 34,
    "EndColumn": 107,
    "Match": "key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v2/ShuttingDown_c321b947/data.json",
    "SymlinkFile": "",
    "Commit": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2023-05-04T16:14:21Z",
    "Message": "Dynamic funding pubkey (#447)\n\nA different funding pubkey is used for each splice.\n\n---------\n\nCo-authored-by: t-bast <bastien@acinq.fr>",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b:src/commonTest/resources/nonreg/v2/ShuttingDown_c321b947/data.json:generic-api-key:110",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 115,
    "EndLine": 115,
    "StartColumn": 34,
    "EndColumn": 107,
    "Match": "key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v2/Normal_ffd9f5db/data.json",
    "SymlinkFile": "",
    "Commit": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2023-05-04T16:14:21Z",
    "Message": "Dynamic funding pubkey (#447)\n\nA different funding pubkey is used for each splice.\n\n---------\n\nCo-authored-by: t-bast <bastien@acinq.fr>",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b:src/commonTest/resources/nonreg/v2/Normal_ffd9f5db/data.json:generic-api-key:115",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 110,
    "EndLine": 110,
    "StartColumn": 34,
    "EndColumn": 107,
    "Match": "key\": \"02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c\"",
    "Secret": "02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c",
    "File": "src/commonTest/resources/nonreg/v2/WaitForFundingConfirmed_f7421b49/data.json",
    "SymlinkFile": "",
    "Commit": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b",
    "Entropy": 3.815461,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2023-05-04T16:14:21Z",
    "Message": "Dynamic funding pubkey (#447)\n\nA different funding pubkey is used for each splice.\n\n---------\n\nCo-authored-by: t-bast <bastien@acinq.fr>",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b:src/commonTest/resources/nonreg/v2/WaitForFundingConfirmed_f7421b49/data.json:generic-api-key:110",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 116,
    "EndLine": 116,
    "StartColumn": 34,
    "EndColumn": 107,
    "Match": "key\": \"02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c\"",
    "Secret": "02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c",
    "File": "src/commonTest/resources/nonreg/v2/ShuttingDown_f89ecd50/data.json",
    "SymlinkFile": "",
    "Commit": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b",
    "Entropy": 3.815461,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2023-05-04T16:14:21Z",
    "Message": "Dynamic funding pubkey (#447)\n\nA different funding pubkey is used for each splice.\n\n---------\n\nCo-authored-by: t-bast <bastien@acinq.fr>",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b:src/commonTest/resources/nonreg/v2/ShuttingDown_f89ecd50/data.json:generic-api-key:116",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 111,
    "EndLine": 111,
    "StartColumn": 34,
    "EndColumn": 107,
    "Match": "key\": \"02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c\"",
    "Secret": "02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c",
    "File": "src/commonTest/resources/nonreg/v2/WaitForFundingConfirmed_fe3c5978/data.json",
    "SymlinkFile": "",
    "Commit": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b",
    "Entropy": 3.815461,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2023-05-04T16:14:21Z",
    "Message": "Dynamic funding pubkey (#447)\n\nA different funding pubkey is used for each splice.\n\n---------\n\nCo-authored-by: t-bast <bastien@acinq.fr>",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b:src/commonTest/resources/nonreg/v2/WaitForFundingConfirmed_fe3c5978/data.json:generic-api-key:111",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 109,
    "EndLine": 109,
    "StartColumn": 34,
    "EndColumn": 107,
    "Match": "key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v2/WaitForRemotePublishFutureCommitment_ae47fde9/data.json",
    "SymlinkFile": "",
    "Commit": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2023-05-04T16:14:21Z",
    "Message": "Dynamic funding pubkey (#447)\n\nA different funding pubkey is used for each splice.\n\n---------\n\nCo-authored-by: t-bast <bastien@acinq.fr>",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b:src/commonTest/resources/nonreg/v2/WaitForRemotePublishFutureCommitment_ae47fde9/data.json:generic-api-key:109",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 110,
    "EndLine": 110,
    "StartColumn": 34,
    "EndColumn": 107,
    "Match": "key\": \"02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c\"",
    "Secret": "02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c",
    "File": "src/commonTest/resources/nonreg/v2/WaitForFundingLocked_f3437082/data.json",
    "SymlinkFile": "",
    "Commit": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b",
    "Entropy": 3.815461,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2023-05-04T16:14:21Z",
    "Message": "Dynamic funding pubkey (#447)\n\nA different funding pubkey is used for each splice.\n\n---------\n\nCo-authored-by: t-bast <bastien@acinq.fr>",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b:src/commonTest/resources/nonreg/v2/WaitForFundingLocked_f3437082/data.json:generic-api-key:110",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 120,
    "EndLine": 120,
    "StartColumn": 34,
    "EndColumn": 107,
    "Match": "key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v3/Closing_029bf8f3/data.json",
    "SymlinkFile": "",
    "Commit": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2023-05-04T16:14:21Z",
    "Message": "Dynamic funding pubkey (#447)\n\nA different funding pubkey is used for each splice.\n\n---------\n\nCo-authored-by: t-bast <bastien@acinq.fr>",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b:src/commonTest/resources/nonreg/v3/Closing_029bf8f3/data.json:generic-api-key:120",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 109,
    "EndLine": 109,
    "StartColumn": 34,
    "EndColumn": 107,
    "Match": "key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v2/WaitForRemotePublishFutureCommitment_d803549f/data.json",
    "SymlinkFile": "",
    "Commit": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2023-05-04T16:14:21Z",
    "Message": "Dynamic funding pubkey (#447)\n\nA different funding pubkey is used for each splice.\n\n---------\n\nCo-authored-by: t-bast <bastien@acinq.fr>",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b:src/commonTest/resources/nonreg/v2/WaitForRemotePublishFutureCommitment_d803549f/data.json:generic-api-key:109",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 108,
    "EndLine": 108,
    "StartColumn": 34,
    "EndColumn": 107,
    "Match": "key\": \"02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c\"",
    "Secret": "02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c",
    "File": "src/commonTest/resources/nonreg/v3/Closing_0ed6ff68/data.json",
    "SymlinkFile": "",
    "Commit": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b",
    "Entropy": 3.815461,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2023-05-04T16:14:21Z",
    "Message": "Dynamic funding pubkey (#447)\n\nA different funding pubkey is used for each splice.\n\n---------\n\nCo-authored-by: t-bast <bastien@acinq.fr>",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b:src/commonTest/resources/nonreg/v3/Closing_0ed6ff68/data.json:generic-api-key:108",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 114,
    "EndLine": 114,
    "StartColumn": 34,
    "EndColumn": 107,
    "Match": "key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v3/Closing_0ba41d17/data.json",
    "SymlinkFile": "",
    "Commit": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2023-05-04T16:14:21Z",
    "Message": "Dynamic funding pubkey (#447)\n\nA different funding pubkey is used for each splice.\n\n---------\n\nCo-authored-by: t-bast <bastien@acinq.fr>",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b:src/commonTest/resources/nonreg/v3/Closing_0ba41d17/data.json:generic-api-key:114",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 114,
    "EndLine": 114,
    "StartColumn": 34,
    "EndColumn": 107,
    "Match": "key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v3/Closing_ebbd24bc/data.json",
    "SymlinkFile": "",
    "Commit": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2023-05-04T16:14:21Z",
    "Message": "Dynamic funding pubkey (#447)\n\nA different funding pubkey is used for each splice.\n\n---------\n\nCo-authored-by: t-bast <bastien@acinq.fr>",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b:src/commonTest/resources/nonreg/v3/Closing_ebbd24bc/data.json:generic-api-key:114",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 108,
    "EndLine": 108,
    "StartColumn": 34,
    "EndColumn": 107,
    "Match": "key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v3/Closing_0efffae3/data.json",
    "SymlinkFile": "",
    "Commit": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2023-05-04T16:14:21Z",
    "Message": "Dynamic funding pubkey (#447)\n\nA different funding pubkey is used for each splice.\n\n---------\n\nCo-authored-by: t-bast <bastien@acinq.fr>",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b:src/commonTest/resources/nonreg/v3/Closing_0efffae3/data.json:generic-api-key:108",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 108,
    "EndLine": 108,
    "StartColumn": 34,
    "EndColumn": 107,
    "Match": "key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v3/Negotiating_da44c6e2/data.json",
    "SymlinkFile": "",
    "Commit": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2023-05-04T16:14:21Z",
    "Message": "Dynamic funding pubkey (#447)\n\nA different funding pubkey is used for each splice.\n\n---------\n\nCo-authored-by: t-bast <bastien@acinq.fr>",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b:src/commonTest/resources/nonreg/v3/Negotiating_da44c6e2/data.json:generic-api-key:108",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 110,
    "EndLine": 110,
    "StartColumn": 34,
    "EndColumn": 107,
    "Match": "key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v2/WaitForFundingConfirmed_ff74dd33/data.json",
    "SymlinkFile": "",
    "Commit": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2023-05-04T16:14:21Z",
    "Message": "Dynamic funding pubkey (#447)\n\nA different funding pubkey is used for each splice.\n\n---------\n\nCo-authored-by: t-bast <bastien@acinq.fr>",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b:src/commonTest/resources/nonreg/v2/WaitForFundingConfirmed_ff74dd33/data.json:generic-api-key:110",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 107,
    "EndLine": 107,
    "StartColumn": 34,
    "EndColumn": 107,
    "Match": "key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v3/Closing_f137669f/data.json",
    "SymlinkFile": "",
    "Commit": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2023-05-04T16:14:21Z",
    "Message": "Dynamic funding pubkey (#447)\n\nA different funding pubkey is used for each splice.\n\n---------\n\nCo-authored-by: t-bast <bastien@acinq.fr>",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b:src/commonTest/resources/nonreg/v3/Closing_f137669f/data.json:generic-api-key:107",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 110,
    "EndLine": 110,
    "StartColumn": 34,
    "EndColumn": 107,
    "Match": "key\": \"02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c\"",
    "Secret": "02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c",
    "File": "src/commonTest/resources/nonreg/v3/Negotiating_fadb50c1/data.json",
    "SymlinkFile": "",
    "Commit": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b",
    "Entropy": 3.815461,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2023-05-04T16:14:21Z",
    "Message": "Dynamic funding pubkey (#447)\n\nA different funding pubkey is used for each splice.\n\n---------\n\nCo-authored-by: t-bast <bastien@acinq.fr>",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b:src/commonTest/resources/nonreg/v3/Negotiating_fadb50c1/data.json:generic-api-key:110",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 108,
    "EndLine": 108,
    "StartColumn": 34,
    "EndColumn": 107,
    "Match": "key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v3/Negotiating_dabbed55/data.json",
    "SymlinkFile": "",
    "Commit": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2023-05-04T16:14:21Z",
    "Message": "Dynamic funding pubkey (#447)\n\nA different funding pubkey is used for each splice.\n\n---------\n\nCo-authored-by: t-bast <bastien@acinq.fr>",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b:src/commonTest/resources/nonreg/v3/Negotiating_dabbed55/data.json:generic-api-key:108",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 117,
    "EndLine": 117,
    "StartColumn": 34,
    "EndColumn": 107,
    "Match": "key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v3/Normal_fe897b64/data.json",
    "SymlinkFile": "",
    "Commit": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2023-05-04T16:14:21Z",
    "Message": "Dynamic funding pubkey (#447)\n\nA different funding pubkey is used for each splice.\n\n---------\n\nCo-authored-by: t-bast <bastien@acinq.fr>",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b:src/commonTest/resources/nonreg/v3/Normal_fe897b64/data.json:generic-api-key:117",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 108,
    "EndLine": 108,
    "StartColumn": 34,
    "EndColumn": 107,
    "Match": "key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v3/Normal_fd10d3cc/data.json",
    "SymlinkFile": "",
    "Commit": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2023-05-04T16:14:21Z",
    "Message": "Dynamic funding pubkey (#447)\n\nA different funding pubkey is used for each splice.\n\n---------\n\nCo-authored-by: t-bast <bastien@acinq.fr>",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b:src/commonTest/resources/nonreg/v3/Normal_fd10d3cc/data.json:generic-api-key:108",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 126,
    "EndLine": 126,
    "StartColumn": 34,
    "EndColumn": 107,
    "Match": "key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v3/Normal_ff4a71b6/data.json",
    "SymlinkFile": "",
    "Commit": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2023-05-04T16:14:21Z",
    "Message": "Dynamic funding pubkey (#447)\n\nA different funding pubkey is used for each splice.\n\n---------\n\nCo-authored-by: t-bast <bastien@acinq.fr>",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b:src/commonTest/resources/nonreg/v3/Normal_ff4a71b6/data.json:generic-api-key:126",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 108,
    "EndLine": 108,
    "StartColumn": 34,
    "EndColumn": 107,
    "Match": "key\": \"02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c\"",
    "Secret": "02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c",
    "File": "src/commonTest/resources/nonreg/v3/Normal_ff248f8d/data.json",
    "SymlinkFile": "",
    "Commit": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b",
    "Entropy": 3.815461,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2023-05-04T16:14:21Z",
    "Message": "Dynamic funding pubkey (#447)\n\nA different funding pubkey is used for each splice.\n\n---------\n\nCo-authored-by: t-bast <bastien@acinq.fr>",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b:src/commonTest/resources/nonreg/v3/Normal_ff248f8d/data.json:generic-api-key:108",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 117,
    "EndLine": 117,
    "StartColumn": 34,
    "EndColumn": 107,
    "Match": "key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v3/ShuttingDown_ef7081a1/data.json",
    "SymlinkFile": "",
    "Commit": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2023-05-04T16:14:21Z",
    "Message": "Dynamic funding pubkey (#447)\n\nA different funding pubkey is used for each splice.\n\n---------\n\nCo-authored-by: t-bast <bastien@acinq.fr>",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b:src/commonTest/resources/nonreg/v3/ShuttingDown_ef7081a1/data.json:generic-api-key:117",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 114,
    "EndLine": 114,
    "StartColumn": 34,
    "EndColumn": 107,
    "Match": "key\": \"02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c\"",
    "Secret": "02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c",
    "File": "src/commonTest/resources/nonreg/v3/ShuttingDown_ef41a1a5/data.json",
    "SymlinkFile": "",
    "Commit": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b",
    "Entropy": 3.815461,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2023-05-04T16:14:21Z",
    "Message": "Dynamic funding pubkey (#447)\n\nA different funding pubkey is used for each splice.\n\n---------\n\nCo-authored-by: t-bast <bastien@acinq.fr>",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b:src/commonTest/resources/nonreg/v3/ShuttingDown_ef41a1a5/data.json:generic-api-key:114",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 108,
    "EndLine": 108,
    "StartColumn": 34,
    "EndColumn": 107,
    "Match": "key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v3/WaitForFundingConfirmed_ff74dd33/data.json",
    "SymlinkFile": "",
    "Commit": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2023-05-04T16:14:21Z",
    "Message": "Dynamic funding pubkey (#447)\n\nA different funding pubkey is used for each splice.\n\n---------\n\nCo-authored-by: t-bast <bastien@acinq.fr>",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b:src/commonTest/resources/nonreg/v3/WaitForFundingConfirmed_ff74dd33/data.json:generic-api-key:108",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 111,
    "EndLine": 111,
    "StartColumn": 34,
    "EndColumn": 107,
    "Match": "key\": \"02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c\"",
    "Secret": "02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c",
    "File": "src/commonTest/resources/nonreg/v3/WaitForFundingConfirmed_fe3c5978/data.json",
    "SymlinkFile": "",
    "Commit": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b",
    "Entropy": 3.815461,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2023-05-04T16:14:21Z",
    "Message": "Dynamic funding pubkey (#447)\n\nA different funding pubkey is used for each splice.\n\n---------\n\nCo-authored-by: t-bast <bastien@acinq.fr>",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b:src/commonTest/resources/nonreg/v3/WaitForFundingConfirmed_fe3c5978/data.json:generic-api-key:111",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 107,
    "EndLine": 107,
    "StartColumn": 34,
    "EndColumn": 107,
    "Match": "key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v3/WaitForRemotePublishFutureCommitment_ae47fde9/data.json",
    "SymlinkFile": "",
    "Commit": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2023-05-04T16:14:21Z",
    "Message": "Dynamic funding pubkey (#447)\n\nA different funding pubkey is used for each splice.\n\n---------\n\nCo-authored-by: t-bast <bastien@acinq.fr>",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b:src/commonTest/resources/nonreg/v3/WaitForRemotePublishFutureCommitment_ae47fde9/data.json:generic-api-key:107",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 108,
    "EndLine": 108,
    "StartColumn": 34,
    "EndColumn": 107,
    "Match": "key\": \"02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c\"",
    "Secret": "02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c",
    "File": "src/commonTest/resources/nonreg/v3/WaitForFundingLocked_f3437082/data.json",
    "SymlinkFile": "",
    "Commit": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b",
    "Entropy": 3.815461,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2023-05-04T16:14:21Z",
    "Message": "Dynamic funding pubkey (#447)\n\nA different funding pubkey is used for each splice.\n\n---------\n\nCo-authored-by: t-bast <bastien@acinq.fr>",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b:src/commonTest/resources/nonreg/v3/WaitForFundingLocked_f3437082/data.json:generic-api-key:108",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 107,
    "EndLine": 107,
    "StartColumn": 34,
    "EndColumn": 107,
    "Match": "key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v3/WaitForRemotePublishFutureCommitment_d803549f/data.json",
    "SymlinkFile": "",
    "Commit": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2023-05-04T16:14:21Z",
    "Message": "Dynamic funding pubkey (#447)\n\nA different funding pubkey is used for each splice.\n\n---------\n\nCo-authored-by: t-bast <bastien@acinq.fr>",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "86db4c8bdb71dffa0bc9abfd84ee22da12085c5b:src/commonTest/resources/nonreg/v3/WaitForRemotePublishFutureCommitment_d803549f/data.json:generic-api-key:107",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 27,
    "EndLine": 27,
    "StartColumn": 11,
    "EndColumn": 0,
    "Match": "apInOnChainWallet: Bip84OnChainKeys",
    "Secret": "Bip84OnChainKeys",
    "File": "src/commonMain/kotlin/fr/acinq/lightning/crypto/KeyManager.kt",
    "SymlinkFile": "",
    "Commit": "364195a57be1fcbb271956510de784957fdd84e9",
    "Entropy": 3.75,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2023-05-04T11:58:37Z",
    "Message": "`KeyManager` cleanup (#446)\n\nThe `KeyManager` is now simply a repository of keys, it is not responsible for signing transactions.\n\nThe interface has been simplified and returns classes of type `NodeKeys`, `ChannelKeys` and `Bip84OnChainKeys`. The latter is used for final and swap-in wallets.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "364195a57be1fcbb271956510de784957fdd84e9:src/commonMain/kotlin/fr/acinq/lightning/crypto/KeyManager.kt:generic-api-key:27",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 25,
    "EndLine": 25,
    "StartColumn": 40,
    "EndColumn": 91,
    "Match": "Key\": \"001434947cfb2e8f6054ddf12daed4308cbe342580d1\"",
    "Secret": "001434947cfb2e8f6054ddf12daed4308cbe342580d1",
    "File": "src/commonTest/resources/nonreg/v2/Closing_0ed6ff68/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.7944684,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v2/Closing_0ed6ff68/data.json:generic-api-key:25",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 57,
    "EndLine": 57,
    "StartColumn": 29,
    "EndColumn": 102,
    "Match": "Key\": \"02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c\"",
    "Secret": "02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c",
    "File": "src/commonTest/resources/nonreg/v2/Closing_0ed6ff68/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.815461,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v2/Closing_0ed6ff68/data.json:generic-api-key:57",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 25,
    "EndLine": 25,
    "StartColumn": 40,
    "EndColumn": 91,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "src/commonTest/resources/nonreg/v2/Closing_2fd2a3fa/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.690942,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v2/Closing_2fd2a3fa/data.json:generic-api-key:25",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 57,
    "EndLine": 57,
    "StartColumn": 29,
    "EndColumn": 102,
    "Match": "Key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v2/Closing_2fd2a3fa/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.8222544,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v2/Closing_2fd2a3fa/data.json:generic-api-key:57",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 187,
    "EndLine": 187,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/Closing_2fd2a3fa/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.7707121,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v2/Closing_2fd2a3fa/data.json:generic-api-key:187",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 134,
    "EndLine": 134,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/Closing_0ed6ff68/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.7707121,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v2/Closing_0ed6ff68/data.json:generic-api-key:134",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 25,
    "EndLine": 25,
    "StartColumn": 40,
    "EndColumn": 91,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "src/commonTest/resources/nonreg/v2/Closing_0ba41d17/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.690942,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v2/Closing_0ba41d17/data.json:generic-api-key:25",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 57,
    "EndLine": 57,
    "StartColumn": 29,
    "EndColumn": 102,
    "Match": "Key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v2/Closing_0ba41d17/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.8222544,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v2/Closing_0ba41d17/data.json:generic-api-key:57",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 201,
    "EndLine": 201,
    "StartColumn": 53,
    "EndColumn": 134,
    "Match": "KeyScript\": \"0020dcb30d6a2f2d4b21e5b65464e8254696ac36e858a51001f7b201cc8b5f9fd053\"",
    "Secret": "0020dcb30d6a2f2d4b21e5b65464e8254696ac36e858a51001f7b201cc8b5f9fd053",
    "File": "src/commonTest/resources/nonreg/v2/Closing_2fd2a3fa/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.873561,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v2/Closing_2fd2a3fa/data.json:generic-api-key:201",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 25,
    "EndLine": 25,
    "StartColumn": 40,
    "EndColumn": 91,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "src/commonTest/resources/nonreg/v2/Closing_0efffae3/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.690942,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v2/Closing_0efffae3/data.json:generic-api-key:25",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 57,
    "EndLine": 57,
    "StartColumn": 29,
    "EndColumn": 102,
    "Match": "Key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v2/Closing_0efffae3/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.8222544,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v2/Closing_0efffae3/data.json:generic-api-key:57",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 156,
    "EndLine": 156,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/Closing_0ba41d17/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.7707121,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v2/Closing_0ba41d17/data.json:generic-api-key:156",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 219,
    "EndLine": 219,
    "StartColumn": 53,
    "EndColumn": 134,
    "Match": "KeyScript\": \"

    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/Closing_0ba41d17/data.json:generic-api-key:168",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 23,
    "EndLine": 23,
    "StartColumn": 40,
    "EndColumn": 91,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "src/commonTest/resources/nonreg/v3/Closing_029bf8f3/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.690942,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/Closing_029bf8f3/data.json:generic-api-key:23",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 55,
    "EndLine": 55,
    "StartColumn": 29,
    "EndColumn": 102,
    "Match": "Key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v3/Closing_029bf8f3/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.8222544,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/Closing_029bf8f3/data.json:generic-api-key:55",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 185,
    "EndLine": 185,
    "StartColumn": 53,
    "EndColumn": 134,
    "Match": "KeyScript\": \"002028eee5bc7a7219b098d1f27ee035b83a6870d2be541097068097ab8a8d2568fb\"",
    "Secret": "002028eee5bc7a7219b098d1f27ee035b83a6870d2be541097068097ab8a8d2568fb",
    "File": "src/commonTest/resources/nonreg/v3/Closing_0ba41d17/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.7810938,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/Closing_0ba41d17/data.json:generic-api-key:185",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 184,
    "EndLine": 184,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/Closing_029bf8f3/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.7707121,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/Closing_029bf8f3/data.json:generic-api-key:184",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 198,
    "EndLine": 198,
    "StartColumn": 53,
    "EndColumn": 134,
    "Match": "KeyScript\": \"00200a56c86dc2e94046c61370f8e91b9cf2b8ceeb1e5597b8b3ea2191ac32076835\"",
    "Secret": "00200a56c86dc2e94046c61370f8e91b9cf2b8ceeb1e5597b8b3ea2191ac32076835",
    "File": "src/commonTest/resources/nonreg/v3/Closing_029bf8f3/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.8839533,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/Closing_029bf8f3/data.json:generic-api-key:198",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 23,
    "EndLine": 23,
    "StartColumn": 40,
    "EndColumn": 91,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "src/commonTest/resources/nonreg/v3/Closing_0efffae3/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.690942,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/Closing_0efffae3/data.json:generic-api-key:23",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 55,
    "EndLine": 55,
    "StartColumn": 29,
    "EndColumn": 102,
    "Match": "Key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v3/Closing_0efffae3/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.8222544,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/Closing_0efffae3/data.json:generic-api-key:55",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 23,
    "EndLine": 23,
    "StartColumn": 40,
    "EndColumn": 91,
    "Match": "Key\": \"001434947cfb2e8f6054ddf12daed4308cbe342580d1\"",
    "Secret": "001434947cfb2e8f6054ddf12daed4308cbe342580d1",
    "File": "src/commonTest/resources/nonreg/v3/Closing_0ed6ff68/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.7944684,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/Closing_0ed6ff68/data.json:generic-api-key:23",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 55,
    "EndLine": 55,
    "StartColumn": 29,
    "EndColumn": 102,
    "Match": "Key\": \"02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c\"",
    "Secret": "02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c",
    "File": "src/commonTest/resources/nonreg/v3/Closing_0ed6ff68/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.815461,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/Closing_0ed6ff68/data.json:generic-api-key:55",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 216,
    "EndLine": 216,
    "StartColumn": 53,
    "EndColumn": 134,
    "Match": "KeyScript\": \"002074e2cfb625e3787193fbb4f26dbd7739c5484c4455fc53c71bb2bce84b476ff7\"",
    "Secret": "002074e2cfb625e3787193fbb4f26dbd7739c5484c4455fc53c71bb2bce84b476ff7",
    "File": "src/commonTest/resources/nonreg/v3/Closing_029bf8f3/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.7454278,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/Closing_029bf8f3/data.json:generic-api-key:216",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 132,
    "EndLine": 132,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/Closing_0efffae3/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.7707121,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/Closing_0efffae3/data.json:generic-api-key:132",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 132,
    "EndLine": 132,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/Closing_0ed6ff68/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.7707121,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/Closing_0ed6ff68/data.json:generic-api-key:132",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 23,
    "EndLine": 23,
    "StartColumn": 40,
    "EndColumn": 91,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "src/commonTest/resources/nonreg/v3/Closing_f137669f/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.690942,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/Closing_f137669f/data.json:generic-api-key:23",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 55,
    "EndLine": 55,
    "StartColumn": 29,
    "EndColumn": 102,
    "Match": "Key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v3/Closing_f137669f/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.8222544,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/Closing_f137669f/data.json:generic-api-key:55",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 234,
    "EndLine": 234,
    "StartColumn": 53,
    "EndColumn": 134,
    "Match": "KeyScript\": \"00205d9ebea209786542771331647e3bfbe27d502165721f79d5fa7c650fc0f4f821\"",
    "Secret": "00205d9ebea209786542771331647e3bfbe27d502165721f79d5fa7c650fc0f4f821",
    "File": "src/commonTest/resources/nonreg/v3/Closing_029bf8f3/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.8564835,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/Closing_029bf8f3/data.json:generic-api-key:234",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 251,
    "EndLine": 251,
    "StartColumn": 53,
    "EndColumn": 134,
    "Match": "KeyScript\": \"00204d3d669bcc3a84c7fddb2b53587dc6e478a73ddc56fc6d741b8d8101275738ec\"",
    "Secret": "00204d3d669bcc3a84c7fddb2b53587dc6e478a73ddc56fc6d741b8d8101275738ec",
    "File": "src/commonTest/resources/nonreg/v3/Closing_029bf8f3/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.821711,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/Closing_029bf8f3/data.json:generic-api-key:251",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 131,
    "EndLine": 131,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/Closing_f137669f/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.7707121,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/Closing_f137669f/data.json:generic-api-key:131",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 23,
    "EndLine": 23,
    "StartColumn": 40,
    "EndColumn": 91,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "src/commonTest/resources/nonreg/v3/Closing_ebbd24bc/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.690942,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/Closing_ebbd24bc/data.json:generic-api-key:23",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 55,
    "EndLine": 55,
    "StartColumn": 29,
    "EndColumn": 102,
    "Match": "Key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v3/Closing_ebbd24bc/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.8222544,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/Closing_ebbd24bc/data.json:generic-api-key:55",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 154,
    "EndLine": 154,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/Closing_ebbd24bc/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.7707121,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/Closing_ebbd24bc/data.json:generic-api-key:154",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 23,
    "EndLine": 23,
    "StartColumn": 40,
    "EndColumn": 91,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "src/commonTest/resources/nonreg/v3/Negotiating_dabbed55/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.690942,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/Negotiating_dabbed55/data.json:generic-api-key:23",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 55,
    "EndLine": 55,
    "StartColumn": 29,
    "EndColumn": 102,
    "Match": "Key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v3/Negotiating_dabbed55/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.8222544,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/Negotiating_dabbed55/data.json:generic-api-key:55",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 23,
    "EndLine": 23,
    "StartColumn": 40,
    "EndColumn": 91,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "src/commonTest/resources/nonreg/v3/Negotiating_da44c6e2/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.690942,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/Negotiating_da44c6e2/data.json:generic-api-key:23",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 55,
    "EndLine": 55,
    "StartColumn": 29,
    "EndColumn": 102,
    "Match": "Key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v3/Negotiating_da44c6e2/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.8222544,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/Negotiating_da44c6e2/data.json:generic-api-key:55",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 168,
    "EndLine": 168,
    "StartColumn": 53,
    "EndColumn": 134,
    "Match": "KeyScript\": \"0020b5abcef667acd30c1793157728653afaf2ea391a4039bc875b6ad468cc2f603a\"",
    "Secret": "0020b5abcef667acd30c1793157728653afaf2ea391a4039bc875b6ad468cc2f603a",
    "File": "src/commonTest/resources/nonreg/v3/Closing_ebbd24bc/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.882854,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/Closing_ebbd24bc/data.json:generic-api-key:168",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 132,
    "EndLine": 132,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/Negotiating_dabbed55/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.7707121,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/Negotiating_dabbed55/data.json:generic-api-key:132",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 185,
    "EndLine": 185,
    "StartColumn": 53,
    "EndColumn": 134,
    "Match": "KeyScript\": \"0020c24f3aaf87264753868abbf958e8e7dc70352378c068e4c47b02fefc0acd114f\"",
    "Secret": "0020c24f3aaf87264753868abbf958e8e7dc70352378c068e4c47b02fefc0acd114f",
    "File": "src/commonTest/resources/nonreg/v3/Closing_ebbd24bc/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.862224,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/Closing_ebbd24bc/data.json:generic-api-key:185",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 132,
    "EndLine": 132,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/Negotiating_da44c6e2/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.7707121,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/Negotiating_da44c6e2/data.json:generic-api-key:132",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 23,
    "EndLine": 23,
    "StartColumn": 40,
    "EndColumn": 91,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "src/commonTest/resources/nonreg/v3/Normal_fd10d3cc/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.690942,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/Normal_fd10d3cc/data.json:generic-api-key:23",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 55,
    "EndLine": 55,
    "StartColumn": 29,
    "EndColumn": 102,
    "Match": "Key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v3/Normal_fd10d3cc/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.8222544,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/Normal_fd10d3cc/data.json:generic-api-key:55",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 148,
    "EndLine": 148,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/Normal_fd10d3cc/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.7707121,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/Normal_fd10d3cc/data.json:generic-api-key:148",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 23,
    "EndLine": 23,
    "StartColumn": 40,
    "EndColumn": 91,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "src/commonTest/resources/nonreg/v3/Normal_fe897b64/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.690942,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/Normal_fe897b64/data.json:generic-api-key:23",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 55,
    "EndLine": 55,
    "StartColumn": 29,
    "EndColumn": 102,
    "Match": "Key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v3/Normal_fe897b64/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.8222544,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/Normal_fe897b64/data.json:generic-api-key:55",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 162,
    "EndLine": 162,
    "StartColumn": 53,
    "EndColumn": 134,
    "Match": "KeyScript\": \"00200d94818c9b1a70b689daa5af63b6c6737ea40a9de55839819bf0421217d68d80\"",
    "Secret": "00200d94818c9b1a70b689daa5af63b6c6737ea40a9de55839819bf0421217d68d80",
    "File": "src/commonTest/resources/nonreg/v3/Normal_fd10d3cc/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.8739407,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/Normal_fd10d3cc/data.json:generic-api-key:162",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 179,
    "EndLine": 179,
    "StartColumn": 53,
    "EndColumn": 134,
    "Match": "KeyScript\": \"0020c9b6634f8ea3a3789e96570513e9bc701395ea27477c07e80931a2ab9bbb549c\"",
    "Secret": "0020c9b6634f8ea3a3789e96570513e9bc701395ea27477c07e80931a2ab9bbb549c",
    "File": "src/commonTest/resources/nonreg/v3/Normal_fd10d3cc/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.7739887,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/Normal_fd10d3cc/data.json:generic-api-key:179",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 141,
    "EndLine": 141,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/Normal_fe897b64/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.7707121,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/Normal_fe897b64/data.json:generic-api-key:141",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 23,
    "EndLine": 23,
    "StartColumn": 40,
    "EndColumn": 91,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "src/commonTest/resources/nonreg/v3/Normal_ff4a71b6/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.690942,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/Normal_ff4a71b6/data.json:generic-api-key:23",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 55,
    "EndLine": 55,
    "StartColumn": 29,
    "EndColumn": 102,
    "Match": "Key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v3/Normal_ff4a71b6/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.8222544,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/Normal_ff4a71b6/data.json:generic-api-key:55",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 23,
    "EndLine": 23,
    "StartColumn": 40,
    "EndColumn": 91,
    "Match": "Key\": \"001434947cfb2e8f6054ddf12daed4308cbe342580d1\"",
    "Secret": "001434947cfb2e8f6054ddf12daed4308cbe342580d1",
    "File": "src/commonTest/resources/nonreg/v3/Negotiating_fadb50c1/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.7944684,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/Negotiating_fadb50c1/data.json:generic-api-key:23",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 56,
    "EndLine": 56,
    "StartColumn": 29,
    "EndColumn": 102,
    "Match": "Key\": \"02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c\"",
    "Secret": "02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c",
    "File": "src/commonTest/resources/nonreg/v3/Negotiating_fadb50c1/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.815461,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/Negotiating_fadb50c1/data.json:generic-api-key:56",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 134,
    "EndLine": 134,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/Negotiating_fadb50c1/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.7707121,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/Negotiating_fadb50c1/data.json:generic-api-key:134",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 23,
    "EndLine": 23,
    "StartColumn": 40,
    "EndColumn": 91,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "src/commonTest/resources/nonreg/v3/ShuttingDown_ef7081a1/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.690942,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/ShuttingDown_ef7081a1/data.json:generic-api-key:23",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 55,
    "EndLine": 55,
    "StartColumn": 29,
    "EndColumn": 102,
    "Match": "Key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v3/ShuttingDown_ef7081a1/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.8222544,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/ShuttingDown_ef7081a1/data.json:generic-api-key:55",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 182,
    "EndLine": 182,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/Normal_ff4a71b6/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.7707121,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/Normal_ff4a71b6/data.json:generic-api-key:182",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 196,
    "EndLine": 196,
    "StartColumn": 53,
    "EndColumn": 134,
    "Match": "KeyScript\": \"00208bf1a46fa0bd7ca44eda0c5df968544c280859a29a527641afac57c27bf7003f\"",
    "Secret": "00208bf1a46fa0bd7ca44eda0c5df968544c280859a29a527641afac57c27bf7003f",
    "File": "src/commonTest/resources/nonreg/v3/Normal_ff4a71b6/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.8157325,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/Normal_ff4a71b6/data.json:generic-api-key:196",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 213,
    "EndLine": 213,
    "StartColumn": 53,
    "EndColumn": 134,
    "Match": "KeyScript\": \"0020de61d4f9864b11e111190b9d11f62ebf7476d517a7bba5b0a5b0f80f4e54eb1f\"",
    "Secret": "0020de61d4f9864b11e111190b9d11f62ebf7476d517a7bba5b0a5b0f80f4e54eb1f",
    "File": "src/commonTest/resources/nonreg/v3/Normal_ff4a71b6/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.647109,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/Normal_ff4a71b6/data.json:generic-api-key:213",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 230,
    "EndLine": 230,
    "StartColumn": 53,
    "EndColumn": 134,
    "Match": "KeyScript\": \"00207805c9f22468069984db4db780b44c97593e4fee2c8ee32114f1c9b4a6d70fe4\"",
    "Secret": "00207805c9f22468069984db4db780b44c97593e4fee2c8ee32114f1c9b4a6d70fe4",
    "File": "src/commonTest/resources/nonreg/v3/Normal_ff4a71b6/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.8421054,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/Normal_ff4a71b6/data.json:generic-api-key:230",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 149,
    "EndLine": 149,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/ShuttingDown_ef7081a1/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.7707121,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/ShuttingDown_ef7081a1/data.json:generic-api-key:149",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 23,
    "EndLine": 23,
    "StartColumn": 40,
    "EndColumn": 91,
    "Match": "Key\": \"001434947cfb2e8f6054ddf12daed4308cbe342580d1\"",
    "Secret": "001434947cfb2e8f6054ddf12daed4308cbe342580d1",
    "File": "src/commonTest/resources/nonreg/v3/Normal_ff248f8d/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.7944684,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/Normal_ff248f8d/data.json:generic-api-key:23",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 55,
    "EndLine": 55,
    "StartColumn": 29,
    "EndColumn": 102,
    "Match": "Key\": \"02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c\"",
    "Secret": "02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c",
    "File": "src/commonTest/resources/nonreg/v3/Normal_ff248f8d/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.815461,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/Normal_ff248f8d/data.json:generic-api-key:55",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 163,
    "EndLine": 163,
    "StartColumn": 53,
    "EndColumn": 134,
    "Match": "KeyScript\": \"0020e274bcf50e49b0edcce64e854d3aa8da60dbe1e7c48f9110936dadfa5187b58f\"",
    "Secret": "0020e274bcf50e49b0edcce64e854d3aa8da60dbe1e7c48f9110936dadfa5187b58f",
    "File": "src/commonTest/resources/nonreg/v3/ShuttingDown_ef7081a1/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.9123697,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/ShuttingDown_ef7081a1/data.json:generic-api-key:163",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 132,
    "EndLine": 132,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/Normal_ff248f8d/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.7707121,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/Normal_ff248f8d/data.json:generic-api-key:132",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 23,
    "EndLine": 23,
    "StartColumn": 40,
    "EndColumn": 91,
    "Match": "Key\": \"001434947cfb2e8f6054ddf12daed4308cbe342580d1\"",
    "Secret": "001434947cfb2e8f6054ddf12daed4308cbe342580d1",
    "File": "src/commonTest/resources/nonreg/v3/ShuttingDown_ef41a1a5/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.7944684,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/ShuttingDown_ef41a1a5/data.json:generic-api-key:23",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 55,
    "EndLine": 55,
    "StartColumn": 29,
    "EndColumn": 102,
    "Match": "Key\": \"02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c\"",
    "Secret": "02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c",
    "File": "src/commonTest/resources/nonreg/v3/ShuttingDown_ef41a1a5/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.815461,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/ShuttingDown_ef41a1a5/data.json:generic-api-key:55",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 23,
    "EndLine": 23,
    "StartColumn": 40,
    "EndColumn": 91,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "src/commonTest/resources/nonreg/v3/WaitForFundingConfirmed_ff74dd33/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.690942,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/WaitForFundingConfirmed_ff74dd33/data.json:generic-api-key:23",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 55,
    "EndLine": 55,
    "StartColumn": 29,
    "EndColumn": 102,
    "Match": "Key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v3/WaitForFundingConfirmed_ff74dd33/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.8222544,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/WaitForFundingConfirmed_ff74dd33/data.json:generic-api-key:55",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 25,
    "EndLine": 25,
    "StartColumn": 40,
    "EndColumn": 91,
    "Match": "Key\": \"001434947cfb2e8f6054ddf12daed4308cbe342580d1\"",
    "Secret": "001434947cfb2e8f6054ddf12daed4308cbe342580d1",
    "File": "src/commonTest/resources/nonreg/v3/WaitForFundingConfirmed_fe3c5978/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.7944684,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/WaitForFundingConfirmed_fe3c5978/data.json:generic-api-key:25",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 58,
    "EndLine": 58,
    "StartColumn": 29,
    "EndColumn": 102,
    "Match": "Key\": \"02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c\"",
    "Secret": "02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c",
    "File": "src/commonTest/resources/nonreg/v3/WaitForFundingConfirmed_fe3c5978/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.815461,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/WaitForFundingConfirmed_fe3c5978/data.json:generic-api-key:58",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 154,
    "EndLine": 154,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/ShuttingDown_ef41a1a5/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.7707121,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/ShuttingDown_ef41a1a5/data.json:generic-api-key:154",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 132,
    "EndLine": 132,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/WaitForFundingConfirmed_ff74dd33/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.7707121,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/WaitForFundingConfirmed_ff74dd33/data.json:generic-api-key:132",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 135,
    "EndLine": 135,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/WaitForFundingConfirmed_fe3c5978/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.7707121,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/WaitForFundingConfirmed_fe3c5978/data.json:generic-api-key:135",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 168,
    "EndLine": 168,
    "StartColumn": 53,
    "EndColumn": 134,
    "Match": "KeyScript\": \"0020cbfc18dfba5112418175b4d4683d6fb064a77a230e2190fd50344d3290d6d4b2\"",
    "Secret": "0020cbfc18dfba5112418175b4d4683d6fb064a77a230e2190fd50344d3290d6d4b2",
    "File": "src/commonTest/resources/nonreg/v3/ShuttingDown_ef41a1a5/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.838173,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/ShuttingDown_ef41a1a5/data.json:generic-api-key:168",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 186,
    "EndLine": 186,
    "StartColumn": 53,
    "EndColumn": 134,
    "Match": "KeyScript\": \"002039c8ad7d16980a6cea19c49477e44f93844c2fac06d07dcd2ad37e27d4f75071\"",
    "Secret": "002039c8ad7d16980a6cea19c49477e44f93844c2fac06d07dcd2ad37e27d4f75071",
    "File": "src/commonTest/resources/nonreg/v3/ShuttingDown_ef41a1a5/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.7608035,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/ShuttingDown_ef41a1a5/data.json:generic-api-key:186",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 23,
    "EndLine": 23,
    "StartColumn": 40,
    "EndColumn": 91,
    "Match": "Key\": \"001434947cfb2e8f6054ddf12daed4308cbe342580d1\"",
    "Secret": "001434947cfb2e8f6054ddf12daed4308cbe342580d1",
    "File": "src/commonTest/resources/nonreg/v3/WaitForFundingLocked_f3437082/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.7944684,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/WaitForFundingLocked_f3437082/data.json:generic-api-key:23",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 55,
    "EndLine": 55,
    "StartColumn": 29,
    "EndColumn": 102,
    "Match": "Key\": \"02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c\"",
    "Secret": "02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c",
    "File": "src/commonTest/resources/nonreg/v3/WaitForFundingLocked_f3437082/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.815461,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/WaitForFundingLocked_f3437082/data.json:generic-api-key:55",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 23,
    "EndLine": 23,
    "StartColumn": 40,
    "EndColumn": 91,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "src/commonTest/resources/nonreg/v3/WaitForRemotePublishFutureCommitment_d803549f/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.690942,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/WaitForRemotePublishFutureCommitment_d803549f/data.json:generic-api-key:23",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 55,
    "EndLine": 55,
    "StartColumn": 29,
    "EndColumn": 102,
    "Match": "Key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v3/WaitForRemotePublishFutureCommitment_d803549f/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.8222544,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/WaitForRemotePublishFutureCommitment_d803549f/data.json:generic-api-key:55",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 155,
    "EndLine": 155,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/WaitForRemotePublishFutureCommitment_d803549f/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.7707121,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/WaitForRemotePublishFutureCommitment_d803549f/data.json:generic-api-key:155",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 132,
    "EndLine": 132,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/WaitForFundingLocked_f3437082/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.7707121,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/WaitForFundingLocked_f3437082/data.json:generic-api-key:132",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 169,
    "EndLine": 169,
    "StartColumn": 53,
    "EndColumn": 134,
    "Match": "KeyScript\": \"0020ff5d65b735c30cb217486f62f8d590e56b6c27192299584ea87dbaa4452ef679\"",
    "Secret": "0020ff5d65b735c30cb217486f62f8d590e56b6c27192299584ea87dbaa4452ef679",
    "File": "src/commonTest/resources/nonreg/v3/WaitForRemotePublishFutureCommitment_d803549f/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.9070091,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/WaitForRemotePublishFutureCommitment_d803549f/data.json:generic-api-key:169",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 186,
    "EndLine": 186,
    "StartColumn": 53,
    "EndColumn": 134,
    "Match": "KeyScript\": \"00204ce05fb81556eaf7911d606ec23f598ea0e05d43e722c78010e1788774bc8fa6\"",
    "Secret": "00204ce05fb81556eaf7911d606ec23f598ea0e05d43e722c78010e1788774bc8fa6",
    "File": "src/commonTest/resources/nonreg/v3/WaitForRemotePublishFutureCommitment_d803549f/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.8532066,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/WaitForRemotePublishFutureCommitment_d803549f/data.json:generic-api-key:186",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 23,
    "EndLine": 23,
    "StartColumn": 40,
    "EndColumn": 91,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "src/commonTest/resources/nonreg/v3/WaitForRemotePublishFutureCommitment_ae47fde9/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.690942,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/WaitForRemotePublishFutureCommitment_ae47fde9/data.json:generic-api-key:23",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 55,
    "EndLine": 55,
    "StartColumn": 29,
    "EndColumn": 102,
    "Match": "Key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v3/WaitForRemotePublishFutureCommitment_ae47fde9/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.8222544,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/WaitForRemotePublishFutureCommitment_ae47fde9/data.json:generic-api-key:55",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 131,
    "EndLine": 131,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/WaitForRemotePublishFutureCommitment_ae47fde9/data.json",
    "SymlinkFile": "",
    "Commit": "3561648189f6498db1eaab6f2c9a187ab231a1ef",
    "Entropy": 3.7707121,
    "Author": "Bastien Teinturier",
    "Email": "31281497+t-bast@users.noreply.github.com",
    "Date": "2023-03-23T15:47:20Z",
    "Message": "Add multiple commitments to data model (#419)\n\nIn preparation for splices, we update the `Commitments` structure to\ncontain a list of active commitments. The dual funding RBF attempts are\nincluded in this active commitments list.",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "3561648189f6498db1eaab6f2c9a187ab231a1ef:src/commonTest/resources/nonreg/v3/WaitForRemotePublishFutureCommitment_ae47fde9/data.json:generic-api-key:131",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 23,
    "EndLine": 23,
    "StartColumn": 36,
    "EndColumn": 87,
    "Match": "Key\": \"001434947cfb2e8f6054ddf12daed4308cbe342580d1\"",
    "Secret": "001434947cfb2e8f6054ddf12daed4308cbe342580d1",
    "File": "src/commonTest/resources/nonreg/v2/Closing_0ed6ff68/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7944684,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Closing_0ed6ff68/data.json:generic-api-key:23",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 55,
    "EndLine": 55,
    "StartColumn": 25,
    "EndColumn": 98,
    "Match": "Key\": \"02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c\"",
    "Secret": "02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c",
    "File": "src/commonTest/resources/nonreg/v2/Closing_0ed6ff68/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.815461,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Closing_0ed6ff68/data.json:generic-api-key:55",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 102,
    "EndLine": 102,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/Closing_0ed6ff68/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Closing_0ed6ff68/data.json:generic-api-key:102",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 154,
    "EndLine": 154,
    "StartColumn": 25,
    "EndColumn": 106,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/Closing_0ed6ff68/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Closing_0ed6ff68/data.json:generic-api-key:154",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 171,
    "EndLine": 171,
    "StartColumn": 29,
    "EndColumn": 110,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/Closing_0ed6ff68/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Closing_0ed6ff68/data.json:generic-api-key:171",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 23,
    "EndLine": 23,
    "StartColumn": 36,
    "EndColumn": 87,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "src/commonTest/resources/nonreg/v2/Closing_0ba41d17/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.690942,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Closing_0ba41d17/data.json:generic-api-key:23",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 55,
    "EndLine": 55,
    "StartColumn": 25,
    "EndColumn": 98,
    "Match": "Key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v2/Closing_0ba41d17/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Closing_0ba41d17/data.json:generic-api-key:55",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 118,
    "EndLine": 118,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/Closing_0ba41d17/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Closing_0ba41d17/data.json:generic-api-key:118",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 132,
    "EndLine": 132,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"00202228f1681cff3adf3524363fad7481c1aad6aa344b223cd97b4fb09a667f5e2d\"",
    "Secret": "00202228f1681cff3adf3524363fad7481c1aad6aa344b223cd97b4fb09a667f5e2d",
    "File": "src/commonTest/resources/nonreg/v2/Closing_0ba41d17/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.842445,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Closing_0ba41d17/data.json:generic-api-key:132",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 149,
    "EndLine": 149,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"002028eee5bc7a7219b098d1f27ee035b83a6870d2be541097068097ab8a8d2568fb\"",
    "Secret": "002028eee5bc7a7219b098d1f27ee035b83a6870d2be541097068097ab8a8d2568fb",
    "File": "src/commonTest/resources/nonreg/v2/Closing_0ba41d17/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7810938,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Closing_0ba41d17/data.json:generic-api-key:149",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 228,
    "EndLine": 228,
    "StartColumn": 25,
    "EndColumn": 106,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/Closing_0ba41d17/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Closing_0ba41d17/data.json:generic-api-key:228",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 249,
    "EndLine": 249,
    "StartColumn": 29,
    "EndColumn": 110,
    "Match": "KeyScript\": \"00206aae960776a0a9f526c5ea6841c28baa16a7ae997313ed417dbc55e38fe1c173\"",
    "Secret": "00206aae960776a0a9f526c5ea6841c28baa16a7ae997313ed417dbc55e38fe1c173",
    "File": "src/commonTest/resources/nonreg/v2/Closing_0ba41d17/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8567593,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Closing_0ba41d17/data.json:generic-api-key:249",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 262,
    "EndLine": 262,
    "StartColumn": 33,
    "EndColumn": 114,
    "Match": "KeyScript\": \"00202228f1681cff3adf3524363fad7481c1aad6aa344b223cd97b4fb09a667f5e2d\"",
    "Secret": "00202228f1681cff3adf3524363fad7481c1aad6aa344b223cd97b4fb09a667f5e2d",
    "File": "src/commonTest/resources/nonreg/v2/Closing_0ba41d17/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.842445,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Closing_0ba41d17/data.json:generic-api-key:262",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 275,
    "EndLine": 275,
    "StartColumn": 33,
    "EndColumn": 114,
    "Match": "KeyScript\": \"002028eee5bc7a7219b098d1f27ee035b83a6870d2be541097068097ab8a8d2568fb\"",
    "Secret": "002028eee5bc7a7219b098d1f27ee035b83a6870d2be541097068097ab8a8d2568fb",
    "File": "src/commonTest/resources/nonreg/v2/Closing_0ba41d17/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7810938,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Closing_0ba41d17/data.json:generic-api-key:275",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 290,
    "EndLine": 290,
    "StartColumn": 33,
    "EndColumn": 114,
    "Match": "KeyScript\": \"00206aae960776a0a9f526c5ea6841c28baa16a7ae997313ed417dbc55e38fe1c173\"",
    "Secret": "00206aae960776a0a9f526c5ea6841c28baa16a7ae997313ed417dbc55e38fe1c173",
    "File": "src/commonTest/resources/nonreg/v2/Closing_0ba41d17/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8567593,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Closing_0ba41d17/data.json:generic-api-key:290",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 301,
    "EndLine": 301,
    "StartColumn": 33,
    "EndColumn": 114,
    "Match": "KeyScript\": \"00206aae960776a0a9f526c5ea6841c28baa16a7ae997313ed417dbc55e38fe1c173\"",
    "Secret": "00206aae960776a0a9f526c5ea6841c28baa16a7ae997313ed417dbc55e38fe1c173",
    "File": "src/commonTest/resources/nonreg/v2/Closing_0ba41d17/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8567593,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Closing_0ba41d17/data.json:generic-api-key:301",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 23,
    "EndLine": 23,
    "StartColumn": 36,
    "EndColumn": 87,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "src/commonTest/resources/nonreg/v2/Closing_0efffae3/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.690942,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Closing_0efffae3/data.json:generic-api-key:23",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 55,
    "EndLine": 55,
    "StartColumn": 25,
    "EndColumn": 98,
    "Match": "Key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v2/Closing_0efffae3/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Closing_0efffae3/data.json:generic-api-key:55",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 102,
    "EndLine": 102,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/Closing_0efffae3/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Closing_0efffae3/data.json:generic-api-key:102",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 154,
    "EndLine": 154,
    "StartColumn": 25,
    "EndColumn": 106,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/Closing_0efffae3/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Closing_0efffae3/data.json:generic-api-key:154",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 178,
    "EndLine": 178,
    "StartColumn": 33,
    "EndColumn": 114,
    "Match": "KeyScript\": \"0020958bb43c9e6d1b985001c2f4dc38d286d7fde78bb04d7ec5a5ae44d8ebf0c40c\"",
    "Secret": "0020958bb43c9e6d1b985001c2f4dc38d286d7fde78bb04d7ec5a5ae44d8ebf0c40c",
    "File": "src/commonTest/resources/nonreg/v2/Closing_0efffae3/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8474662,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Closing_0efffae3/data.json:generic-api-key:178",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 189,
    "EndLine": 189,
    "StartColumn": 33,
    "EndColumn": 114,
    "Match": "KeyScript\": \"0020adccc1c6aa5fb4707079451e579c5dc33c513f0d829346cefe8c80179e09d49c\"",
    "Secret": "0020adccc1c6aa5fb4707079451e579c5dc33c513f0d829346cefe8c80179e09d49c",
    "File": "src/commonTest/resources/nonreg/v2/Closing_0efffae3/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8196247,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Closing_0efffae3/data.json:generic-api-key:189",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 201,
    "EndLine": 201,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"0020290b762b514961d8ea3f1d0a0b7325c43daebfad3751211816c7bba59e607ccf\"",
    "Secret": "0020290b762b514961d8ea3f1d0a0b7325c43daebfad3751211816c7bba59e607ccf",
    "File": "src/commonTest/resources/nonreg/v2/Closing_0efffae3/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.9123697,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Closing_0efffae3/data.json:generic-api-key:201",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 212,
    "EndLine": 212,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00201923dc9176a660d04e0744c51f5e04a3168985962079a6c5d9e2e763170878e1\"",
    "Secret": "00201923dc9176a660d04e0744c51f5e04a3168985962079a6c5d9e2e763170878e1",
    "File": "src/commonTest/resources/nonreg/v2/Closing_0efffae3/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7736492,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Closing_0efffae3/data.json:generic-api-key:212",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 223,
    "EndLine": 223,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00202956089b232f4af35587c10894e6e4351d029b0534fb01f7c3c90285a35d45ff\"",
    "Secret": "00202956089b232f4af35587c10894e6e4351d029b0534fb01f7c3c90285a35d45ff",
    "File": "src/commonTest/resources/nonreg/v2/Closing_0efffae3/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8147776,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Closing_0efffae3/data.json:generic-api-key:223",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 234,
    "EndLine": 234,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"002058acb1c4d7241726756c7e09705500409991b84ce93c86d6ca4eceef76c98cd4\"",
    "Secret": "002058acb1c4d7241726756c7e09705500409991b84ce93c86d6ca4eceef76c98cd4",
    "File": "src/commonTest/resources/nonreg/v2/Closing_0efffae3/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7853658,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Closing_0efffae3/data.json:generic-api-key:234",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 247,
    "EndLine": 247,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"0020adccc1c6aa5fb4707079451e579c5dc33c513f0d829346cefe8c80179e09d49c\"",
    "Secret": "0020adccc1c6aa5fb4707079451e579c5dc33c513f0d829346cefe8c80179e09d49c",
    "File": "src/commonTest/resources/nonreg/v2/Closing_0efffae3/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8196247,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Closing_0efffae3/data.json:generic-api-key:247",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 258,
    "EndLine": 258,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"0020adccc1c6aa5fb4707079451e579c5dc33c513f0d829346cefe8c80179e09d49c\"",
    "Secret": "0020adccc1c6aa5fb4707079451e579c5dc33c513f0d829346cefe8c80179e09d49c",
    "File": "src/commonTest/resources/nonreg/v2/Closing_0efffae3/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8196247,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Closing_0efffae3/data.json:generic-api-key:258",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 23,
    "EndLine": 23,
    "StartColumn": 36,
    "EndColumn": 87,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "src/commonTest/resources/nonreg/v2/Closing_3bb07fb6/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.690942,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Closing_3bb07fb6/data.json:generic-api-key:23",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 55,
    "EndLine": 55,
    "StartColumn": 25,
    "EndColumn": 98,
    "Match": "Key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v2/Closing_3bb07fb6/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Closing_3bb07fb6/data.json:generic-api-key:55",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 118,
    "EndLine": 118,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/Closing_3bb07fb6/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Closing_3bb07fb6/data.json:generic-api-key:118",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 132,
    "EndLine": 132,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"0020b5abcef667acd30c1793157728653afaf2ea391a4039bc875b6ad468cc2f603a\"",
    "Secret": "0020b5abcef667acd30c1793157728653afaf2ea391a4039bc875b6ad468cc2f603a",
    "File": "src/commonTest/resources/nonreg/v2/Closing_3bb07fb6/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.882854,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Closing_3bb07fb6/data.json:generic-api-key:132",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 149,
    "EndLine": 149,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"0020c24f3aaf87264753868abbf958e8e7dc70352378c068e4c47b02fefc0acd114f\"",
    "Secret": "0020c24f3aaf87264753868abbf958e8e7dc70352378c068e4c47b02fefc0acd114f",
    "File": "src/commonTest/resources/nonreg/v2/Closing_3bb07fb6/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.862224,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Closing_3bb07fb6/data.json:generic-api-key:149",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 228,
    "EndLine": 228,
    "StartColumn": 25,
    "EndColumn": 106,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/Closing_3bb07fb6/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Closing_3bb07fb6/data.json:generic-api-key:228",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 250,
    "EndLine": 250,
    "StartColumn": 29,
    "EndColumn": 110,
    "Match": "KeyScript\": \"0020958bb43c9e6d1b985001c2f4dc38d286d7fde78bb04d7ec5a5ae44d8ebf0c40c\"",
    "Secret": "0020958bb43c9e6d1b985001c2f4dc38d286d7fde78bb04d7ec5a5ae44d8ebf0c40c",
    "File": "src/commonTest/resources/nonreg/v2/Closing_3bb07fb6/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8474662,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Closing_3bb07fb6/data.json:generic-api-key:250",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 263,
    "EndLine": 263,
    "StartColumn": 33,
    "EndColumn": 114,
    "Match": "KeyScript\": \"002018f3224d69b1cc3fbb44bd3779eb2bb07c6c5dc9722fd99f9cd6da90c4f9bd45\"",
    "Secret": "002018f3224d69b1cc3fbb44bd3779eb2bb07c6c5dc9722fd99f9cd6da90c4f9bd45",
    "File": "src/commonTest/resources/nonreg/v2/Closing_3bb07fb6/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7470005,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Closing_3bb07fb6/data.json:generic-api-key:263",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 276,
    "EndLine": 276,
    "StartColumn": 33,
    "EndColumn": 114,
    "Match": "KeyScript\": \"00206c71102142c7c3090165e8d136f64fda559496481a26e89019fc7964812fdfad\"",
    "Secret": "00206c71102142c7c3090165e8d136f64fda559496481a26e89019fc7964812fdfad",
    "File": "src/commonTest/resources/nonreg/v2/Closing_3bb07fb6/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7947228,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Closing_3bb07fb6/data.json:generic-api-key:276",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 23,
    "EndLine": 23,
    "StartColumn": 36,
    "EndColumn": 87,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "src/commonTest/resources/nonreg/v2/Closing_ef682e2e/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.690942,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Closing_ef682e2e/data.json:generic-api-key:23",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 55,
    "EndLine": 55,
    "StartColumn": 25,
    "EndColumn": 98,
    "Match": "Key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v2/Closing_ef682e2e/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Closing_ef682e2e/data.json:generic-api-key:55",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 125,
    "EndLine": 125,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/Closing_ef682e2e/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Closing_ef682e2e/data.json:generic-api-key:125",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 139,
    "EndLine": 139,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"0020ff5d65b735c30cb217486f62f8d590e56b6c27192299584ea87dbaa4452ef679\"",
    "Secret": "0020ff5d65b735c30cb217486f62f8d590e56b6c27192299584ea87dbaa4452ef679",
    "File": "src/commonTest/resources/nonreg/v2/Closing_ef682e2e/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.9070091,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Closing_ef682e2e/data.json:generic-api-key:139",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 156,
    "EndLine": 156,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"00204ce05fb81556eaf7911d606ec23f598ea0e05d43e722c78010e1788774bc8fa6\"",
    "Secret": "00204ce05fb81556eaf7911d606ec23f598ea0e05d43e722c78010e1788774bc8fa6",
    "File": "src/commonTest/resources/nonreg/v2/Closing_ef682e2e/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.

    "StartColumn": 20,
    "EndColumn": 71,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "src/commonTest/resources/nonreg/v2/Negotiating_ee10091c/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.690942,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Negotiating_ee10091c/data.json:generic-api-key:167",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 177,
    "EndLine": 177,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/Negotiating_ee10091c/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Negotiating_ee10091c/data.json:generic-api-key:177",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 206,
    "EndLine": 206,
    "StartColumn": 25,
    "EndColumn": 106,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/Negotiating_ee10091c/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Negotiating_ee10091c/data.json:generic-api-key:206",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 23,
    "EndLine": 23,
    "StartColumn": 36,
    "EndColumn": 87,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "src/commonTest/resources/nonreg/v2/Normal_748a735b/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.690942,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Normal_748a735b/data.json:generic-api-key:23",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 55,
    "EndLine": 55,
    "StartColumn": 25,
    "EndColumn": 98,
    "Match": "Key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v2/Normal_748a735b/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Normal_748a735b/data.json:generic-api-key:55",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 101,
    "EndLine": 101,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/Normal_748a735b/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Normal_748a735b/data.json:generic-api-key:101",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 153,
    "EndLine": 153,
    "StartColumn": 25,
    "EndColumn": 106,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/Normal_748a735b/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Normal_748a735b/data.json:generic-api-key:153",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 23,
    "EndLine": 23,
    "StartColumn": 36,
    "EndColumn": 87,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "src/commonTest/resources/nonreg/v2/Negotiating_c8d15808/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.690942,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Negotiating_c8d15808/data.json:generic-api-key:23",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 55,
    "EndLine": 55,
    "StartColumn": 25,
    "EndColumn": 98,
    "Match": "Key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v2/Negotiating_c8d15808/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Negotiating_c8d15808/data.json:generic-api-key:55",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 102,
    "EndLine": 102,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/Negotiating_c8d15808/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Negotiating_c8d15808/data.json:generic-api-key:102",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 154,
    "EndLine": 154,
    "StartColumn": 25,
    "EndColumn": 106,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/Negotiating_c8d15808/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Negotiating_c8d15808/data.json:generic-api-key:154",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 163,
    "EndLine": 163,
    "StartColumn": 20,
    "EndColumn": 71,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "src/commonTest/resources/nonreg/v2/Negotiating_c8d15808/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.690942,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Negotiating_c8d15808/data.json:generic-api-key:163",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 167,
    "EndLine": 167,
    "StartColumn": 20,
    "EndColumn": 71,
    "Match": "Key\": \"001434947cfb2e8f6054ddf12daed4308cbe342580d1\"",
    "Secret": "001434947cfb2e8f6054ddf12daed4308cbe342580d1",
    "File": "src/commonTest/resources/nonreg/v2/Negotiating_c8d15808/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7944684,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Negotiating_c8d15808/data.json:generic-api-key:167",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 187,
    "EndLine": 187,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/Negotiating_c8d15808/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Negotiating_c8d15808/data.json:generic-api-key:187",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 215,
    "EndLine": 215,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/Negotiating_c8d15808/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Negotiating_c8d15808/data.json:generic-api-key:215",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 243,
    "EndLine": 243,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/Negotiating_c8d15808/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Negotiating_c8d15808/data.json:generic-api-key:243",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 271,
    "EndLine": 271,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/Negotiating_c8d15808/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Negotiating_c8d15808/data.json:generic-api-key:271",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 299,
    "EndLine": 299,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/Negotiating_c8d15808/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Negotiating_c8d15808/data.json:generic-api-key:299",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 327,
    "EndLine": 327,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/Negotiating_c8d15808/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Negotiating_c8d15808/data.json:generic-api-key:327",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 355,
    "EndLine": 355,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/Negotiating_c8d15808/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Negotiating_c8d15808/data.json:generic-api-key:355",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 383,
    "EndLine": 383,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/Negotiating_c8d15808/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Negotiating_c8d15808/data.json:generic-api-key:383",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 411,
    "EndLine": 411,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/Negotiating_c8d15808/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Negotiating_c8d15808/data.json:generic-api-key:411",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 439,
    "EndLine": 439,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/Negotiating_c8d15808/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Negotiating_c8d15808/data.json:generic-api-key:439",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 467,
    "EndLine": 467,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/Negotiating_c8d15808/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Negotiating_c8d15808/data.json:generic-api-key:467",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 495,
    "EndLine": 495,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/Negotiating_c8d15808/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Negotiating_c8d15808/data.json:generic-api-key:495",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 523,
    "EndLine": 523,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/Negotiating_c8d15808/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Negotiating_c8d15808/data.json:generic-api-key:523",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 551,
    "EndLine": 551,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/Negotiating_c8d15808/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Negotiating_c8d15808/data.json:generic-api-key:551",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 579,
    "EndLine": 579,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/Negotiating_c8d15808/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Negotiating_c8d15808/data.json:generic-api-key:579",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 607,
    "EndLine": 607,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/Negotiating_c8d15808/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Negotiating_c8d15808/data.json:generic-api-key:607",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 636,
    "EndLine": 636,
    "StartColumn": 25,
    "EndColumn": 106,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/Negotiating_c8d15808/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Negotiating_c8d15808/data.json:generic-api-key:636",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 23,
    "EndLine": 23,
    "StartColumn": 36,
    "EndColumn": 87,
    "Match": "Key\": \"001434947cfb2e8f6054ddf12daed4308cbe342580d1\"",
    "Secret": "001434947cfb2e8f6054ddf12daed4308cbe342580d1",
    "File": "src/commonTest/resources/nonreg/v2/Normal_ff248f8d/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7944684,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Normal_ff248f8d/data.json:generic-api-key:23",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 55,
    "EndLine": 55,
    "StartColumn": 25,
    "EndColumn": 98,
    "Match": "Key\": \"02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c\"",
    "Secret": "02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c",
    "File": "src/commonTest/resources/nonreg/v2/Normal_ff248f8d/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.815461,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Normal_ff248f8d/data.json:generic-api-key:55",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 102,
    "EndLine": 102,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/Normal_ff248f8d/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Normal_ff248f8d/data.json:generic-api-key:102",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 154,
    "EndLine": 154,
    "StartColumn": 25,
    "EndColumn": 106,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/Normal_ff248f8d/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Normal_ff248f8d/data.json:generic-api-key:154",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 23,
    "EndLine": 23,
    "StartColumn": 36,
    "EndColumn": 87,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "src/commonTest/resources/nonreg/v2/Negotiating_f52b19b8/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.690942,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Negotiating_f52b19b8/data.json:generic-api-key:23",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 55,
    "EndLine": 55,
    "StartColumn": 25,
    "EndColumn": 98,
    "Match": "Key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v2/Negotiating_f52b19b8/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Negotiating_f52b19b8/data.json:generic-api-key:55",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 102,
    "EndLine": 102,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/Negotiating_f52b19b8/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Negotiating_f52b19b8/data.json:generic-api-key:102",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 154,
    "EndLine": 154,
    "StartColumn": 25,
    "EndColumn": 106,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/Negotiating_f52b19b8/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Negotiating_f52b19b8/data.json:generic-api-key:154",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 166,
    "EndLine": 166,
    "StartColumn": 20,
    "EndColumn": 71,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "src/commonTest/resources/nonreg/v2/Negotiating_f52b19b8/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.690942,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Negotiating_f52b19b8/data.json:generic-api-key:166",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 170,
    "EndLine": 170,
    "StartColumn": 20,
    "EndColumn": 77,
    "Match": "Key\": \"76a9143a52c0fc59997d1e53009d607e3d93ca56b1014c88ac\"",
    "Secret": "76a9143a52c0fc59997d1e53009d607e3d93ca56b1014c88ac",
    "File": "src/commonTest/resources/nonreg/v2/Negotiating_f52b19b8/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8239818,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Negotiating_f52b19b8/data.json:generic-api-key:170",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 190,
    "EndLine": 190,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/Negotiating_f52b19b8/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Negotiating_f52b19b8/data.json:generic-api-key:190",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 23,
    "EndLine": 23,
    "StartColumn": 36,
    "EndColumn": 87,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "src/commonTest/resources/nonreg/v2/Normal_e2253ddd/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.690942,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Normal_e2253ddd/data.json:generic-api-key:23",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 55,
    "EndLine": 55,
    "StartColumn": 25,
    "EndColumn": 98,
    "Match": "Key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v2/Normal_e2253ddd/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Normal_e2253ddd/data.json:generic-api-key:55",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 158,
    "EndLine": 158,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/Normal_e2253ddd/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Normal_e2253ddd/data.json:generic-api-key:158",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 172,
    "EndLine": 172,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"00206520f0c1ce76af1efb00c648cbb90fd645c162b0f5e4b2b750967644352f71dd\"",
    "Secret": "00206520f0c1ce76af1efb00c648cbb90fd645c162b0f5e4b2b750967644352f71dd",
    "File": "src/commonTest/resources/nonreg/v2/Normal_e2253ddd/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7663062,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Normal_e2253ddd/data.json:generic-api-key:172",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 189,
    "EndLine": 189,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"00202a9429e3863a2ef776e475172706053ac34fd5e459a9a2a108797a49614f0ace\"",
    "Secret": "00202a9429e3863a2ef776e475172706053ac34fd5e459a9a2a108797a49614f0ace",
    "File": "src/commonTest/resources/nonreg/v2/Normal_e2253ddd/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7499378,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Normal_e2253ddd/data.json:generic-api-key:189",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 207,
    "EndLine": 207,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"00206f9fef6a1c60ac312c4889117dccbb62809ca95476afcf1faa89071109ff4c66\"",
    "Secret": "00206f9fef6a1c60ac312c4889117dccbb62809ca95476afcf1faa89071109ff4c66",
    "File": "src/commonTest/resources/nonreg/v2/Normal_e2253ddd/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7016,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Normal_e2253ddd/data.json:generic-api-key:207",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 325,
    "EndLine": 325,
    "StartColumn": 25,
    "EndColumn": 106,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/Normal_e2253ddd/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Normal_e2253ddd/data.json:generic-api-key:325",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 23,
    "EndLine": 23,
    "StartColumn": 36,
    "EndColumn": 87,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "src/commonTest/resources/nonreg/v2/Normal_ffd9f5db/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.690942,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Normal_ffd9f5db/data.json:generic-api-key:23",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 55,
    "EndLine": 55,
    "StartColumn": 25,
    "EndColumn": 98,
    "Match": "Key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v2/Normal_ffd9f5db/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Normal_ffd9f5db/data.json:generic-api-key:55",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 102,
    "EndLine": 102,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/Normal_ffd9f5db/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Normal_ffd9f5db/data.json:generic-api-key:102",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 181,
    "EndLine": 181,
    "StartColumn": 25,
    "EndColumn": 106,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/Normal_ffd9f5db/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Normal_ffd9f5db/data.json:generic-api-key:181",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 23,
    "EndLine": 23,
    "StartColumn": 36,
    "EndColumn": 87,
    "Match": "Key\": \"001434947cfb2e8f6054ddf12daed4308cbe342580d1\"",
    "Secret": "001434947cfb2e8f6054ddf12daed4308cbe342580d1",
    "File": "src/commonTest/resources/nonreg/v2/WaitForFundingConfirmed_f7421b49/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7944684,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/WaitForFundingConfirmed_f7421b49/data.json:generic-api-key:23",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 55,
    "EndLine": 55,
    "StartColumn": 25,
    "EndColumn": 98,
    "Match": "Key\": \"02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c\"",
    "Secret": "02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c",
    "File": "src/commonTest/resources/nonreg/v2/WaitForFundingConfirmed_f7421b49/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.815461,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/WaitForFundingConfirmed_f7421b49/data.json:generic-api-key:55",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 102,
    "EndLine": 102,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/WaitForFundingConfirmed_f7421b49/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/WaitForFundingConfirmed_f7421b49/data.json:generic-api-key:102",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 154,
    "EndLine": 154,
    "StartColumn": 25,
    "EndColumn": 106,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/WaitForFundingConfirmed_f7421b49/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/WaitForFundingConfirmed_f7421b49/data.json:generic-api-key:154",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 23,
    "EndLine": 23,
    "StartColumn": 36,
    "EndColumn": 87,
    "Match": "Key\": \"001434947cfb2e8f6054ddf12daed4308cbe342580d1\"",
    "Secret": "001434947cfb2e8f6054ddf12daed4308cbe342580d1",
    "File": "src/commonTest/resources/nonreg/v2/WaitForFundingConfirmed_fe3c5978/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7944684,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/WaitForFundingConfirmed_fe3c5978/data.json:generic-api-key:23",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 56,
    "EndLine": 56,
    "StartColumn": 25,
    "EndColumn": 98,
    "Match": "Key\": \"02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c\"",
    "Secret": "02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c",
    "File": "src/commonTest/resources/nonreg/v2/WaitForFundingConfirmed_fe3c5978/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.815461,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/WaitForFundingConfirmed_fe3c5978/data.json:generic-api-key:56",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 103,
    "EndLine": 103,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/WaitForFundingConfirmed_fe3c5978/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/WaitForFundingConfirmed_fe3c5978/data.json:generic-api-key:103",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 155,
    "EndLine": 155,
    "StartColumn": 25,
    "EndColumn": 106,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/WaitForFundingConfirmed_fe3c5978/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/WaitForFundingConfirmed_fe3c5978/data.json:generic-api-key:155",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 23,
    "EndLine": 23,
    "StartColumn": 36,
    "EndColumn": 87,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "src/commonTest/resources/nonreg/v2/Normal_ff4a71b6/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.690942,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Normal_ff4a71b6/data.json:generic-api-key:23",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 55,
    "EndLine": 55,
    "StartColumn": 25,
    "EndColumn": 98,
    "Match": "Key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v2/Normal_ff4a71b6/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Normal_ff4a71b6/data.json:generic-api-key:55",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 134,
    "EndLine": 134,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/Normal_ff4a71b6/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Normal_ff4a71b6/data.json:generic-api-key:134",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 148,
    "EndLine": 148,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"00208bf1a46fa0bd7ca44eda0c5df968544c280859a29a527641afac57c27bf7003f\"",
    "Secret": "00208bf1a46fa0bd7ca44eda0c5df968544c280859a29a527641afac57c27bf7003f",
    "File": "src/commonTest/resources/nonreg/v2/Normal_ff4a71b6/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8157325,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Normal_ff4a71b6/data.json:generic-api-key:148",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 165,
    "EndLine": 165,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"0020de61d4f9864b11e111190b9d11f62ebf7476d517a7bba5b0a5b0f80f4e54eb1f\"",
    "Secret": "0020de61d4f9864b11e111190b9d11f62ebf7476d517a7bba5b0a5b0f80f4e54eb1f",
    "File": "src/commonTest/resources/nonreg/v2/Normal_ff4a71b6/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.647109,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Normal_ff4a71b6/data.json:generic-api-key:165",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 182,
    "EndLine": 182,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"00207805c9f22468069984db4db780b44c97593e4fee2c8ee32114f1c9b4a6d70fe4\"",
    "Secret": "00207805c9f22468069984db4db780b44c97593e4fee2c8ee32114f1c9b4a6d70fe4",
    "File": "src/commonTest/resources/nonreg/v2/Normal_ff4a71b6/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8421054,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Normal_ff4a71b6/data.json:generic-api-key:182",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 332,
    "EndLine": 332,
    "StartColumn": 25,
    "EndColumn": 106,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/Normal_ff4a71b6/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/Normal_ff4a71b6/data.json:generic-api-key:332",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 23,
    "EndLine": 23,
    "StartColumn": 36,
    "EndColumn": 87,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "src/commonTest/resources/nonreg/v2/WaitForFundingConfirmed_ff74dd33/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.690942,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/WaitForFundingConfirmed_ff74dd33/data.json:generic-api-key:23",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 55,
    "EndLine": 55,
    "StartColumn": 25,
    "EndColumn": 98,
    "Match": "Key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v2/WaitForFundingConfirmed_ff74dd33/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/WaitForFundingConfirmed_ff74dd33/data.json:generic-api-key:55",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 102,
    "EndLine": 102,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/WaitForFundingConfirmed_ff74dd33/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/WaitForFundingConfirmed_ff74dd33/data.json:generic-api-key:102",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 154,
    "EndLine": 154,
    "StartColumn": 25,
    "EndColumn": 106,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/WaitForFundingConfirmed_ff74dd33/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/WaitForFundingConfirmed_ff74dd33/data.json:generic-api-key:154",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 23,
    "EndLine": 23,
    "StartColumn": 36,
    "EndColumn": 87,
    "Match": "Key\": \"001434947cfb2e8f6054ddf12daed4308cbe342580d1\"",
    "Secret": "001434947cfb2e8f6054ddf12daed4308cbe342580d1",
    "File": "src/commonTest/resources/nonreg/v2/WaitForFundingLocked_f3437082/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7944684,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/WaitForFundingLocked_f3437082/data.json:generic-api-key:23",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 55,
    "EndLine": 55,
    "StartColumn": 25,
    "EndColumn": 98,
    "Match": "Key\": \"02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c\"",
    "Secret": "02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c",
    "File": "src/commonTest/resources/nonreg/v2/WaitForFundingLocked_f3437082/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.815461,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/WaitForFundingLocked_f3437082/data.json:generic-api-key:55",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 102,
    "EndLine": 102,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/WaitForFundingLocked_f3437082/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/WaitForFundingLocked_f3437082/data.json:generic-api-key:102",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 154,
    "EndLine": 154,
    "StartColumn": 25,
    "EndColumn": 106,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/WaitForFundingLocked_f3437082/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/WaitForFundingLocked_f3437082/data.json:generic-api-key:154",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 23,
    "EndLine": 23,
    "StartColumn": 36,
    "EndColumn": 87,
    "Match": "Key\": \"001434947cfb2e8f6054ddf12daed4308cbe342580d1\"",
    "Secret": "001434947cfb2e8f6054ddf12daed4308cbe342580d1",
    "File": "src/commonTest/resources/nonreg/v2/ShuttingDown_f89ecd50/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7944684,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/ShuttingDown_f89ecd50/data.json:generic-api-key:23",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 55,
    "EndLine": 55,
    "StartColumn": 25,
    "EndColumn": 98,
    "Match": "Key\": \"02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c\"",
    "Secret": "02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c",
    "File": "src/commonTest/resources/nonreg/v2/ShuttingDown_f89ecd50/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.815461,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/ShuttingDown_f89ecd50/data.json:generic-api-key:55",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 118,
    "EndLine": 118,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/ShuttingDown_f89ecd50/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/ShuttingDown_f89ecd50/data.json:generic-api-key:118",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 132,
    "EndLine": 132,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"0020cbfc18dfba5112418175b4d4683d6fb064a77a230e2190fd50344d3290d6d4b2\"",
    "Secret": "0020cbfc18dfba5112418175b4d4683d6fb064a77a230e2190fd50344d3290d6d4b2",
    "File": "src/commonTest/resources/nonreg/v2/ShuttingDown_f89ecd50/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.838173,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/ShuttingDown_f89ecd50/data.json:generic-api-key:132",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 150,
    "EndLine": 150,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"002039c8ad7d16980a6cea19c49477e44f93844c2fac06d07dcd2ad37e27d4f75071\"",
    "Secret": "002039c8ad7d16980a6cea19c49477e44f93844c2fac06d07dcd2ad37e27d4f75071",
    "File": "src/commonTest/resources/nonreg/v2/ShuttingDown_f89ecd50/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7608035,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/ShuttingDown_f89ecd50/data.json:generic-api-key:150",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 259,
    "EndLine": 259,
    "StartColumn": 25,
    "EndColumn": 106,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/ShuttingDown_f89ecd50/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/ShuttingDown_f89ecd50/data.json:generic-api-key:259",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 268,
    "EndLine": 268,
    "StartColumn": 20,
    "EndColumn": 71,
    "Match": "Key\": \"001434947cfb2e8f6054ddf12daed4308cbe342580d1\"",
    "Secret": "001434947cfb2e8f6054ddf12daed4308cbe342580d1",
    "File": "src/commonTest/resources/nonreg/v2/ShuttingDown_f89ecd50/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7944684,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/ShuttingDown_f89ecd50/data.json:generic-api-key:268",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 272,
    "EndLine": 272,
    "StartColumn": 20,
    "EndColumn": 71,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "src/commonTest/resources/nonreg/v2/ShuttingDown_f89ecd50/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.690942,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/ShuttingDown_f89ecd50/data.json:generic-api-key:272",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 23,
    "EndLine": 23,
    "StartColumn": 36,
    "EndColumn": 87,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "src/commonTest/resources/nonreg/v2/WaitForRemotePublishFutureCommitment_ae47fde9/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.690942,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/WaitForRemotePublishFutureCommitment_ae47fde9/data.json:generic-api-key:23",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 55,
    "EndLine": 55,
    "StartColumn": 25,
    "EndColumn": 98,
    "Match": "Key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v2/WaitForRemotePublishFutureCommitment_ae47fde9/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/WaitForRemotePublishFutureCommitment_ae47fde9/data.json:generic-api-key:55",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 101,
    "EndLine": 101,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/WaitForRemotePublishFutureCommitment_ae47fde9/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/WaitForRemotePublishFutureCommitment_ae47fde9/data.json:generic-api-key:101",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 153,
    "EndLine": 153,
    "StartColumn": 25,
    "EndColumn": 106,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/WaitForRemotePublishFutureCommitment_ae47fde9/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/WaitForRemotePublishFutureCommitment_ae47fde9/data.json:generic-api-key:153",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 23,
    "EndLine": 23,
    "StartColumn": 36,
    "EndColumn": 87,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "src/commonTest/resources/nonreg/v2/WaitForRemotePublishFutureCommitment_d803549f/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.690942,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/WaitForRemotePublishFutureCommitment_d803549f/data.json:generic-api-key:23",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 55,
    "EndLine": 55,
    "StartColumn": 25,
    "EndColumn": 98,
    "Match": "Key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v2/WaitForRemotePublishFutureCommitment_d803549f/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/WaitForRemotePublishFutureCommitment_d803549f/data.json:generic-api-key:55",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 125,
    "EndLine": 125,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/WaitForRemotePublishFutureCommitment_d803549f/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/WaitForRemotePublishFutureCommitment_d803549f/data.json:generic-api-key:125",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 139,
    "EndLine": 139,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"0020ff5d65b735c30cb217486f62f8d590e56b6c27192299584ea87dbaa4452ef679\"",
    "Secret": "0020ff5d65b735c30cb217486f62f8d590e56b6c27192299584ea87dbaa4452ef679",
    "File": "src/commonTest/resources/nonreg/v2/WaitForRemotePublishFutureCommitment_d803549f/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.9070091,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/WaitForRemotePublishFutureCommitment_d803549f/data.json:generic-api-key:139",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 156,
    "EndLine": 156,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"00204ce05fb81556eaf7911d606ec23f598ea0e05d43e722c78010e1788774bc8fa6\"",
    "Secret": "00204ce05fb81556eaf7911d606ec23f598ea0e05d43e722c78010e1788774bc8fa6",
    "File": "src/commonTest/resources/nonreg/v2/WaitForRemotePublishFutureCommitment_d803549f/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8532066,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/WaitForRemotePublishFutureCommitment_d803549f/data.json:generic-api-key:156",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 238,
    "EndLine": 238,
    "StartColumn": 25,
    "EndColumn": 106,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/WaitForRemotePublishFutureCommitment_d803549f/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/WaitForRemotePublishFutureCommitment_d803549f/data.json:generic-api-key:238",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 21,
    "EndLine": 21,
    "StartColumn": 36,
    "EndColumn": 87,
    "Match": "Key\": \"001434947cfb2e8f6054ddf12daed4308cbe342580d1\"",
    "Secret": "001434947cfb2e8f6054ddf12daed4308cbe342580d1",
    "File": "src/commonTest/resources/nonreg/v3/Closing_0ed6ff68/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7944684,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Closing_0ed6ff68/data.json:generic-api-key:21",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 53,
    "EndLine": 53,
    "StartColumn": 25,
    "EndColumn": 98,
    "Match": "Key\": \"02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c\"",
    "Secret": "02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c",
    "File": "src/commonTest/resources/nonreg/v3/Closing_0ed6ff68/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.815461,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Closing_0ed6ff68/data.json:generic-api-key:53",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 100,
    "EndLine": 100,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/Closing_0ed6ff68/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Closing_0ed6ff68/data.json:generic-api-key:100",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 152,
    "EndLine": 152,
    "StartColumn": 25,
    "EndColumn": 106,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/Closing_0ed6ff68/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Closing_0ed6ff68/data.json:generic-api-key:152",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 169,
    "EndLine": 169,
    "StartColumn": 29,
    "EndColumn": 110,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/Closing_0ed6ff68/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Closing_0ed6ff68/data.json:generic-api-key:169",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 21,
    "EndLine": 21,
    "StartColumn": 36,
    "EndColumn": 87,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "src/commonTest/resources/nonreg/v3/Closing_0ba41d17/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.690942,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Closing_0ba41d17/data.json:generic-api-key:21",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 53,
    "EndLine": 53,
    "StartColumn": 25,
    "EndColumn": 98,
    "Match": "Key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v3/Closing_0ba41d17/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Closing_0ba41d17/data.json:generic-api-key:53",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 116,
    "EndLine": 116,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/Closing_0ba41d17/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Closing_0ba41d17/data.json:generic-api-key:116",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 130,
    "EndLine": 130,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"00202228f1681cff3adf3524363fad7481c1aad6aa344b223cd97b4fb09a667f5e2d\"",
    "Secret": "00202228f1681cff3adf3524363fad7481c1aad6aa344b223cd97b4fb09a667f5e2d",
    "File": "src/commonTest/resources/nonreg/v3/Closing_0ba41d17/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.842445,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Closing_0ba41d17/data.json:generic-api-key:130",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 147,
    "EndLine": 147,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"002028eee5bc7a7219b098d1f27ee035b83a6870d2be541097068097ab8a8d2568fb\"",
    "Secret": "002028eee5bc7a7219b098d1f27ee035b83a6870d2be541097068097ab8a8d2568fb",
    "File": "src/commonTest/resources/nonreg/v3/Closing_0ba41d17/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7810938,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Closing_0ba41d17/data.json:generic-api-key:147",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 226,
    "EndLine": 226,
    "StartColumn": 25,
    "EndColumn": 106,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/Closing_0ba41d17/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Closing_0ba41d17/data.json:generic-api-key:226",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 247,
    "EndLine": 247,
    "StartColumn": 29,
    "EndColumn": 110,
    "Match": "KeyScript\": \"00206aae960776a0a9f526c5ea6841c28baa16a7ae997313ed417dbc55e38fe1c173\"",
    "Secret": "00206aae960776a0a9f526c5ea6841c28baa16a7ae997313ed417dbc55e38fe1c173",
    "File": "src/commonTest/resources/nonreg/v3/Closing_0ba41d17/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8567593,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Closing_0ba41d17/data.json:generic-api-key:247",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 260,
    "EndLine": 260,
    "StartColumn": 33,
    "EndColumn": 114,
    "Match": "KeyScript\": \"00202228f1681cff3adf3524363fad7481c1aad6aa344b223cd97b4fb09a667f5e2d\"",
    "Secret": "00202228f1681cff3adf3524363fad7481c1aad6aa344b223cd97b4fb09a667f5e2d",
    "File": "src/commonTest/resources/nonreg/v3/Closing_0ba41d17/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.842445,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Closing_0ba41d17/data.json:generic-api-key:260",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 273,
    "EndLine": 273,
    "StartColumn": 33,
    "EndColumn": 114,
    "Match": "KeyScript\": \"002028eee5bc7a7219b098d1f27ee035b83a6870d2be541097068097ab8a8d2568fb\"",
    "Secret": "002028eee5bc7a7219b098d1f27ee035b83a6870d2be541097068097ab8a8d2568fb",
    "File": "src/commonTest/resources/nonreg/v3/Closing_0ba41d17/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7810938,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Closing_0ba41d17/data.json:generic-api-key:273",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 288,
    "EndLine": 288,
    "StartColumn": 33,
    "EndColumn": 114,
    "Match": "KeyScript\": \"00206aae960776a0a9f526c5ea6841c28baa16a7ae997313ed417dbc55e38fe1c173\"",
    "Secret": "00206aae960776a0a9f526c5ea6841c28baa16a7ae997313ed417dbc55e38fe1c173",
    "File": "src/commonTest/resources/nonreg/v3/Closing_0ba41d17/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8567593,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Closing_0ba41d17/data.json:generic-api-key:288",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 299,
    "EndLine": 299,
    "StartColumn": 33,
    "EndColumn": 114,
    "Match": "KeyScript\": \"00206aae960776a0a9f526c5ea6841c28baa16a7ae997313ed417dbc55e38fe1c173\"",
    "Secret": "00206aae960776a0a9f526c5ea6841c28baa16a7ae997313ed417dbc55e38fe1c173",
    "File": "src/commonTest/resources/nonreg/v3/Closing_0ba41d17/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8567593,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Closing_0ba41d17/data.json:generic-api-key:299",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 23,
    "EndLine": 23,
    "StartColumn": 36,
    "EndColumn": 87,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "src/commonTest/resources/nonreg/v2/ShuttingDown_c321b947/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.690942,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/ShuttingDown_c321b947/data.json:generic-api-key:23",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 55,
    "EndLine": 55,
    "StartColumn": 25,
    "EndColumn": 98,
    "Match": "Key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v2/ShuttingDown_c321b947/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/ShuttingDown_c321b947/data.json:generic-api-key:55",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 118,
    "EndLine": 118,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/ShuttingDown_c321b947/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/ShuttingDown_c321b947/data.json:generic-api-key:118",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 132,
    "EndLine": 132,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"00200d94818c9b1a70b689daa5af63b6c6737ea40a9de55839819bf0421217d68d80\"",
    "Secret": "00200d94818c9b1a70b689daa5af63b6c6737ea40a9de55839819bf0421217d68d80",
    "File": "src/commonTest/resources/nonreg/v2/ShuttingDown_c321b947/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8739407,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/ShuttingDown_c321b947/data.json:generic-api-key:132",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 149,
    "EndLine": 149,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"0020c9b6634f8ea3a3789e96570513e9bc701395ea27477c07e80931a2ab9bbb549c\"",
    "Secret": "0020c9b6634f8ea3a3789e96570513e9bc701395ea27477c07e80931a2ab9bbb549c",
    "File": "src/commonTest/resources/nonreg/v2/ShuttingDown_c321b947/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7739887,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/ShuttingDown_c321b947/data.json:generic-api-key:149",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 222,
    "EndLine": 222,
    "StartColumn": 25,
    "EndColumn": 106,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v2/ShuttingDown_c321b947/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/ShuttingDown_c321b947/data.json:generic-api-key:222",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 234,
    "EndLine": 234,
    "StartColumn": 20,
    "EndColumn": 71,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "src/commonTest/resources/nonreg/v2/ShuttingDown_c321b947/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.690942,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/ShuttingDown_c321b947/data.json:generic-api-key:234",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 238,
    "EndLine": 238,
    "StartColumn": 20,
    "EndColumn": 71,
    "Match": "Key\": \"001434947cfb2e8f6054ddf12daed4308cbe342580d1\"",
    "Secret": "001434947cfb2e8f6054ddf12daed4308cbe342580d1",
    "File": "src/commonTest/resources/nonreg/v2/ShuttingDown_c321b947/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7944684,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v2/ShuttingDown_c321b947/data.json:generic-api-key:238",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 21,
    "EndLine": 21,
    "StartColumn": 36,
    "EndColumn": 87,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "src/commonTest/resources/nonreg/v3/Closing_029bf8f3/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.690942,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Closing_029bf8f3/data.json:generic-api-key:21",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 53,
    "EndLine": 53,
    "StartColumn": 25,
    "EndColumn": 98,
    "Match": "Key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v3/Closing_029bf8f3/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Closing_029bf8f3/data.json:generic-api-key:53",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 140,
    "EndLine": 140,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/Closing_029bf8f3/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Closing_029bf8f3/data.json:generic-api-key:140",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 154,
    "EndLine": 154,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"00200a56c86dc2e94046c61370f8e91b9cf2b8ceeb1e5597b8b3ea2191ac32076835\"",
    "Secret": "00200a56c86dc2e94046c61370f8e91b9cf2b8ceeb1e5597b8b3ea2191ac32076835",
    "File": "src/commonTest/resources/nonreg/v3/Closing_029bf8f3/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8839533,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Closing_029bf8f3/data.json:generic-api-key:154",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 172,
    "EndLine": 172,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"002074e2cfb625e3787193fbb4f26dbd7739c5484c4455fc53c71bb2bce84b476ff7\"",
    "Secret": "002074e2cfb625e3787193fbb4f26dbd7739c5484c4455fc53c71bb2bce84b476ff7",
    "File": "src/commonTest/resources/nonreg/v3/Closing_029bf8f3/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7454278,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Closing_029bf8f3/data.json:generic-api-key:172",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 190,
    "EndLine": 190,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"00205d9ebea209786542771331647e3bfbe27d502165721f79d5fa7c650fc0f4f821\"",
    "Secret": "00205d9ebea209786542771331647e3bfbe27d502165721f79d5fa7c650fc0f4f821",
    "File": "src/commonTest/resources/nonreg/v3/Closing_029bf8f3/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8564835,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Closing_029bf8f3/data.json:generic-api-key:190",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 207,
    "EndLine": 207,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"00204d3d669bcc3a84c7fddb2b53587dc6e478a73ddc56fc6d741b8d8101275738ec\"",
    "Secret": "00204d3d669bcc3a84c7fddb2b53587dc6e478a73ddc56fc6d741b8d8101275738ec",
    "File": "src/commonTest/resources/nonreg/v3/Closing_029bf8f3/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.821711,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Closing_029bf8f3/data.json:generic-api-key:207",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 374,
    "EndLine": 374,
    "StartColumn": 25,
    "EndColumn": 106,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/Closing_029bf8f3/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Closing_029bf8f3/data.json:generic-api-key:374",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 396,
    "EndLine": 396,
    "StartColumn": 29,
    "EndColumn": 110,
    "Match": "KeyScript\": \"0020958bb43c9e6d1b985001c2f4dc38d286d7fde78bb04d7ec5a5ae44d8ebf0c40c\"",
    "Secret": "0020958bb43c9e6d1b985001c2f4dc38d286d7fde78bb04d7ec5a5ae44d8ebf0c40c",
    "File": "src/commonTest/resources/nonreg/v3/Closing_029bf8f3/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8474662,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Closing_029bf8f3/data.json:generic-api-key:396",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 409,
    "EndLine": 409,
    "StartColumn": 33,
    "EndColumn": 114,
    "Match": "KeyScript\": \"00200b3d269935bf5376248fa97dce2ea8106caff5b854f88f29f171e5f33a704363\"",
    "Secret": "00200b3d269935bf5376248fa97dce2ea8106caff5b854f88f29f171e5f33a704363",
    "File": "src/commonTest/resources/nonreg/v3/Closing_029bf8f3/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8961432,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Closing_029bf8f3/data.json:generic-api-key:409",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 422,
    "EndLine": 422,
    "StartColumn": 33,
    "EndColumn": 114,
    "Match": "KeyScript\": \"0020ac7cc695a72ece2b540ef241a0befb12d85a3480f0de4a6c4d6469c42d64b649\"",
    "Secret": "0020ac7cc695a72ece2b540ef241a0befb12d85a3480f0de4a6c4d6469c42d64b649",
    "File": "src/commonTest/resources/nonreg/v3/Closing_029bf8f3/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.819903,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Closing_029bf8f3/data.json:generic-api-key:422",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 21,
    "EndLine": 21,
    "StartColumn": 36,
    "EndColumn": 87,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "src/commonTest/resources/nonreg/v3/Closing_f137669f/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.690942,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Closing_f137669f/data.json:generic-api-key:21",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 53,
    "EndLine": 53,
    "StartColumn": 25,
    "EndColumn": 98,
    "Match": "Key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v3/Closing_f137669f/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Closing_f137669f/data.json:generic-api-key:53",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 99,
    "EndLine": 99,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/Closing_f137669f/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Closing_f137669f/data.json:generic-api-key:99",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 151,
    "EndLine": 151,
    "StartColumn": 25,
    "EndColumn": 106,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/Closing_f137669f/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Closing_f137669f/data.json:generic-api-key:151",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 170,
    "EndLine": 170,
    "StartColumn": 29,
    "EndColumn": 110,
    "Match": "KeyScript\": \"0020958bb43c9e6d1b985001c2f4dc38d286d7fde78bb04d7ec5a5ae44d8ebf0c40c\"",
    "Secret": "0020958bb43c9e6d1b985001c2f4dc38d286d7fde78bb04d7ec5a5ae44d8ebf0c40c",
    "File": "src/commonTest/resources/nonreg/v3/Closing_f137669f/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8474662,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Closing_f137669f/data.json:generic-api-key:170",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 21,
    "EndLine": 21,
    "StartColumn": 36,
    "EndColumn": 87,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "src/commonTest/resources/nonreg/v3/Closing_0efffae3/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.690942,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Closing_0efffae3/data.json:generic-api-key:21",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 53,
    "EndLine": 53,
    "StartColumn": 25,
    "EndColumn": 98,
    "Match": "Key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v3/Closing_0efffae3/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Closing_0efffae3/data.json:generic-api-key:53",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 100,
    "EndLine": 100,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/Closing_0efffae3/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Closing_0efffae3/data.json:generic-api-key:100",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 152,
    "EndLine": 152,
    "StartColumn": 25,
    "EndColumn": 106,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/Closing_0efffae3/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Closing_0efffae3/data.json:generic-api-key:152",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 176,
    "EndLine": 176,
    "StartColumn": 33,
    "EndColumn": 114,
    "Match": "KeyScript\": \"0020958bb43c9e6d1b985001c2f4dc38d286d7fde78bb04d7ec5a5ae44d8ebf0c40c\"",
    "Secret": "0020958bb43c9e6d1b985001c2f4dc38d286d7fde78bb04d7ec5a5ae44d8ebf0c40c",
    "File": "src/commonTest/resources/nonreg/v3/Closing_0efffae3/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8474662,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Closing_0efffae3/data.json:generic-api-key:176",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 187,
    "EndLine": 187,
    "StartColumn": 33,
    "EndColumn": 114,
    "Match": "KeyScript\": \"0020adccc1c6aa5fb4707079451e579c5dc33c513f0d829346cefe8c80179e09d49c\"",
    "Secret": "0020adccc1c6aa5fb4707079451e579c5dc33c513f0d829346cefe8c80179e09d49c",
    "File": "src/commonTest/resources/nonreg/v3/Closing_0efffae3/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8196247,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Closing_0efffae3/data.json:generic-api-key:187",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 199,
    "EndLine": 199,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"0020290b762b514961d8ea3f1d0a0b7325c43daebfad3751211816c7bba59e607ccf\"",
    "Secret": "0020290b762b514961d8ea3f1d0a0b7325c43daebfad3751211816c7bba59e607ccf",
    "File": "src/commonTest/resources/nonreg/v3/Closing_0efffae3/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.9123697,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Closing_0efffae3/data.json:generic-api-key:199",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 210,
    "EndLine": 210,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00201923dc9176a660d04e0744c51f5e04a3168985962079a6c5d9e2e763170878e1\"",
    "Secret": "00201923dc9176a660d04e0744c51f5e04a3168985962079a6c5d9e2e763170878e1",
    "File": "src/commonTest/resources/nonreg/v3/Closing_0efffae3/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7736492,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Closing_0efffae3/data.json:generic-api-key:210",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 221,
    "EndLine": 221,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00202956089b232f4af35587c10894e6e4351d029b0534fb01f7c3c90285a35d45ff\"",
    "Secret": "00202956089b232f4af35587c10894e6e4351d029b0534fb01f7c3c90285a35d45ff",
    "File": "src/commonTest/resources/nonreg/v3/Closing_0efffae3/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8147776,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Closing_0efffae3/data.json:generic-api-key:221",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 232,
    "EndLine": 232,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"002058acb1c4d7241726756c7e09705500409991b84ce93c86d6ca4eceef76c98cd4\"",
    "Secret": "002058acb1c4d7241726756c7e09705500409991b84ce93c86d6ca4eceef76c98cd4",
    "File": "src/commonTest/resources/nonreg/v3/Closing_0efffae3/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7853658,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Closing_0efffae3/data.json:generic-api-key:232",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 245,
    "EndLine": 245,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"0020adccc1c6aa5fb4707079451e579c5dc33c513f0d829346cefe8c80179e09d49c\"",
    "Secret": "0020adccc1c6aa5fb4707079451e579c5dc33c513f0d829346cefe8c80179e09d49c",
    "File": "src/commonTest/resources/nonreg/v3/Closing_0efffae3/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8196247,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Closing_0efffae3/data.json:generic-api-key:245",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 256,
    "EndLine": 256,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"0020adccc1c6aa5fb4707079451e579c5dc33c513f0d829346cefe8c80179e09d49c\"",
    "Secret": "0020adccc1c6aa5fb4707079451e579c5dc33c513f0d829346cefe8c80179e09d49c",
    "File": "src/commonTest/resources/nonreg/v3/Closing_0efffae3/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8196247,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Closing_0efffae3/data.json:generic-api-key:256",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 21,
    "EndLine": 21,
    "StartColumn": 36,
    "EndColumn": 87,
    "Match": "Key\": \"001434947cfb2e8f6054ddf12daed4308cbe342580d1\"",
    "Secret": "001434947cfb2e8f6054ddf12daed4308cbe342580d1",
    "File": "src/commonTest/resources/nonreg/v3/Negotiating_fadb50c1/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7944684,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Negotiating_fadb50c1/data.json:generic-api-key:21",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 54,
    "EndLine": 54,
    "StartColumn": 25,
    "EndColumn": 98,
    "Match": "Key\": \"02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c\"",
    "Secret": "02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c",
    "File": "src/commonTest/resources/nonreg/v3/Negotiating_fadb50c1/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.815461,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Negotiating_fadb50c1/data.json:generic-api-key:54",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 102,
    "EndLine": 102,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/Negotiating_fadb50c1/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Negotiating_fadb50c1/data.json:generic-api-key:102",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 154,
    "EndLine": 154,
    "StartColumn": 25,
    "EndColumn": 106,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/Negotiating_fadb50c1/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Negotiating_fadb50c1/data.json:generic-api-key:154",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 163,
    "EndLine": 163,
    "StartColumn": 20,
    "EndColumn": 71,
    "Match": "Key\": \"001434947cfb2e8f6054ddf12daed4308cbe342580d1\"",
    "Secret": "001434947cfb2e8f6054ddf12daed4308cbe342580d1",
    "File": "src/commonTest/resources/nonreg/v3/Negotiating_fadb50c1/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7944684,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Negotiating_fadb50c1/data.json:generic-api-key:163",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 21,
    "EndLine": 21,
    "StartColumn": 36,
    "EndColumn": 87,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "src/commonTest/resources/nonreg/v3/Negotiating_da44c6e2/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.690942,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Negotiating_da44c6e2/data.json:generic-api-key:21",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 53,
    "EndLine": 53,
    "StartColumn": 25,
    "EndColumn": 98,
    "Match": "Key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v3/Negotiating_da44c6e2/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Negotiating_da44c6e2/data.json:generic-api-key:53",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 100,
    "EndLine": 100,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/Negotiating_da44c6e2/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Negotiating_da44c6e2/data.json:generic-api-key:100",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 152,
    "EndLine": 152,
    "StartColumn": 25,
    "EndColumn": 106,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/Negotiating_da44c6e2/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Negotiating_da44c6e2/data.json:generic-api-key:152",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 161,
    "EndLine": 161,
    "StartColumn": 20,
    "EndColumn": 71,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "src/commonTest/resources/nonreg/v3/Negotiating_da44c6e2/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.690942,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Negotiating_da44c6e2/data.json:generic-api-key:161",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 165,
    "EndLine": 165,
    "StartColumn": 20,
    "EndColumn": 71,
    "Match": "Key\": \"001434947cfb2e8f6054ddf12daed4308cbe342580d1\"",
    "Secret": "001434947cfb2e8f6054ddf12daed4308cbe342580d1",
    "File": "src/commonTest/resources/nonreg/v3/Negotiating_da44c6e2/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7944684,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Negotiating_da44c6e2/data.json:generic-api-key:165",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 185,
    "EndLine": 185,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/Negotiating_da44c6e2/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Negotiating_da44c6e2/data.json:generic-api-key:185",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 213,
    "EndLine": 213,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/Negotiating_da44c6e2/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Negotiating_da44c6e2/data.json:generic-api-key:213",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 241,
    "EndLine": 241,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/Negotiating_da44c6e2/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Negotiating_da44c6e2/data.json:generic-api-key:241",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 270,
    "EndLine": 270,
    "StartColumn": 25,
    "EndColumn": 106,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/Negotiating_da44c6e2/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Negotiating_da44c6e2/data.json:generic-api-key:270",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 21,
    "EndLine": 21,
    "StartColumn": 36,
    "EndColumn": 87,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "src/commonTest/resources/nonreg/v3/Closing_ebbd24bc/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.690942,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Closing_ebbd24bc/data.json:generic-api-key:21",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 53,
    "EndLine": 53,
    "StartColumn": 25,
    "EndColumn": 98,
    "Match": "Key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v3/Closing_ebbd24bc/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Closing_ebbd24bc/data.json:generic-api-key:53",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 116,
    "EndLine": 116,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/Closing_ebbd24bc/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Closing_ebbd24bc/data.json:generic-api-key:116",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 130,
    "EndLine": 130,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"0020b5abcef667acd30c1793157728653afaf2ea391a4039bc875b6ad468cc2f603a\"",
    "Secret": "0020b5abcef667acd30c1793157728653afaf2ea391a4039bc875b6ad468cc2f603a",
    "File": "src/commonTest/resources/nonreg/v3/Closing_ebbd24bc/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.882854,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Closing_ebbd24bc/data.json:generic-api-key:130",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 147,
    "EndLine": 147,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"0020c24f3aaf87264753868abbf958e8e7dc70352378c068e4c47b02fefc0acd114f\"",
    "Secret": "0020c24f3aaf87264753868abbf958e8e7dc70352378c068e4c47b02fefc0acd114f",
    "File": "src/commonTest/resources/nonreg/v3/Closing_ebbd24bc/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.862224,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Closing_ebbd24bc/data.json:generic-api-key:147",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 226,
    "EndLine": 226,
    "StartColumn": 25,
    "EndColumn": 106,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/Closing_ebbd24bc/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Closing_ebbd24bc/data.json:generic-api-key:226",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 248,
    "EndLine": 248,
    "StartColumn": 29,
    "EndColumn": 110,
    "Match": "KeyScript\": \"0020958bb43c9e6d1b985001c2f4dc38d286d7fde78bb04d7ec5a5ae44d8ebf0c40c\"",
    "Secret": "0020958bb43c9e6d1b985001c2f4dc38d286d7fde78bb04d7ec5a5ae44d8ebf0c40c",
    "File": "src/commonTest/resources/nonreg/v3/Closing_ebbd24bc/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8474662,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Closing_ebbd24bc/data.json:generic-api-key:248",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 261,
    "EndLine": 261,
    "StartColumn": 33,
    "EndColumn": 114,
    "Match": "KeyScript\": \"002018f3224d69b1cc3fbb44bd3779eb2bb07c6c5dc9722fd99f9cd6da90c4f9bd45\"",
    "Secret": "002018f3224d69b1cc3fbb44bd3779eb2bb07c6c5dc9722fd99f9cd6da90c4f9bd45",
    "File": "src/commonTest/resources/nonreg/v3/Closing_ebbd24bc/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7470005,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Closing_ebbd24bc/data.json:generic-api-key:261",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 274,
    "EndLine": 274,
    "StartColumn": 33,
    "EndColumn": 114,
    "Match": "KeyScript\": \"00206c71102142c7c3090165e8d136f64fda559496481a26e89019fc7964812fdfad\"",
    "Secret": "00206c71102142c7c3090165e8d136f64fda559496481a26e89019fc7964812fdfad",
    "File": "src/commonTest/resources/nonreg/v3/Closing_ebbd24bc/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7947228,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Closing_ebbd24bc/data.json:generic-api-key:274",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 21,
    "EndLine": 21,
    "StartColumn": 36,
    "EndColumn": 87,
    "Match": "Key\": \"001434947cfb2e8f6054ddf12daed4308cbe342580d1\"",
    "Secret": "001434947cfb2e8f6054ddf12daed4308cbe342580d1",
    "File": "src/commonTest/resources/nonreg/v3/Normal_ff248f8d/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7944684,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Normal_ff248f8d/data.json:generic-api-key:21",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 53,
    "EndLine": 53,
    "StartColumn": 25,
    "EndColumn": 98,
    "Match": "Key\": \"02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c\"",
    "Secret": "02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c",
    "File": "src/commonTest/resources/nonreg/v3/Normal_ff248f8d/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.815461,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Normal_ff248f8d/data.json:generic-api-key:53",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 100,
    "EndLine": 100,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/Normal_ff248f8d/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Normal_ff248f8d/data.json:generic-api-key:100",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 152,
    "EndLine": 152,
    "StartColumn": 25,
    "EndColumn": 106,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/Normal_ff248f8d/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Normal_ff248f8d/data.json:generic-api-key:152",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 21,
    "EndLine": 21,
    "StartColumn": 36,
    "EndColumn": 87,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "src/commonTest/resources/nonreg/v3/Negotiating_dabbed55/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.690942,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Negotiating_dabbed55/data.json:generic-api-key:21",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 53,
    "EndLine": 53,
    "StartColumn": 25,
    "EndColumn": 98,
    "Match": "Key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v3/Negotiating_dabbed55/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Negotiating_dabbed55/data.json:generic-api-key:53",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 100,
    "EndLine": 100,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/Negotiating_dabbed55/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Negotiating_dabbed55/data.json:generic-api-key:100",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 152,
    "EndLine": 152,
    "StartColumn": 25,
    "EndColumn": 106,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/Negotiating_dabbed55/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Negotiating_dabbed55/data.json:generic-api-key:152",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 161,
    "EndLine": 161,
    "StartColumn": 20,
    "EndColumn": 71,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "src/commonTest/resources/nonreg/v3/Negotiating_dabbed55/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.690942,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Negotiating_dabbed55/data.json:generic-api-key:161",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 165,
    "EndLine": 165,
    "StartColumn": 20,
    "EndColumn": 71,
    "Match": "Key\": \"001434947cfb2e8f6054ddf12daed4308cbe342580d1\"",
    "Secret": "001434947cfb2e8f6054ddf12daed4308cbe342580d1",
    "File": "src/commonTest/resources/nonreg/v3/Negotiating_dabbed55/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7944684,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Negotiating_dabbed55/data.json:generic-api-key:165",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 185,
    "EndLine": 185,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/Negotiating_dabbed55/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Negotiating_dabbed55/data.json:generic-api-key:185",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 213,
    "EndLine": 213,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/Negotiating_dabbed55/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Negotiating_dabbed55/data.json:generic-api-key:213",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 241,
    "EndLine": 241,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/Negotiating_dabbed55/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Negotiating_dabbed55/data.json:generic-api-key:241",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 269,
    "EndLine": 269,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/Negotiating_dabbed55/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Negotiating_dabbed55/data.json:generic-api-key:269",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 297,
    "EndLine": 297,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/Negotiating_dabbed55/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Negotiating_dabbed55/data.json:generic-api-key:297",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 325,
    "EndLine": 325,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/Negotiating_dabbed55/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Negotiating_dabbed55/data.json:generic-api-key:325",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 353,
    "EndLine": 353,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/Negotiating_dabbed55/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Negotiating_dabbed55/data.json:generic-api-key:353",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 382,
    "EndLine": 382,
    "StartColumn": 25,
    "EndColumn": 106,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/Negotiating_dabbed55/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Negotiating_dabbed55/data.json:generic-api-key:382",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 21,
    "EndLine": 21,
    "StartColumn": 36,
    "EndColumn": 87,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "src/commonTest/resources/nonreg/v3/Normal_fd10d3cc/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.690942,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Normal_fd10d3cc/data.json:generic-api-key:21",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 53,
    "EndLine": 53,
    "StartColumn": 25,
    "EndColumn": 98,
    "Match": "Key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v3/Normal_fd10d3cc/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Normal_fd10d3cc/data.json:generic-api-key:53",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 116,
    "EndLine": 116,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/Normal_fd10d3cc/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Normal_fd10d3cc/data.json:generic-api-key:116",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 130,
    "EndLine": 130,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"00200d94818c9b1a70b689daa5af63b6c6737ea40a9de55839819bf0421217d68d80\"",
    "Secret": "00200d94818c9b1a70b689daa5af63b6c6737ea40a9de55839819bf0421217d68d80",
    "File": "src/commonTest/resources/nonreg/v3/Normal_fd10d3cc/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8739407,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Normal_fd10d3cc/data.json:generic-api-key:130",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 147,
    "EndLine": 147,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"0020c9b6634f8ea3a3789e96570513e9bc701395ea27477c07e80931a2ab9bbb549c\"",
    "Secret": "0020c9b6634f8ea3a3789e96570513e9bc701395ea27477c07e80931a2ab9bbb549c",
    "File": "src/commonTest/resources/nonreg/v3/Normal_fd10d3cc/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7739887,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Normal_fd10d3cc/data.json:generic-api-key:147",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 220,
    "EndLine": 220,
    "StartColumn": 25,
    "EndColumn": 106,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/Normal_fd10d3cc/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Normal_fd10d3cc/data.json:generic-api-key:220",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 249,
    "EndLine": 249,
    "StartColumn": 20,
    "EndColumn": 71,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "src/commonTest/resources/nonreg/v3/Normal_fd10d3cc/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.690942,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Normal_fd10d3cc/data.json:generic-api-key:249",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 21,
    "EndLine": 21,
    "StartColumn": 36,
    "EndColumn": 87,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "src/commonTest/resources/nonreg/v3/Normal_fe897b64/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.690942,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Normal_fe897b64/data.json:generic-api-key:21",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 53,
    "EndLine": 53,
    "StartColumn": 25,
    "EndColumn": 98,
    "Match": "Key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v3/Normal_fe897b64/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Normal_fe897b64/data.json:generic-api-key:53",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 100,
    "EndLine": 100,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/Normal_fe897b64/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Normal_fe897b64/data.json:generic-api-key:100",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 193,
    "EndLine": 193,
    "StartColumn": 25,
    "EndColumn": 106,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/Normal_fe897b64/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Normal_fe897b64/data.json:generic-api-key:193",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 23,
    "EndLine": 23,
    "StartColumn": 36,
    "EndColumn": 87,
    "Match": "Key\": \"001434947cfb2e8f6054ddf12daed4308cbe342580d1\"",
    "Secret": "001434947cfb2e8f6054ddf12daed4308cbe342580d1",
    "File": "src/commonTest/resources/nonreg/v3/WaitForFundingConfirmed_fe3c5978/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7944684,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/WaitForFundingConfirmed_fe3c5978/data.json:generic-api-key:23",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 56,
    "EndLine": 56,
    "StartColumn": 25,
    "EndColumn": 98,
    "Match": "Key\": \"02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c\"",
    "Secret": "02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c",
    "File": "src/commonTest/resources/nonreg/v3/WaitForFundingConfirmed_fe3c5978/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.815461,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/WaitForFundingConfirmed_fe3c5978/data.json:generic-api-key:56",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 103,
    "EndLine": 103,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/WaitForFundingConfirmed_fe3c5978/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/WaitForFundingConfirmed_fe3c5978/data.json:generic-api-key:103",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 155,
    "EndLine": 155,
    "StartColumn": 25,
    "EndColumn": 106,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/WaitForFundingConfirmed_fe3c5978/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/WaitForFundingConfirmed_fe3c5978/data.json:generic-api-key:155",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 21,
    "EndLine": 21,
    "StartColumn": 36,
    "EndColumn": 87,
    "Match": "Key\": \"001434947cfb2e8f6054ddf12daed4308cbe342580d1\"",
    "Secret": "001434947cfb2e8f6054ddf12daed4308cbe342580d1",
    "File": "src/commonTest/resources/nonreg/v3/ShuttingDown_ef41a1a5/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7944684,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/ShuttingDown_ef41a1a5/data.json:generic-api-key:21",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 53,
    "EndLine": 53,
    "StartColumn": 25,
    "EndColumn": 98,
    "Match": "Key\": \"02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c\"",
    "Secret": "02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c",
    "File": "src/commonTest/resources/nonreg/v3/ShuttingDown_ef41a1a5/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.815461,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/ShuttingDown_ef41a1a5/data.json:generic-api-key:53",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 116,
    "EndLine": 116,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/ShuttingDown_ef41a1a5/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/ShuttingDown_ef41a1a5/data.json:generic-api-key:116",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 130,
    "EndLine": 130,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"0020cbfc18dfba5112418175b4d4683d6fb064a77a230e2190fd50344d3290d6d4b2\"",
    "Secret": "0020cbfc18dfba5112418175b4d4683d6fb064a77a230e2190fd50344d3290d6d4b2",
    "File": "src/commonTest/resources/nonreg/v3/ShuttingDown_ef41a1a5/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.838173,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/ShuttingDown_ef41a1a5/data.json:generic-api-key:130",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 148,
    "EndLine": 148,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"002039c8ad7d16980a6cea19c49477e44f93844c2fac06d07dcd2ad37e27d4f75071\"",
    "Secret": "002039c8ad7d16980a6cea19c49477e44f93844c2fac06d07dcd2ad37e27d4f75071",
    "File": "src/commonTest/resources/nonreg/v3/ShuttingDown_ef41a1a5/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7608035,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/ShuttingDown_ef41a1a5/data.json:generic-api-key:148",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 257,
    "EndLine": 257,
    "StartColumn": 25,
    "EndColumn": 106,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/ShuttingDown_ef41a1a5/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/ShuttingDown_ef41a1a5/data.json:generic-api-key:257",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 266,
    "EndLine": 266,
    "StartColumn": 20,
    "EndColumn": 71,
    "Match": "Key\": \"001434947cfb2e8f6054ddf12daed4308cbe342580d1\"",
    "Secret": "001434947cfb2e8f6054ddf12daed4308cbe342580d1",
    "File": "src/commonTest/resources/nonreg/v3/ShuttingDown_ef41a1a5/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7944684,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/ShuttingDown_ef41a1a5/data.json:generic-api-key:266",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 270,
    "EndLine": 270,
    "StartColumn": 20,
    "EndColumn": 71,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "src/commonTest/resources/nonreg/v3/ShuttingDown_ef41a1a5/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.690942,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/ShuttingDown_ef41a1a5/data.json:generic-api-key:270",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 21,
    "EndLine": 21,
    "StartColumn": 36,
    "EndColumn": 87,
    "Match": "Key\": \"001434947cfb2e8f6054ddf12daed4308cbe342580d1\"",
    "Secret": "001434947cfb2e8f6054ddf12daed4308cbe342580d1",
    "File": "src/commonTest/resources/nonreg/v3/WaitForFundingLocked_f3437082/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7944684,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/WaitForFundingLocked_f3437082/data.json:generic-api-key:21",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 53,
    "EndLine": 53,
    "StartColumn": 25,
    "EndColumn": 98,
    "Match": "Key\": \"02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c\"",
    "Secret": "02b6eaf304d966a6df90f3b3df7af7be6b1625854bbc096cb8b3507b2a37c2bf9c",
    "File": "src/commonTest/resources/nonreg/v3/WaitForFundingLocked_f3437082/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.815461,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/WaitForFundingLocked_f3437082/data.json:generic-api-key:53",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 100,
    "EndLine": 100,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/WaitForFundingLocked_f3437082/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/WaitForFundingLocked_f3437082/data.json:generic-api-key:100",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 152,
    "EndLine": 152,
    "StartColumn": 25,
    "EndColumn": 106,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/WaitForFundingLocked_f3437082/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/WaitForFundingLocked_f3437082/data.json:generic-api-key:152",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 21,
    "EndLine": 21,
    "StartColumn": 36,
    "EndColumn": 87,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "src/commonTest/resources/nonreg/v3/WaitForFundingConfirmed_ff74dd33/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.690942,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/WaitForFundingConfirmed_ff74dd33/data.json:generic-api-key:21",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 53,
    "EndLine": 53,
    "StartColumn": 25,
    "EndColumn": 98,
    "Match": "Key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v3/WaitForFundingConfirmed_ff74dd33/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/WaitForFundingConfirmed_ff74dd33/data.json:generic-api-key:53",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 100,
    "EndLine": 100,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/WaitForFundingConfirmed_ff74dd33/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/WaitForFundingConfirmed_ff74dd33/data.json:generic-api-key:100",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 152,
    "EndLine": 152,
    "StartColumn": 25,
    "EndColumn": 106,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/WaitForFundingConfirmed_ff74dd33/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/WaitForFundingConfirmed_ff74dd33/data.json:generic-api-key:152",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 21,
    "EndLine": 21,
    "StartColumn": 36,
    "EndColumn": 87,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "src/commonTest/resources/nonreg/v3/Normal_ff4a71b6/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.690942,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Normal_ff4a71b6/data.json:generic-api-key:21",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 53,
    "EndLine": 53,
    "StartColumn": 25,
    "EndColumn": 98,
    "Match": "Key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v3/Normal_ff4a71b6/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Normal_ff4a71b6/data.json:generic-api-key:53",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 132,
    "EndLine": 132,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/Normal_ff4a71b6/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Normal_ff4a71b6/data.json:generic-api-key:132",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 146,
    "EndLine": 146,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"00208bf1a46fa0bd7ca44eda0c5df968544c280859a29a527641afac57c27bf7003f\"",
    "Secret": "00208bf1a46fa0bd7ca44eda0c5df968544c280859a29a527641afac57c27bf7003f",
    "File": "src/commonTest/resources/nonreg/v3/Normal_ff4a71b6/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8157325,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Normal_ff4a71b6/data.json:generic-api-key:146",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 163,
    "EndLine": 163,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"0020de61d4f9864b11e111190b9d11f62ebf7476d517a7bba5b0a5b0f80f4e54eb1f\"",
    "Secret": "0020de61d4f9864b11e111190b9d11f62ebf7476d517a7bba5b0a5b0f80f4e54eb1f",
    "File": "src/commonTest/resources/nonreg/v3/Normal_ff4a71b6/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.647109,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Normal_ff4a71b6/data.json:generic-api-key:163",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 180,
    "EndLine": 180,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"00207805c9f22468069984db4db780b44c97593e4fee2c8ee32114f1c9b4a6d70fe4\"",
    "Secret": "00207805c9f22468069984db4db780b44c97593e4fee2c8ee32114f1c9b4a6d70fe4",
    "File": "src/commonTest/resources/nonreg/v3/Normal_ff4a71b6/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8421054,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Normal_ff4a71b6/data.json:generic-api-key:180",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 330,
    "EndLine": 330,
    "StartColumn": 25,
    "EndColumn": 106,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/Normal_ff4a71b6/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/Normal_ff4a71b6/data.json:generic-api-key:330",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 21,
    "EndLine": 21,
    "StartColumn": 36,
    "EndColumn": 87,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "src/commonTest/resources/nonreg/v3/WaitForRemotePublishFutureCommitment_d803549f/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.690942,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/WaitForRemotePublishFutureCommitment_d803549f/data.json:generic-api-key:21",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 53,
    "EndLine": 53,
    "StartColumn": 25,
    "EndColumn": 98,
    "Match": "Key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v3/WaitForRemotePublishFutureCommitment_d803549f/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/WaitForRemotePublishFutureCommitment_d803549f/data.json:generic-api-key:53",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 123,
    "EndLine": 123,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/WaitForRemotePublishFutureCommitment_d803549f/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/WaitForRemotePublishFutureCommitment_d803549f/data.json:generic-api-key:123",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 137,
    "EndLine": 137,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"0020ff5d65b735c30cb217486f62f8d590e56b6c27192299584ea87dbaa4452ef679\"",
    "Secret": "0020ff5d65b735c30cb217486f62f8d590e56b6c27192299584ea87dbaa4452ef679",
    "File": "src/commonTest/resources/nonreg/v3/WaitForRemotePublishFutureCommitment_d803549f/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.9070091,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/WaitForRemotePublishFutureCommitment_d803549f/data.json:generic-api-key:137",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 154,
    "EndLine": 154,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"00204ce05fb81556eaf7911d606ec23f598ea0e05d43e722c78010e1788774bc8fa6\"",
    "Secret": "00204ce05fb81556eaf7911d606ec23f598ea0e05d43e722c78010e1788774bc8fa6",
    "File": "src/commonTest/resources/nonreg/v3/WaitForRemotePublishFutureCommitment_d803549f/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8532066,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/WaitForRemotePublishFutureCommitment_d803549f/data.json:generic-api-key:154",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 236,
    "EndLine": 236,
    "StartColumn": 25,
    "EndColumn": 106,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/WaitForRemotePublishFutureCommitment_d803549f/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/WaitForRemotePublishFutureCommitment_d803549f/data.json:generic-api-key:236",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 21,
    "EndLine": 21,
    "StartColumn": 36,
    "EndColumn": 87,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "src/commonTest/resources/nonreg/v3/WaitForRemotePublishFutureCommitment_ae47fde9/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.690942,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/WaitForRemotePublishFutureCommitment_ae47fde9/data.json:generic-api-key:21",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 53,
    "EndLine": 53,
    "StartColumn": 25,
    "EndColumn": 98,
    "Match": "Key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v3/WaitForRemotePublishFutureCommitment_ae47fde9/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/WaitForRemotePublishFutureCommitment_ae47fde9/data.json:generic-api-key:53",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 99,
    "EndLine": 99,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/WaitForRemotePublishFutureCommitment_ae47fde9/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/WaitForRemotePublishFutureCommitment_ae47fde9/data.json:generic-api-key:99",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 151,
    "EndLine": 151,
    "StartColumn": 25,
    "EndColumn": 106,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/WaitForRemotePublishFutureCommitment_ae47fde9/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/WaitForRemotePublishFutureCommitment_ae47fde9/data.json:generic-api-key:151",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 21,
    "EndLine": 21,
    "StartColumn": 36,
    "EndColumn": 87,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "src/commonTest/resources/nonreg/v3/ShuttingDown_ef7081a1/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.690942,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/ShuttingDown_ef7081a1/data.json:generic-api-key:21",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 53,
    "EndLine": 53,
    "StartColumn": 25,
    "EndColumn": 98,
    "Match": "Key\": \"0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4\"",
    "Secret": "0385cfd7d8850e4cb8fcbed57310911218e5d5e1fd34f92ef5d9db14d56418caa4",
    "File": "src/commonTest/resources/nonreg/v3/ShuttingDown_ef7081a1/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.8222544,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/ShuttingDown_ef7081a1/data.json:generic-api-key:53",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 108,
    "EndLine": 108,
    "StartColumn": 37,
    "EndColumn": 118,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/ShuttingDown_ef7081a1/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/ShuttingDown_ef7081a1/data.json:generic-api-key:108",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 122,
    "EndLine": 122,
    "StartColumn": 45,
    "EndColumn": 126,
    "Match": "KeyScript\": \"0020e274bcf50e49b0edcce64e854d3aa8da60dbe1e7c48f9110936dadfa5187b58f\"",
    "Secret": "0020e274bcf50e49b0edcce64e854d3aa8da60dbe1e7c48f9110936dadfa5187b58f",
    "File": "src/commonTest/resources/nonreg/v3/ShuttingDown_ef7081a1/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.9123697,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/ShuttingDown_ef7081a1/data.json:generic-api-key:122",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 236,
    "EndLine": 236,
    "StartColumn": 25,
    "EndColumn": 106,
    "Match": "KeyScript\": \"00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8\"",
    "Secret": "00204ba42b50e089764e60e9e0c76c57e41d252d6beb976332f14a7b255bbb5322b8",
    "File": "src/commonTest/resources/nonreg/v3/ShuttingDown_ef7081a1/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7707121,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/ShuttingDown_ef7081a1/data.json:generic-api-key:236",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 248,
    "EndLine": 248,
    "StartColumn": 20,
    "EndColumn": 71,
    "Match": "Key\": \"001405e0104aa726e34ff5cd3a6320d05c0862b5b01c\"",
    "Secret": "001405e0104aa726e34ff5cd3a6320d05c0862b5b01c",
    "File": "src/commonTest/resources/nonreg/v3/ShuttingDown_ef7081a1/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.690942,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/ShuttingDown_ef7081a1/data.json:generic-api-key:248",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 252,
    "EndLine": 252,
    "StartColumn": 20,
    "EndColumn": 71,
    "Match": "Key\": \"001434947cfb2e8f6054ddf12daed4308cbe342580d1\"",
    "Secret": "001434947cfb2e8f6054ddf12daed4308cbe342580d1",
    "File": "src/commonTest/resources/nonreg/v3/ShuttingDown_ef7081a1/data.json",
    "SymlinkFile": "",
    "Commit": "954db4736b1174c1420da726add57bbfdbc21265",
    "Entropy": 3.7944684,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2022-11-07T14:36:16Z",
    "Message": "Rework channel data model and serialization (#391)\n\nSummary:\n- make `ChannelState` only contain channel state. One can now serialize/deserialize channel data without any additional context\n- introduce a new self-contained serialization for channel persistence that doesn't depend on `kotlinx.serialization`\n- introduce a new self-contained serialization for json, using external serializers\n- clean up legacy serialization code by making it read-only and removing unused code\n- make legacy serialization code as self-contained as possible using external serializers, and clean up the codebase from `Serializable` annotations\n- add a comprehensive non-reg test for legacy serialization\n\nDetails:\n\n* remove v1 serialization\n\nIt has never been used for mainnet.\n\n* make v2/v3 classes internal\n\nInternal persistence details should stay, well internal.\n\nIdeally they should be package-private like in eclair, but unfortunately\nKotlin doesn't allow that. We could merge everything (codecs, classes) in\na single file and mark the content private, but it makes the change more\ncomplex for little advantage.\n\nTook the opportunity to clean up unused classes.\n\n* remove redundant polymorphic class registration\n\nManual registration of subclasses of `sealed class` is not needed since\nkotlinx.serialization v0.14.0, released ages ago.\n\nSee (now removed) doc paragraph about that, removed since:\n\n> Sealed classes: before 0.14.0\n>If you, for some reason, use an older version of the library, to serialize sealed hierarchies polymorphically, you have to perform the same steps as for any other class hierarchy: make a SerialModule, register all subclasses, etc., etc. Moreover, you should explicitly mark every usage of sealed class in @Serializable classes as @Polymorphic\n\n(src:\nhttps://github.com/Kotlin/kotlinx.serialization/blob/65fdbc7d8ac24c96110a6b1230f7a361e5e145bc/docs/polymorphism.md#sealed-classes-before-0140)\n\n* remove non-persisted classes from binary codecs\n\nIn an attempt to minimize potential deserialization risks, we have\nduplicated `ChannelState` classes with mirror classes that are supposed\nto be immutable. But we actually only need to duplicate subclasses of\n`ChannelStateWithCommitments`, because the other ones are not persisted.\n\nThe confusion stems for the fact that there was an ambiguity between\njson and binary serialization. But they have a different scope:\n- json serialization should apply to (original) subclasses of `ChannelState`,\nincluding the ones that are not persisted\n- binary serialization should apply to (mirror) subclasses of\n  `ChannelStateWithCommitments`.\n\n* new channel data model [no ci]\n\nOur current model for channel data is flawed because `ChannelState`\ncontains data that is unrelated to the actual channel state. This is\nwhy deserializing channel data requires an `NodeParams` to be provided,\ninstead of simply being a piece of data that can be serialized and\ndeserialized without context.\n\nThe non-channel-related data in question already stands out as the only\n`abstract` values at the top of the `ChannelState` hierarchy, and they\nare the same for all channel states. In eclair, those would typically be\nprovided in the constructor of the `FSM` actor (as opposed to attributes\nof the data class).\n\nIn this commit, we separate the channel state from the context in which\nthe channel operates (which we could also call the Machine of the FSM).\nFor clarity, below is the gist of the change. The `State`/`Command`/\n`Action` triplet remains the same, but we have a new `Context`, which\ncarries external context (node params, current block count, etc.).\nBecause `process` is now an _extension_ function of `Context`, it has\naccess to both the context and state data from its body. This is what\nmakes the change almost a drop-in replacement. Running a command on a\nstate is now slightly more complex, but it is also closer to the eclair\ncodebase.\n\n```kotlin\nsealed class Command {\n    object Command1 : Command()\n}\n\nsealed class Action {\n    object Action1 : Action()\n}\n\nsealed class State {\n\n    abstract fun Context.process(cmd: Command): Pair<State, List<Action>>\n\n    data class State1(val a: String) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            // we have access to both the context and the state\n            val params = this.params\n            val state = this@State1\n            return State2(10) to listOf<Action.Action1>()\n        }\n    }\n\n    data class State2(val b: Int) : State() {\n        override fun Context.process(cmd: Command): Pair<State, List<Action>> {\n            TODO(\"Not yet implemented\")\n        }\n    }\n}\n\ndata class Params(val alias: String)\n\ndata class Context(val params: Params)\n\nfun test() {\n    val context = Context(Params(alias = \"my node\"))\n    val state = State.State1(\"foobar\")\n\n    val (state1, actions) = state.run { context.process(Command.Command1) }\n}\n```\n\nNotes:\n- overall there is less code than before because we remove unnecessary\ndata that we were carrying around for no reason\n- end states like `Aborted` are now `object`s\n- the channel state containing `currentTip` was misleading, because it\nwas not consistent with pending htlcs\n\n* update tests\n\nNB:\n- got rid of `CurrentBlockCount` command entirely, it's not needed\n  anymore since the block count is part of the context now\n- a few tests have been ignored, it's easier to continue the rework of\nserialization before fixing them\n\n* factor and decouple serialization/encryption\n\nIt makes sense because:\n- in the context of local persistence we use only serialization\n- in the context of restore we use serialization + encryption\n\n* add a new v4 serialization [no ci]\n\nIt uses a custom serialization, contrary to v2 and v3 which use\n`kotlinx-serialization`.\n\nThe tradeoff for initially going with `kotlinx-serialization` was to\naccept using a serialization mechanism that we don't fully control, in\nexchange for more automation and less code. On top of that, we get json\nserialization for free.\n\nHowever, because channel data is so sensitive, we are not fully\nconfident in the serialization system (particularly w.r.t data model\nmigrations). In an attempt to reduce the risk of not being able to read\nolder data in the future, we have mirrored most of channel classes in\nthe `vX` packages, and we serialize those mirror classes, instead of the\nactual ones.\n\nWe end up with well over a thousand lines of code for each version of\nthe serialization *and* the complexity inherent to\n`kotlinx.serialization` with the concept of annotations, surrogate\nserializers, modules, encoders and decoders, etc. On top of that, it\nturns out json doesn't come for free since the variations in encoding\nbetween json and binary spills over in various parts of the codebase in\nthe form of `@Contextual` annotations. In other words the tradeoff\ndoesn't seem to be worth it.\n\nWith custom serialization we get full control of the system, but we have\npotentially more code. It turns out that it's actually not that much\n(especially if we delegate relevant parts to existing bitcoin/lightning\nprotocol serializers) and, critically, it's systematic code with few\nconcepts to be aware of. In fact, there is a good chance that we end up\nwith less code than what we had before for each serialization version.\n\nOne drawback of custom serialization is that, since we can't rely on\nreflection, and aren't using a compiler plugin like\n`kotlinx.serialization`, the handling of polymorphism isn't very nice.\nIt's trivial to serialize child classes by prefixing with a\ndiscriminator, but deserializing is another matter, as we have no way\nto resolve a class constructor corresponding to a discriminator. As a\nresult, we can't factor the discriminator<->class correspondance which\nmeans we need to be careful to keep serialization and deserialization\nin sync.\n\nRegarding json, I believe that we should still be using\n`kotlinx-seriaization`, but in a different, non-intrusive way. We could\ndefine a `json` package with serializers generated using the new\n`forClass` attribute of `@Serializable`. This would free our regular\nclasses of all the annotation mess, and we wouldn't need `@Contextual`\nanymore due to only using json. There is a catch though, as we still\nneed to handle backward compatibility for binary codecs relying on those\nannotations. I think we can use the same `forClass` trick to duplicate\nthe necessary serializers in `vX`.\n\n* revert dual funding changes to v3\n\nWhen adding support for dual funding, we updated v3 serialization. But\nit has never been released, and now we have a brand new v4\nserialization, so we will never use v3 for dual funding and can revert\nthose changes.\n\n* make v2/v3 serialization read-only\n\nOlder serialization scheme should only be used to read older data, not\nto write. We can remove all conversions from _live_ to _mirror_ classes,\nand serialization functions.\n\nNB: non-reg tests have been removed and will be added back later. We\ndidn't check v2 data.\n\n* add json support and non-reg tests\n\n* clean up annotations from the codebase\n\nNow that we have non-reg tests, we can clean up our codebase by removing\nall serialization-related annotations from our main classes, and define\nexternal serializers in the `serialization` and `json` packages. There\nis some duplication, but it will allow us to easily remove a\nserialization version in the future, and keeps our main classes\nannotation-free.\n\nThe following classes have been left untouched:\n- `CltvExpiry`/`CltvExpiryDelta`/`ShortChannelId`: because they have a\n  private attribute that prevent external serializer generation. Btw,\n  setting `MilliSatoshi.msat` private (as it should be) would break\n  `v2`/`v3` serialization for the same reason.\n- `Feature`, `TransactionWithInputInfo` and all their subclasses,\n  because we would have to define a serializer for every single one of\n  them in `v2`, `v3` and `json`, as well as manual `polymorphic`\n  declarations in `SerializersModule` of each of those packages. The\n  kotlin compiler plugin does a lot of heavy lifting for `sealed class`.\n  I did that work for `LightningMessage` because it was worth it to\n  clean up those classes.\n\n* introduce a PersistedChannelState state\n\nNot all `ChannelStateWithCommitments` are persisted, for example\n`Offline`, `Syncing` are not.\n\nThere is a potential backward incompatibility on old serialized data for\n`ErrorInformationLeak` (which I'm not tagging as\n`PersistedChannelState`), but we shouldn't have any.\n\nNote that in eclair `Closed` isn't peristed either, but I left it here\nbecause it does make sense, and that would certainly cause backward\ncompat issues anyway.\n\n* remove legacy FundingLocked from wire package\n\nWe duplicate the class in `v2`/`v3`. There is no need to do fancy\nbackward compat with class names, because we never used the class in a\npolymorphic way, it was always referenced directly, not by a super\nclass.\n\n* serialize all ChannelState\n\nSo the caller doesn't have to worry about what ChannelState is\nserializable. We use a placeholder serializer for remaining channel\nstates.\n\n* remove Node",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "954db4736b1174c1420da726add57bbfdbc21265:src/commonTest/resources/nonreg/v3/ShuttingDown_ef7081a1/data.json:generic-api-key:252",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 120,
    "EndLine": 121,
    "StartColumn": 39,
    "EndColumn": 1,
    "Match": "key: 1552dfba4f6cf29a62a0af13c8d6981d36d0ef8d61ba10fb0fe90da7634d7e1301",
    "Secret": "1552dfba4f6cf29a62a0af13c8d6981d36d0ef8d61ba10fb0fe90da7634d7e1301",
    "File": "src/commonTest/kotlin/fr/acinq/eclair/transactions/AnchorOutputsTestsCommon.kt",
    "SymlinkFile": "",
    "Commit": "145cd2557de8a13db83ec7926ce48494325d3a1c",
    "Entropy": 3.843512,
    "Author": "Fabrice Drouin",
    "Email": "sstone@users.noreply.github.com",
    "Date": "2020-11-30T11:29:29Z",
    "Message": "Implement \"anchor outputs\" commitments (#112)\n\n* Add a new \"anchor outputs\" type of Commitments format\n\n* Cleanup and move test vectors to their own resource file\n\n* Always use the anchor_outputs commitment format\n\nLegacy format code is removed.\n\n* Make sighash parameter mandatory\n\n* Don't require anchor_outputs support on connnection\n\nWe set our feature bit to optional, but still require that channels opened to use are anchor_outputs channels (this\ncheck is done later).\nThis will allow our peer to open anchor_outputs channels to Phoenix without advertising it in their init message.\n\nCo-authored-by: Bastien Teinturier <31281497+t-bast@users.noreply.github.com>",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "145cd2557de8a13db83ec7926ce48494325d3a1c:src/commonTest/kotlin/fr/acinq/eclair/transactions/AnchorOutputsTestsCommon.kt:generic-api-key:120",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 131,
    "EndLine": 132,
    "StartColumn": 31,
    "EndColumn": 1,
    "Match": "key: 8deba327a7cc6d638ab0eb025770400a6184afcba6713c210d8d10e199ff2fda01",
    "Secret": "8deba327a7cc6d638ab0eb025770400a6184afcba6713c210d8d10e199ff2fda01",
    "File": "src/commonTest/kotlin/fr/acinq/eclair/transactions/AnchorOutputsTestsCommon.kt",
    "SymlinkFile": "",
    "Commit": "145cd2557de8a13db83ec7926ce48494325d3a1c",
    "Entropy": 3.8642378,
    "Author": "Fabrice Drouin",
    "Email": "sstone@users.noreply.github.com",
    "Date": "2020-11-30T11:29:29Z",
    "Message": "Implement \"anchor outputs\" commitments (#112)\n\n* Add a new \"anchor outputs\" type of Commitments format\n\n* Cleanup and move test vectors to their own resource file\n\n* Always use the anchor_outputs commitment format\n\nLegacy format code is removed.\n\n* Make sighash parameter mandatory\n\n* Don't require anchor_outputs support on connnection\n\nWe set our feature bit to optional, but still require that channels opened to use are anchor_outputs channels (this\ncheck is done later).\nThis will allow our peer to open anchor_outputs channels to Phoenix without advertising it in their init message.\n\nCo-authored-by: Bastien Teinturier <31281497+t-bast@users.noreply.github.com>",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "145cd2557de8a13db83ec7926ce48494325d3a1c:src/commonTest/kotlin/fr/acinq/eclair/transactions/AnchorOutputsTestsCommon.kt:generic-api-key:131",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 133,
    "EndLine": 134,
    "StartColumn": 38,
    "EndColumn": 1,
    "Match": "key: adf3464ce9c2f230fd2582fda4c6965e4993ca5524e8c9580e3df0cf226981ad01",
    "Secret": "adf3464ce9c2f230fd2582fda4c6965e4993ca5524e8c9580e3df0cf226981ad01",
    "File": "src/commonTest/kotlin/fr/acinq/eclair/transactions/AnchorOutputsTestsCommon.kt",
    "SymlinkFile": "",
    "Commit": "145cd2557de8a13db83ec7926ce48494325d3a1c",
    "Entropy": 3.756368,
    "Author": "Fabrice Drouin",
    "Email": "sstone@users.noreply.github.com",
    "Date": "2020-11-30T11:29:29Z",
    "Message": "Implement \"anchor outputs\" commitments (#112)\n\n* Add a new \"anchor outputs\" type of Commitments format\n\n* Cleanup and move test vectors to their own resource file\n\n* Always use the anchor_outputs commitment format\n\nLegacy format code is removed.\n\n* Make sighash parameter mandatory\n\n* Don't require anchor_outputs support on connnection\n\nWe set our feature bit to optional, but still require that channels opened to use are anchor_outputs channels (this\ncheck is done later).\nThis will allow our peer to open anchor_outputs channels to Phoenix without advertising it in their init message.\n\nCo-authored-by: Bastien Teinturier <31281497+t-bast@users.noreply.github.com>",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "145cd2557de8a13db83ec7926ce48494325d3a1c:src/commonTest/kotlin/fr/acinq/eclair/transactions/AnchorOutputsTestsCommon.kt:generic-api-key:133",
    "repo": "LightningEver-eclair-kmp"
  },
  {
    "Description": "Detected a Generic API Key, potentially exposing access to various services and sensitive operations.",
    "StartLine": 557,
    "EndLine": 557,
    "StartColumn": 33,
    "EndColumn": 58,
    "Match": "Secret = ByteVector32.One ",
    "Secret": "ByteVector32.One",
    "File": "src/commonTest/kotlin/fr/acinq/eclair/payment/IncomingPaymentHandlerTestsCommon.kt",
    "SymlinkFile": "",
    "Commit": "d4bd1096be06227a1b3fe29f4acf822b9bc336b2",
    "Entropy": 3.5778196,
    "Author": "Pierre-Marie Padiou",
    "Email": "pm47@users.noreply.github.com",
    "Date": "2020-11-18T14:31:43Z",
    "Message": "Pay-to-open: return proper onion-encrypted failure messages (#106)\n\nWe now return proper errors instead of having endurance return a generic `UnknownNextPeer` error.\n\nCo-authored-by: Fabrice Drouin <sstone@users.noreply.github.com>",
    "Tags": [],
    "RuleID": "generic-api-key",
    "Fingerprint": "d4bd1096be06227a1b3fe29f4acf822b9bc336b2:src/commonTest/kotlin/fr/acinq/eclair/payment/IncomingPaymentHandlerTestsCommon.kt:generic-api-key:557",
    "repo": "LightningEver-eclair-kmp"
  }
]
